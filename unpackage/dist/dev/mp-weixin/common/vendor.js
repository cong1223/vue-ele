(global["webpackJsonp"] = global["webpackJsonp"] || []).push([["common/vendor"],{

/***/ "../../../workspace/vue-ele/data/cityData.js":
/*!*********************************************!*\
  !*** D:/workspace/vue-ele/data/cityData.js ***!
  \*********************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

var cityData = {
  A: [
  { code: 2013, name: "阿巴嘎旗", pinyin: "abagaqi", shouzimu: "e" },
  { code: 6802, name: "阿坝县", pinyin: "abaxian", shouzimu: "e" },
  { code: 2610, name: "阿城市", pinyin: "achengshi", shouzimu: "e" },
  { code: 1986, name: "阿尔山市", pinyin: "aershanshi", shouzimu: "e" },
  { code: 8933, name: "阿合奇县", pinyin: "aheqixian", shouzimu: "e" },
  { code: 8266, name: "阿克塞县", pinyin: "akesaixian", shouzimu: "e" },
  { code: 8910, name: "阿克苏市", pinyin: "akesushi", shouzimu: "e" },
  { code: 8932, name: "阿克陶县", pinyin: "aketaoxian", shouzimu: "e" },
  { code: 9031, name: "阿拉尔市", pinyin: "alaershi", shouzimu: "e" },
  { code: 8847, name: "阿拉山口口岸", pinyin: "alashankoukouan", shouzimu: "e" },
  { code: 2080, name: "阿拉善盟", pinyin: "alashanmeng", shouzimu: "e" },
  { code: 9020, name: "阿勒泰市", pinyin: "aletaishi", shouzimu: "e" },
  { code: 1941, name: "阿鲁科尔沁旗", pinyin: "alukeerqinqi", shouzimu: "e" },
  { code: 1965, name: "阿荣旗", pinyin: "arongqi", shouzimu: "e" },
  { code: 8930, name: "阿图什市", pinyin: "atushenshi", shouzimu: "e" },
  { code: 8918, name: "阿瓦提县", pinyin: "awatixian", shouzimu: "e" },
  { code: 2082, name: "阿右旗", pinyin: "ayouqi", shouzimu: "e" },
  { code: 2762, name: "安达市", pinyin: "andashi", shouzimu: "a" },
  { code: 7794, name: "安多县", pinyin: "anduoxian", shouzimu: "a" },
  { code: 4362, name: "安福县", pinyin: "anfuxian", shouzimu: "a" },
  { code: 1369, name: "安国市", pinyin: "anguoshi", shouzimu: "a" },
  { code: 5616, name: "安化县", pinyin: "anhuaxian", shouzimu: "a" },
  { code: 3363, name: "安吉县", pinyin: "anjixian", shouzimu: "a" },
  { code: 8010, name: "安康市", pinyin: "ankangshi", shouzimu: "a" },
  { code: 7078, name: "安龙县", pinyin: "anlongxian", shouzimu: "a" },
  { code: 5353, name: "安陆市", pinyin: "anlushi", shouzimu: "a" },
  { code: 7310, name: "安宁市", pinyin: "anningshi", shouzimu: "a" },
  { code: 1488, name: "安平县", pinyin: "anpingxian", shouzimu: "a" },
  { code: 3680, name: "安庆市", pinyin: "anqingshi", shouzimu: "a" },
  { code: 4581, name: "安丘市", pinyin: "anqiushi", shouzimu: "a" },
  { code: 5642, name: "安仁县", pinyin: "anrenxian", shouzimu: "a" },
  { code: 8045, name: "安塞县", pinyin: "ansaixian", shouzimu: "a" },
  { code: 7110, name: "安顺市", pinyin: "anshunshi", shouzimu: "a" },
  { code: 2498, name: "安图县", pinyin: "antuxian", shouzimu: "a" },
  { code: 8267, name: "安西县", pinyin: "anxixian", shouzimu: "a" },
  { code: 3974, name: "安溪县", pinyin: "anxixian", shouzimu: "a" },
  { code: 6593, name: "安县", pinyin: "anxian", shouzimu: "a" },
  { code: 5581, name: "安乡县", pinyin: "anxiangxian", shouzimu: "a" },
  { code: 1363, name: "安新县", pinyin: "anxinxian", shouzimu: "a" },
  { code: 4960, name: "安阳市", pinyin: "anyangshi", shouzimu: "a" },
  { code: 4962, name: "安阳县", pinyin: "anyangxian", shouzimu: "a" },
  { code: 4210, name: "安义县", pinyin: "anyixian", shouzimu: "a" },
  { code: 4288, name: "安远县", pinyin: "anyuanxian", shouzimu: "a" },
  { code: 6633, name: "安岳县", pinyin: "anyuexian", shouzimu: "a" },
  { code: 1779, name: "安泽县", pinyin: "anzexian", shouzimu: "a" },
  { code: 2230, name: "鞍山市", pinyin: "anshanshi", shouzimu: "a" },
  { code: 7766, name: "昂仁县", pinyin: "angrenxian", shouzimu: "a" },
  { code: 1949, name: "敖汉旗", pinyin: "aohanqi", shouzimu: "a" }],

  B: [
  { code: 7726, name: "八宿县", pinyin: "baxiuxian", shouzimu: "b" },
  { code: 8952, name: "巴楚县", pinyin: "bachuxian", shouzimu: "b" },
  { code: 5414, name: "巴东县", pinyin: "badongxian", shouzimu: "b" },
  { code: 8842, name: "巴里坤哈萨克自治县", pinyin: "balikunhasakezizhixian", shouzimu: "b" },
  { code: 1943, name: "巴林右旗", pinyin: "balinyouqi", shouzimu: "b" },
  { code: 1942, name: "巴林左旗", pinyin: "balinzuoqi", shouzimu: "b" },
  { code: 6289, name: "巴马县", pinyin: "bamaxian", shouzimu: "b" },
  { code: 6530, name: "巴南区", pinyin: "bananqu", shouzimu: "b" },
  { code: 7798, name: "巴青县", pinyin: "baqingxian", shouzimu: "b" },
  { code: 6826, name: "巴塘县", pinyin: "batangxian", shouzimu: "b" },
  { code: 2070, name: "巴彦淖尔盟", pinyin: "bayannaoermeng", shouzimu: "b" },
  { code: 2610, name: "巴彦县", pinyin: "bayanxian", shouzimu: "b" },
  { code: 6758, name: "巴中市", pinyin: "bazhongshi", shouzimu: "b" },
  { code: 1467, name: "霸州市", pinyin: "bazhoushi", shouzimu: "b" },
  { code: 2470, name: "白城市", pinyin: "baichengshi", shouzimu: "b" },
  { code: 8021, name: "白河县", pinyin: "baihexian", shouzimu: "b" },
  { code: 7768, name: "白朗县", pinyin: "bailangxian", shouzimu: "b" },
  { code: 6410, name: "白沙县", pinyin: "baishaxian", shouzimu: "b" },
  { code: 2460, name: "白山市", pinyin: "baishanshi", shouzimu: "b" },
  { code: 7979, name: "白水县", pinyin: "baishuixian", shouzimu: "b" },
  { code: 8240, name: "白银市", pinyin: "baiyinshi", shouzimu: "b" },
  { code: 6822, name: "白玉县", pinyin: "baiyuxian", shouzimu: "b" },
  { code: 6261, name: "百色市", pinyin: "baiseshi", shouzimu: "b" },
  { code: 1325, name: "柏乡县", pinyin: "baixiangxian", shouzimu: "b" },
  { code: 8916, name: "拜城县", pinyin: "baichengxian", shouzimu: "b" },
  { code: 2652, name: "拜泉县", pinyin: "baiquanxian", shouzimu: "b" },
  { code: 7797, name: "班戈县", pinyin: "bangexian", shouzimu: "b" },
  { code: 8572, name: "班玛县", pinyin: "banmaxian", shouzimu: "b" },
  { code: 3630, name: "蚌埠市", pinyin: "bengbushi", shouzimu: "b" },
  { code: 1920, name: "包头市", pinyin: "baotoushi", shouzimu: "b" },
  { code: 4951, name: "宝丰县", pinyin: "baofengxian", shouzimu: "b" },
  { code: 7930, name: "宝鸡市", pinyin: "baojishi", shouzimu: "b" },
  { code: 2724, name: "宝清县", pinyin: "baoqingxian", shouzimu: "b" },
  { code: 6778, name: "宝兴县", pinyin: "baoxingxian", shouzimu: "b" },
  { code: 3122, name: "宝应县", pinyin: "baoyingxian", shouzimu: "b" },
  { code: 1724, name: "保德县", pinyin: "baodexian", shouzimu: "b" },
  { code: 1340, name: "保定市", pinyin: "baodingshi", shouzimu: "b" },
  { code: 5695, name: "保靖县", pinyin: "baojingxian", shouzimu: "b" },
  { code: 5285, name: "保康县", pinyin: "baokangxian", shouzimu: "b" },
  { code: 7530, name: "保山市", pinyin: "baoshanshi", shouzimu: "b" },
  { code: 6410, name: "保亭县", pinyin: "baotingxian", shouzimu: "b" },
  { code: 2782, name: "北安市", pinyin: "beianshi", shouzimu: "b" },
  { code: 6595, name: "北川县", pinyin: "beichuanxian", shouzimu: "b" },
  { code: 6230, name: "北海市", pinyin: "beihaishi", shouzimu: "b" },
  { code: 1000, name: "北京市", pinyin: "beijingshi", shouzimu: "b" },
  { code: 6246, name: "北流市", pinyin: "beiliushi", shouzimu: "b" },
  { code: 2273, name: "北宁市", pinyin: "beiningshi", shouzimu: "b" },
  { code: 2346, name: "北票市", pinyin: "beipiaoshi", shouzimu: "b" },
  { code: 8844, name: "北屯镇", pinyin: "beitunzhen", shouzimu: "b" },
  { code: 2250, name: "本溪市", pinyin: "benxishi", shouzimu: "b" },
  { code: 2251, name: "本溪县", pinyin: "benxixian", shouzimu: "b" },
  { code: 7792, name: "比如县", pinyin: "biruxian", shouzimu: "b" },
  { code: 7090, name: "毕节市", pinyin: "bijieshi", shouzimu: "b" },
  { code: 6530, name: "璧山县", pinyin: "bishanxian", shouzimu: "b" },
  { code: 7730, name: "边坝县", pinyin: "bianbaxian", shouzimu: "b" },
  { code: 7514, name: "宾川县", pinyin: "binchuanxian", shouzimu: "b" },
  { code: 2610, name: "宾县", pinyin: "binxian", shouzimu: "b" },
  { code: 6110, name: "宾阳县", pinyin: "binyangxian", shouzimu: "b" },
  { code: 7957, name: "彬县", pinyin: "binxian", shouzimu: "b" },
  { code: 3112, name: "滨海县", pinyin: "binhaixian", shouzimu: "b" },
  { code: 4660, name: "滨州市", pinyin: "binzhoushi", shouzimu: "b" },
  { code: 7834, name: "波密县", pinyin: "bomixian", shouzimu: "b" },
  { code: 1441, name: "泊头市", pinyin: "botoushi", shouzimu: "b" },
  { code: 2741, name: "勃利县", pinyin: "bolixian", shouzimu: "b" },
  { code: 3722, name: "亳州市", pinyin: "bozhoushi", shouzimu: "b" },
  { code: 5012, name: "博爱县", pinyin: "boaixian", shouzimu: "b" },
  { code: 6248, name: "博白县", pinyin: "bobaixian", shouzimu: "b" },
  { code: 8889, name: "博湖县", pinyin: "bohuxian", shouzimu: "b" },
  { code: 8870, name: "博乐市", pinyin: "boleshi", shouzimu: "b" },
  { code: 5952, name: "博罗县", pinyin: "boluoxian", shouzimu: "b" },
  { code: 4666, name: "博兴县", pinyin: "boxingxian", shouzimu: "b" },
  { code: 1371, name: "博野县", pinyin: "boyexian", shouzimu: "b" },
  { code: 9022, name: "布尔津县", pinyin: "buerjinxian", shouzimu: "b" },
  { code: 6849, name: "布拖县", pinyin: "butuoxian", shouzimu: "b" }],

  C: [
  { code: 2463, name: "长白县", pinyin: "changbaixian", shouzimu: "c" },
  { code: 2410, name: "长春市", pinyin: "changchunshi", shouzimu: "c" },
  { code: 4566, name: "长岛县", pinyin: "changdaoxian", shouzimu: "c" },
  { code: 3610, name: "长丰县", pinyin: "changfengxian", shouzimu: "c" },
  { code: 5031, name: "长葛市", pinyin: "changgeshi", shouzimu: "c" },
  { code: 2225, name: "长海县", pinyin: "changhaixian", shouzimu: "c" },
  { code: 3910, name: "长乐市", pinyin: "changleshi", shouzimu: "c" },
  { code: 2475, name: "长岭县", pinyin: "changlingxian", shouzimu: "c" },
  { code: 6715, name: "长宁县", pinyin: "changningxian", shouzimu: "c" },
  { code: 5510, name: "长沙市", pinyin: "changshashi", shouzimu: "c" },
  { code: 6530, name: "长寿区", pinyin: "changshouqu", shouzimu: "c" },
  { code: 7159, name: "长顺县", pinyin: "changshunxian", shouzimu: "c" },
  { code: 3995, name: "长泰县", pinyin: "changtaixian", shouzimu: "c" },
  { code: 4052, name: "长汀县", pinyin: "changtingxian", shouzimu: "c" },
  { code: 7958, name: "长武县", pinyin: "changwuxian", shouzimu: "c" },
  { code: 3362, name: "长兴县", pinyin: "changxingxian", shouzimu: "c" },
  { code: 5268, name: "长阳县", pinyin: "changyangxian", shouzimu: "c" },
  { code: 4986, name: "长垣县", pinyin: "changyuanxian", shouzimu: "c" },
  { code: 1640, name: "长治市", pinyin: "changzhishi", shouzimu: "c" },
  { code: 1661, name: "长治县", pinyin: "changzhixian", shouzimu: "c" },
  { code: 1668, name: "长子县", pinyin: "changzixian", shouzimu: "c" },
  { code: 3336, name: "苍南县", pinyin: "cangnanxian", shouzimu: "c" },
  { code: 4734, name: "苍山县", pinyin: "cangshanxian", shouzimu: "c" },
  { code: 6211, name: "苍梧县", pinyin: "cangwuxian", shouzimu: "c" },
  { code: 6614, name: "苍溪县", pinyin: "cangxixian", shouzimu: "c" },
  { code: 1431, name: "沧县", pinyin: "cangxian", shouzimu: "c" },
  { code: 7588, name: "沧源县", pinyin: "cangyuanxian", shouzimu: "c" },
  { code: 1430, name: "沧州市", pinyin: "cangzhoushi", shouzimu: "c" },
  { code: 4752, name: "曹县", pinyin: "caoxian", shouzimu: "c" },
  { code: 7077, name: "册亨县", pinyin: "cehengxian", shouzimu: "c" },
  { code: 8966, name: "策勒县", pinyin: "celexian", shouzimu: "c" },
  { code: 7136, name: "岑巩县", pinyin: "cengongxian", shouzimu: "c" },
  { code: 6221, name: "岑溪市", pinyin: "cenxishi", shouzimu: "c" },
  { code: 5523, name: "茶陵县", pinyin: "chalingxian", shouzimu: "c" },
  { code: 8993, name: "察布查尔锡伯自治县", pinyin: "chabuzhaerxibozizhixian", shouzimu: "c" },
  { code: 2044, name: "察哈尔右翼后旗", pinyin: "chahaeryouyihouqi", shouzimu: "c" },
  { code: 2042, name: "察哈尔右翼前旗", pinyin: "chahaeryouyiqianqi", shouzimu: "c" },
  { code: 2043, name: "察哈尔右翼中旗", pinyin: "chahaeryouyizhongqi", shouzimu: "c" },
  { code: 7725, name: "察雅县", pinyin: "chayaxian", shouzimu: "c" },
  { code: 7835, name: "察隅县", pinyin: "chayuxian", shouzimu: "c" },
  { code: 7720, name: "昌都县", pinyin: "changdouxian", shouzimu: "c" },
  { code: 8850, name: "昌吉市", pinyin: "changjishi", shouzimu: "c" },
  { code: 6410, name: "昌江县", pinyin: "changjiangxian", shouzimu: "c" },
  { code: 4584, name: "昌乐县", pinyin: "changlexian", shouzimu: "c" },
  { code: 1262, name: "昌黎县", pinyin: "changlixian", shouzimu: "c" },
  { code: 7535, name: "昌宁县", pinyin: "changningxian", shouzimu: "c" },
  { code: 2333, name: "昌图县", pinyin: "changtuxian", shouzimu: "c" },
  { code: 4585, name: "昌邑市", pinyin: "changyishi", shouzimu: "c" },
  { code: 5580, name: "常德市", pinyin: "changdeshi", shouzimu: "c" },
  { code: 5545, name: "常宁市", pinyin: "changningshi", shouzimu: "c" },
  { code: 3412, name: "常山县", pinyin: "changshanxian", shouzimu: "c" },
  { code: 3055, name: "常熟市", pinyin: "changshushi", shouzimu: "c" },
  { code: 3040, name: "常州市", pinyin: "changzhoushi", shouzimu: "c" },
  { code: 3781, name: "巢湖市", pinyin: "chaohushi", shouzimu: "c" },
  { code: 2340, name: "朝阳市", pinyin: "chaoyangshi", shouzimu: "c" },
  { code: 5872, name: "潮安营业部", pinyin: "chaoanyingyebu", shouzimu: "c" },
  { code: 5860, name: "潮阳市", pinyin: "chaoyangshi", shouzimu: "c" },
  { code: 5869, name: "潮州市", pinyin: "chaozhoushi", shouzimu: "c" },
  { code: 5630, name: "郴州市", pinyin: "chenzhoushi", shouzimu: "c" },
  { code: 5675, name: "辰溪县", pinyin: "chenxixian", shouzimu: "c" },
  { code: 1974, name: "陈巴尔虎旗", pinyin: "chenbaerhuqi", shouzimu: "c" },
  { code: 8583, name: "称多县", pinyin: "chengduoxian", shouzimu: "c" },
  { code: 1288, name: "成安县", pinyin: "chenganxian", shouzimu: "c" },
  { code: 6510, name: "成都市", pinyin: "chengdoushi", shouzimu: "c" },
  { code: 4754, name: "成武县", pinyin: "chengwuxian", shouzimu: "c" },
  { code: 8313, name: "成县", pinyin: "chengxian", shouzimu: "c" },
  { code: 7310, name: "呈贡县", pinyin: "chenggongxian", shouzimu: "c" },
  { code: 1410, name: "承德市", pinyin: "chengdeshi", shouzimu: "c" },
  { code: 1411, name: "承德县", pinyin: "chengdexian", shouzimu: "c" },
  { code: 5559, name: "城步县", pinyin: "chengbuxian", shouzimu: "c" },
  { code: 7993, name: "城固县", pinyin: "chengguxian", shouzimu: "c" },
  { code: 6681, name: "城口县", pinyin: "chengkouxian", shouzimu: "c" },
  { code: 7978, name: "澄城县", pinyin: "chengchengxian", shouzimu: "c" },
  { code: 5860, name: "澄海市", pinyin: "chenghaishi", shouzimu: "c" },
  { code: 7413, name: "澄江县", pinyin: "chengjiangxian", shouzimu: "c" },
  { code: 6410, name: "澄迈县", pinyin: "chengmaixian", shouzimu: "c" },
  { code: 3790, name: "池州市", pinyin: "chizhoushi", shouzimu: "c" },
  { code: 4715, name: "茌平县", pinyin: "chipingxian", shouzimu: "c" },
  { code: 5362, name: "赤壁市", pinyin: "chibishi", shouzimu: "c" },
  { code: 1402, name: "赤城县", pinyin: "chichengxian", shouzimu: "c" },
  { code: 1940, name: "赤峰市", pinyin: "chifengshi", shouzimu: "c" },
  { code: 7043, name: "赤水市", pinyin: "chishuishi", shouzimu: "c" },
  { code: 1403, name: "崇礼县", pinyin: "chonglixian", shouzimu: "c" },
  { code: 4375, name: "崇仁县", pinyin: "chongrenxian", shouzimu: "c" },
  { code: 8334, name: "崇信县", pinyin: "chongxinxian", shouzimu: "c" },
  { code: 5365, name: "崇阳县", pinyin: "chongyangxian", shouzimu: "c" },
  { code: 4287, name: "崇义县", pinyin: "chongyixian", shouzimu: "c" },
  { code: 6510, name: "崇州市", pinyin: "chongzhoushi", shouzimu: "c" },
  { code: 6128, name: "崇左市", pinyin: "chongzuoshi", shouzimu: "c" },
  { code: 3750, name: "滁州市", pinyin: "chuzhoushi", shouzimu: "c" },
  { code: 7380, name: "楚雄市", pinyin: "chuxiongshi", shouzimu: "c" },
  { code: 3310, name: "淳安县", pinyin: "chunanxian", shouzimu: "c" },
  { code: 7961, name: "淳化县", pinyin: "chunhuaxian", shouzimu: "c" },
  { code: 5591, name: "慈利县", pinyin: "cilixian", shouzimu: "c" },
  { code: 3325, name: "慈溪市", pinyin: "cixishi", shouzimu: "c" },
  { code: 1291, name: "磁县", pinyin: "cixian", shouzimu: "c" },
  { code: 3683, name: "枞阳县", pinyin: "congyangxian", shouzimu: "c" },
  { code: 5810, name: "从化市", pinyin: "conghuashi", shouzimu: "c" },
  { code: 7144, name: "从江县", pinyin: "congjiangxian", shouzimu: "c" },
  { code: 7746, name: "措美县", pinyin: "cuomeixian", shouzimu: "c" },
  { code: 7816, name: "措勤县", pinyin: "cuoqinxian", shouzimu: "c" },
  { code: 7750, name: "错那县", pinyin: "cuonaxian", shouzimu: "c" }],

  D: [
  { code: 2045, name: "达尔罕茂明安联合旗", pinyin: "daerhanmaominganlianheqi", shouzimu: "d" },
  { code: 2052, name: "达拉特旗", pinyin: "dalateqi", shouzimu: "d" },
  { code: 8574, name: "达日县", pinyin: "darixian", shouzimu: "d" },
  { code: 6752, name: "达县", pinyin: "daxian", shouzimu: "d" },
  { code: 6750, name: "达州市", pinyin: "dazhoushi", shouzimu: "d" },
  { code: 7700, name: "达孜县", pinyin: "dazixian", shouzimu: "d" },
  { code: 2474, name: "大安市", pinyin: "daanshi", shouzimu: "d" },
  { code: 1468, name: "大厂县", pinyin: "dachangxian", shouzimu: "d" },
  { code: 1465, name: "大城县", pinyin: "dachengxian", shouzimu: "d" },
  { code: 7092, name: "大方县", pinyin: "dafangxian", shouzimu: "d" },
  { code: 3116, name: "大丰市", pinyin: "dafengshi", shouzimu: "d" },
  { code: 7345, name: "大关县", pinyin: "daguanxian", shouzimu: "d" },
  { code: 6292, name: "大化县", pinyin: "dahuaxian", shouzimu: "d" },
  { code: 7510, name: "大理市", pinyin: "dalishi", shouzimu: "d" },
  { code: 7976, name: "大荔县", pinyin: "dalixian", shouzimu: "d" },
  { code: 2220, name: "大连市", pinyin: "dalianshi", shouzimu: "d" },
  { code: 1281, name: "大名县", pinyin: "damingxian", shouzimu: "d" },
  { code: 1785, name: "大宁县", pinyin: "daningxian", shouzimu: "d" },
  { code: 5962, name: "大埔县", pinyin: "dapuxian", shouzimu: "d" },
  { code: 2650, name: "大庆市", pinyin: "daqingshi", shouzimu: "d" },
  { code: 2262, name: "大石桥市", pinyin: "dashiqiaoshi", shouzimu: "d" },
  { code: 3954, name: "大田县", pinyin: "datianxian", shouzimu: "d" },
  { code: 8510, name: "大通回族土族自治县", pinyin: "datonghuizutuzuzizhixian", shouzimu: "d" },
  { code: 1620, name: "大同市", pinyin: "datongshi", shouzimu: "d" },
  { code: 1639, name: "大同县", pinyin: "datongxian", shouzimu: "d" },
  { code: 2321, name: "大洼县", pinyin: "dawaxian", shouzimu: "d" },
  { code: 5355, name: "大悟县", pinyin: "dawuxian", shouzimu: "d" },
  { code: 6129, name: "大新县", pinyin: "daxinxian", shouzimu: "d" },
  { code: 2790, name: "大兴安岭地区", pinyin: "daxinganlingdiqu", shouzimu: "d" },
  { code: 5956, name: "大亚湾营业部", pinyin: "dayawanyingyebu", shouzimu: "d" },
  { code: 7386, name: "大姚县", pinyin: "dayaoxian", shouzimu: "d" },
  { code: 5221, name: "大冶市", pinyin: "dayeshi", shouzimu: "d" },
  { code: 6510, name: "大邑县", pinyin: "dayixian", shouzimu: "d" },
  { code: 6623, name: "大英县", pinyin: "dayingxian", shouzimu: "d" },
  { code: 4285, name: "大余县", pinyin: "dayuxian", shouzimu: "d" },
  { code: 6761, name: "大竹县", pinyin: "dazhuxian", shouzimu: "d" },
  { code: 6530, name: "大足县", pinyin: "dazuxian", shouzimu: "d" },
  { code: 1715, name: "代县", pinyin: "daixian", shouzimu: "d" },
  { code: 3421, name: "岱山县", pinyin: "daishanxian", shouzimu: "d" },
  { code: 6813, name: "丹巴县", pinyin: "danbaxian", shouzimu: "d" },
  { code: 2260, name: "丹东市", pinyin: "dandongshi", shouzimu: "d" },
  { code: 8033, name: "丹凤县", pinyin: "danfengxian", shouzimu: "d" },
  { code: 5391, name: "丹江口市", pinyin: "danjiangkoushi", shouzimu: "d" },
  { code: 6665, name: "丹棱县", pinyin: "danlengxian", shouzimu: "d" },
  { code: 3141, name: "丹阳市", pinyin: "danyangshi", shouzimu: "d" },
  { code: 7147, name: "丹寨县", pinyin: "danzhaixian", shouzimu: "d" },
  { code: 4755, name: "单县", pinyin: "danxian", shouzimu: "d" },
  { code: 5087, name: "郸城县", pinyin: "danchengxian", shouzimu: "d" },
  { code: 6410, name: "儋州市", pinyin: "danzhoushi", shouzimu: "d" },
  { code: 3651, name: "当涂县", pinyin: "dangtuxian", shouzimu: "d" },
  { code: 7700, name: "当雄县", pinyin: "dangxiongxian", shouzimu: "d" },
  { code: 5262, name: "当阳市", pinyin: "dangyangshi", shouzimu: "d" },
  { code: 3742, name: "砀山县", pinyin: "dangshanxian", shouzimu: "d" },
  { code: 8312, name: "宕昌县", pinyin: "dangchangxian", shouzimu: "d" },
  { code: 6816, name: "道孚县", pinyin: "daofuxian", shouzimu: "d" },
  { code: 5654, name: "道县", pinyin: "daoxian", shouzimu: "d" },
  { code: 7036, name: "道真县", pinyin: "daozhenxian", shouzimu: "d" },
  { code: 6828, name: "稻城县", pinyin: "daochengxian", shouzimu: "d" },
  { code: 6829, name: "得荣县", pinyin: "derongxian", shouzimu: "d" },
  { code: 4246, name: "德安县", pinyin: "deanxian", shouzimu: "d" },
  { code: 6275, name: "德保县", pinyin: "debaoxian", shouzimu: "d" },
  { code: 6859, name: "德昌县", pinyin: "dechangxian", shouzimu: "d" },
  { code: 6821, name: "德格县", pinyin: "degexian", shouzimu: "d" },
  { code: 3976, name: "德化县", pinyin: "dehuaxian", shouzimu: "d" },
  { code: 2410, name: "德惠市", pinyin: "dehuishi", shouzimu: "d" },
  { code: 7057, name: "德江县", pinyin: "dejiangxian", shouzimu: "d" },
  { code: 8592, name: "德令哈市", pinyin: "delinghashi", shouzimu: "d" },
  { code: 7572, name: "德钦县", pinyin: "deqinxian", shouzimu: "d" },
  { code: 3361, name: "德清县", pinyin: "deqingxian", shouzimu: "d" },
  { code: 5936, name: "德庆县", pinyin: "deqingxian", shouzimu: "d" },
  { code: 4342, name: "德兴市", pinyin: "dexingshi", shouzimu: "d" },
  { code: 6580, name: "德阳市", pinyin: "deyangshi", shouzimu: "d" },
  { code: 4680, name: "德州市", pinyin: "dezhoushi", shouzimu: "d" },
  { code: 2312, name: "灯塔市", pinyin: "dengtashi", shouzimu: "d" },
  { code: 4910, name: "登封市", pinyin: "dengfengshi", shouzimu: "d" },
  { code: 5132, name: "邓州市", pinyin: "dengzhoushi", shouzimu: "d" },
  { code: 2073, name: "磴口县", pinyin: "dengkouxian", shouzimu: "d" },
  { code: 5923, name: "电白县", pinyin: "dianbaixian", shouzimu: "d" },
  { code: 6692, name: "垫江县", pinyin: "dianjiangxian", shouzimu: "d" },
  { code: 8384, name: "迭部县", pinyin: "diebuxian", shouzimu: "d" },
  { code: 7724, name: "丁青县", pinyin: "dingqingxian", shouzimu: "d" },
  { code: 6410, name: "定安县", pinyin: "dinganxian", shouzimu: "d" },
  { code: 8066, name: "定边县", pinyin: "dingbianxian", shouzimu: "d" },
  { code: 7771, name: "定结县", pinyin: "dingjiexian", shouzimu: "d" },
  { code: 4291, name: "定南县", pinyin: "dingnanxian", shouzimu: "d" },
  { code: 7763, name: "定日县", pinyin: "dingrixian", shouzimu: "d" },
  { code: 4753, name: "定陶县", pinyin: "dingtaoxian", shouzimu: "d" },
  { code: 8290, name: "定西市", pinyin: "dingxishi", shouzimu: "d" },
  { code: 1712, name: "定襄县", pinyin: "dingxiangxian", shouzimu: "d" },
  { code: 1356, name: "定兴县", pinyin: "dingxingxian", shouzimu: "d" },
  { code: 3755, name: "定远县", pinyin: "dingyuanxian", shouzimu: "d" },
  { code: 1351, name: "定州市", pinyin: "dingzhoushi", shouzimu: "d" },
  { code: 4716, name: "东阿县", pinyin: "dongexian", shouzimu: "d" },
  { code: 5653, name: "东安县", pinyin: "donganxian", shouzimu: "d" },
  { code: 6410, name: "东方市", pinyin: "dongfangshi", shouzimu: "d" },
  { code: 2441, name: "东丰县", pinyin: "dongfengxian", shouzimu: "d" },
  { code: 2263, name: "东港市", pinyin: "donggangshi", shouzimu: "d" },
  { code: 6020, name: "东莞市", pinyin: "dongguanshi", shouzimu: "d" },
  { code: 1447, name: "东光县", pinyin: "dongguangxian", shouzimu: "d" },
  { code: 3072, name: "东海县", pinyin: "donghaixian", shouzimu: "d" },
  { code: 6288, name: "东兰县", pinyin: "donglanxian", shouzimu: "d" },
  { code: 2442, name: "东辽县", pinyin: "dongliaoxian", shouzimu: "d" },
  { code: 4761, name: "东明县", pinyin: "dongmingxian", shouzimu: "d" },
  { code: 2754, name: "东宁县", pinyin: "dongningxian", shouzimu: "d" },
  { code: 4633, name: "东平县", pinyin: "dongpingxian", shouzimu: "d" },
  { code: 3996, name: "东山县", pinyin: "dongshanxian", shouzimu: "d" },
  { code: 3117, name: "东台市", pinyin: "dongtaishi", shouzimu: "d" },
  { code: 2016, name: "东乌珠穆沁旗", pinyin: "dongwuzhumuqinqi", shouzimu: "d" },
  { code: 4381, name: "东乡县", pinyin: "dongxiangxian", shouzimu: "d" },
  { code: 8367, name: "东乡县", pinyin: "dongxiangxian", shouzimu: "d" },
  { code: 6331, name: "东兴市", pinyin: "dongxingshi", shouzimu: "d" },
  { code: 3388, name: "东阳市", pinyin: "dongyangshi", shouzimu: "d" },
  { code: 4550, name: "东营市", pinyin: "dongyingshi", shouzimu: "d" },
  { code: 5985, name: "东源县", pinyin: "dongyuanxian", shouzimu: "d" },
  { code: 3792, name: "东至县", pinyin: "dongzhixian", shouzimu: "d" },
  { code: 5555, name: "洞口县", pinyin: "dongkouxian", shouzimu: "d" },
  { code: 3332, name: "洞头县", pinyin: "dongtouxian", shouzimu: "d" },
  { code: 6291, name: "都安县", pinyin: "douanxian", shouzimu: "d" },
  { code: 4248, name: "都昌县", pinyin: "douchangxian", shouzimu: "d" },
  { code: 6510, name: "都江堰市", pinyin: "doujiangyanshi", shouzimu: "d" },
  { code: 8594, name: "都兰县", pinyin: "doulanxian", shouzimu: "d" },
  { code: 7150, name: "都匀市", pinyin: "douyunshi", shouzimu: "d" },
  { code: 7156, name: "独山县", pinyin: "dushanxian", shouzimu: "d" },
  { code: 2646, name: "杜蒙县", pinyin: "dumengxian", shouzimu: "d" },
  { code: 7700, name: "堆龙德庆县", pinyin: "duilongdeqingxian", shouzimu: "d" },
  { code: 2493, name: "敦化市", pinyin: "dunhuashi", shouzimu: "d" },
  { code: 8263, name: "敦煌市", pinyin: "dunhuangshi", shouzimu: "d" },
  { code: 2023, name: "多伦县", pinyin: "duolunxian", shouzimu: "d" }],

  E: [
  { code: 6662, name: "峨边县", pinyin: "ebianxian", shouzimu: "e" },
  { code: 6664, name: "峨眉山市", pinyin: "emeishanshi", shouzimu: "e" },
  { code: 7417, name: "峨山县", pinyin: "eshanxian", shouzimu: "e" },
  { code: 1967, name: "额尔古纳市", pinyin: "eergunashi", shouzimu: "e" },
  { code: 2083, name: "额济纳旗", pinyin: "ejinaqi", shouzimu: "e" },
  { code: 9012, name: "额敏县", pinyin: "eminxian", shouzimu: "e" },
  { code: 2050, name: "鄂尔多斯市", pinyin: "eerduosishi", shouzimu: "e" },
  { code: 1969, name: "鄂伦春族自治旗", pinyin: "elunchunzuzizhiqi", shouzimu: "e" },
  { code: 2055, name: "鄂托克旗", pinyin: "etuokeqi", shouzimu: "e" },
  { code: 2054, name: "鄂托克前旗", pinyin: "etuokeqianqi", shouzimu: "e" },
  { code: 1971, name: "鄂温克族自治旗", pinyin: "ewenkezuzizhiqi", shouzimu: "e" },
  { code: 5310, name: "鄂州市", pinyin: "ezhoushi", shouzimu: "e" },
  { code: 5893, name: "恩平市", pinyin: "enpingshi", shouzimu: "e" },
  { code: 5410, name: "恩施州", pinyin: "enshizhou", shouzimu: "e" },
  { code: 7521, name: "洱源县", pinyin: "eryuanxian", shouzimu: "e" },
  { code: 2011, name: "二连浩特市", pinyin: "erlianhaoteshi", shouzimu: "e" }],

  F: [
  { code: 2210, name: "法库县", pinyin: "fakuxian", shouzimu: "f" },
  { code: 3622, name: "繁昌县", pinyin: "fanchangxian", shouzimu: "f" },
  { code: 1716, name: "繁峙县", pinyin: "fanzhixian", shouzimu: "f" },
  { code: 5023, name: "范县", pinyin: "fanxian", shouzimu: "f" },
  { code: 5134, name: "方城县", pinyin: "fangchengxian", shouzimu: "f" },
  { code: 1741, name: "方山县", pinyin: "fangshanxian", shouzimu: "f" },
  { code: 2610, name: "方正县", pinyin: "fangzhengxian", shouzimu: "f" },
  { code: 6330, name: "防城港市", pinyin: "fangchenggangshi", shouzimu: "f" },
  { code: 5396, name: "房县", pinyin: "fangxian", shouzimu: "f" },
  { code: 4632, name: "肥城市", pinyin: "feichengshi", shouzimu: "f" },
  { code: 3610, name: "肥东县", pinyin: "feidongxian", shouzimu: "f" },
  { code: 3610, name: "肥西县", pinyin: "feixixian", shouzimu: "f" },
  { code: 1286, name: "肥乡县", pinyin: "feixiangxian", shouzimu: "f" },
  { code: 4742, name: "费县", pinyin: "feixian", shouzimu: "f" },
  { code: 4261, name: "分宜县", pinyin: "fenyixian", shouzimu: "f" },
  { code: 1788, name: "汾西县", pinyin: "fenxixian", shouzimu: "f" },
  { code: 1731, name: "汾阳市", pinyin: "fenyangshi", shouzimu: "f" },
  { code: 4312, name: "丰城市", pinyin: "fengchengshi", shouzimu: "f" },
  { code: 6694, name: "丰都县", pinyin: "fengdouxian", shouzimu: "f" },
  { code: 1425, name: "丰宁县", pinyin: "fengningxian", shouzimu: "f" },
  { code: 5963, name: "丰顺县", pinyin: "fengshunxian", shouzimu: "f" },
  { code: 3031, name: "丰县", pinyin: "fengxian", shouzimu: "f" },
  { code: 2039, name: "丰镇市", pinyin: "fengzhenshi", shouzimu: "f" },
  { code: 5935, name: "封开县", pinyin: "fengkaixian", shouzimu: "f" },
  { code: 4985, name: "封丘县", pinyin: "fengqiuxian", shouzimu: "f" },
  { code: 2261, name: "凤城市", pinyin: "fengchengshi", shouzimu: "f" },
  { code: 7038, name: "凤冈县", pinyin: "fenggangxian", shouzimu: "f" },
  { code: 5693, name: "凤凰县", pinyin: "fenghuangxian", shouzimu: "f" },
  { code: 7582, name: "凤庆县", pinyin: "fengqingxian", shouzimu: "f" },
  { code: 6287, name: "凤山县", pinyin: "fengshanxian", shouzimu: "f" },
  { code: 3641, name: "凤台县", pinyin: "fengtaixian", shouzimu: "f" },
  { code: 7939, name: "凤县", pinyin: "fengxian", shouzimu: "f" },
  { code: 7932, name: "凤翔县", pinyin: "fengxiangxian", shouzimu: "f" },
  { code: 3756, name: "凤阳县", pinyin: "fengyangxian", shouzimu: "f" },
  { code: 3326, name: "奉化市", pinyin: "fenghuashi", shouzimu: "f" },
  { code: 6677, name: "奉节县", pinyin: "fengjiexian", shouzimu: "f" },
  { code: 4315, name: "奉新县", pinyin: "fengxinxian", shouzimu: "f" },
  { code: 6011, name: "佛冈县", pinyin: "fogangxian", shouzimu: "f" },
  { code: 8002, name: "佛坪县", pinyin: "fopingxian", shouzimu: "f" },
  { code: 5880, name: "佛山市", pinyin: "foshanshi", shouzimu: "f" },
  { code: 7934, name: "扶风县", pinyin: "fufengxian", shouzimu: "f" },
  { code: 5082, name: "扶沟县", pinyin: "fugouxian", shouzimu: "f" },
  { code: 6127, name: "扶绥县", pinyin: "fusuixian", shouzimu: "f" },
  { code: 2473, name: "扶余县", pinyin: "fuyuxian", shouzimu: "f" },
  { code: 4222, name: "浮梁县", pinyin: "fuliangxian", shouzimu: "f" },
  { code: 1781, name: "浮山县", pinyin: "fushanxian", shouzimu: "f" },
  { code: 6690, name: "涪陵区", pinyin: "fulingqu", shouzimu: "f" },
  { code: 4034, name: "福安市", pinyin: "fuanshi", shouzimu: "f" },
  { code: 4032, name: "福鼎市", pinyin: "fudingshi", shouzimu: "f" },
  { code: 7562, name: "福贡县", pinyin: "fugongxian", shouzimu: "f" },
  { code: 9024, name: "福海县", pinyin: "fuhaixian", shouzimu: "f" },
  { code: 3910, name: "福清市", pinyin: "fuqingshi", shouzimu: "f" },
  { code: 7154, name: "福泉市", pinyin: "fuquanshi", shouzimu: "f" },
  { code: 3910, name: "福州市", pinyin: "fuzhoushi", shouzimu: "f" },
  { code: 1263, name: "抚宁县", pinyin: "funingxian", shouzimu: "f" },
  { code: 2240, name: "抚顺市", pinyin: "fushunshi", shouzimu: "f" },
  { code: 2241, name: "抚顺县", pinyin: "fushunxian", shouzimu: "f" },
  { code: 2461, name: "抚松县", pinyin: "fusongxian", shouzimu: "f" },
  { code: 2727, name: "抚远县", pinyin: "fuyuanxian", shouzimu: "f" },
  { code: 4370, name: "抚州市", pinyin: "fuzhoushi", shouzimu: "f" },
  { code: 8063, name: "府谷县", pinyin: "fuguxian", shouzimu: "f" },
  { code: 1492, name: "阜城县", pinyin: "fuchengxian", shouzimu: "f" },
  { code: 8856, name: "阜康市", pinyin: "fukangshi", shouzimu: "f" },
  { code: 3728, name: "阜南县", pinyin: "funanxian", shouzimu: "f" },
  { code: 3113, name: "阜宁县", pinyin: "funingxian", shouzimu: "f" },
  { code: 1368, name: "阜平县", pinyin: "fupingxian", shouzimu: "f" },
  { code: 2290, name: "阜新市", pinyin: "fuxinshi", shouzimu: "f" },
  { code: 2291, name: "阜新县", pinyin: "fuxinxian", shouzimu: "f" },
  { code: 3720, name: "阜阳市", pinyin: "fuyangshi", shouzimu: "f" },
  { code: 6227, name: "富川县", pinyin: "fuchuanxian", shouzimu: "f" },
  { code: 2731, name: "富锦市", pinyin: "fujinshi", shouzimu: "f" },
  { code: 7310, name: "富民县", pinyin: "fuminxian", shouzimu: "f" },
  { code: 7458, name: "富宁县", pinyin: "funingxian", shouzimu: "f" },
  { code: 7982, name: "富平县", pinyin: "fupingxian", shouzimu: "f" },
  { code: 6552, name: "富顺县", pinyin: "fushunxian", shouzimu: "f" },
  { code: 8049, name: "富县", pinyin: "fuxian", shouzimu: "f" },
  { code: 3310, name: "富阳市", pinyin: "fuyangshi", shouzimu: "f" },
  { code: 2647, name: "富裕县", pinyin: "fuyuxian", shouzimu: "f" },
  { code: 7364, name: "富源县", pinyin: "fuyuanxian", shouzimu: "f" },
  { code: 9023, name: "富蕴县", pinyin: "fuyunxian", shouzimu: "f" }],

  G: [
  { code: 7810, name: "噶尔县", pinyin: "gaerxian", shouzimu: "g" },
  { code: 7815, name: "改则县", pinyin: "gaizexian", shouzimu: "g" },
  { code: 2282, name: "盖州市", pinyin: "gaizhoushi", shouzimu: "g" },
  { code: 8573, name: "甘德县", pinyin: "gandexian", shouzimu: "g" },
  { code: 8253, name: "甘谷县", pinyin: "ganguxian", shouzimu: "g" },
  { code: 6856, name: "甘洛县", pinyin: "ganluoxian", shouzimu: "g" },
  { code: 2645, name: "甘南县", pinyin: "gannanxian", shouzimu: "g" },
  { code: 8380, name: "甘南州", pinyin: "gannanzhou", shouzimu: "g" },
  { code: 8048, name: "甘泉县", pinyin: "ganquanxian", shouzimu: "g" },
  { code: 6818, name: "甘孜县", pinyin: "ganzixian", shouzimu: "g" },
  { code: 4282, name: "赣县", pinyin: "ganxian", shouzimu: "g" },
  { code: 3071, name: "赣榆县", pinyin: "ganyuxian", shouzimu: "g" },
  { code: 4280, name: "赣州市", pinyin: "ganzhoushi", shouzimu: "g" },
  { code: 8544, name: "刚察县", pinyin: "gangchaxian", shouzimu: "g" },
  { code: 7777, name: "岗巴县", pinyin: "gangbaxian", shouzimu: "g" },
  { code: 8210, name: "皋兰县", pinyin: "gaolanxian", shouzimu: "g" },
  { code: 4314, name: "高安市", pinyin: "gaoanshi", shouzimu: "g" },
  { code: 1366, name: "高碑店市", pinyin: "gaobeidianshi", shouzimu: "g" },
  { code: 3010, name: "高淳县", pinyin: "gaochunxian", shouzimu: "g" },
  { code: 7915, name: "高陵县", pinyin: "gaolingxian", shouzimu: "g" },
  { code: 4586, name: "高密市", pinyin: "gaomishi", shouzimu: "g" },
  { code: 1683, name: "高平市", pinyin: "gaopingshi", shouzimu: "g" },
  { code: 4535, name: "高青县", pinyin: "gaoqingxian", shouzimu: "g" },
  { code: 8275, name: "高台县", pinyin: "gaotaixian", shouzimu: "g" },
  { code: 4718, name: "高唐县", pinyin: "gaotangxian", shouzimu: "g" },
  { code: 6716, name: "高县", pinyin: "gaoxian", shouzimu: "g" },
  { code: 1362, name: "高阳县", pinyin: "gaoyangxian", shouzimu: "g" },
  { code: 5931, name: "高要市", pinyin: "gaoyaoshi", shouzimu: "g" },
  { code: 1210, name: "高邑县", pinyin: "gaoyixian", shouzimu: "g" },
  { code: 3121, name: "高邮市", pinyin: "gaoyoushi", shouzimu: "g" },
  { code: 5922, name: "高州市", pinyin: "gaozhoushi", shouzimu: "g" },
  { code: 1210, name: "藁城市", pinyin: "gaochengshi", shouzimu: "g" },
  { code: 7814, name: "革吉县", pinyin: "gejixian", shouzimu: "g" },
  { code: 8591, name: "格尔木市", pinyin: "geermushi", shouzimu: "g" },
  { code: 7430, name: "个旧市", pinyin: "gejiushi", shouzimu: "g" },
  { code: 1968, name: "根河市", pinyin: "genheshi", shouzimu: "g" },
  { code: 7587, name: "耿马县", pinyin: "gengmaxian", shouzimu: "g" },
  { code: 7831, name: "工布江达县", pinyin: "gongbujiangdaxian", shouzimu: "g" },
  { code: 5378, name: "公安县", pinyin: "gonganxian", shouzimu: "g" },
  { code: 2434, name: "公主岭市", pinyin: "gongzhulingshi", shouzimu: "g" },
  { code: 6191, name: "恭城县", pinyin: "gongchengxian", shouzimu: "g" },
  { code: 8995, name: "巩留县", pinyin: "gongliuxian", shouzimu: "g" },
  { code: 4910, name: "巩义市", pinyin: "gongyishi", shouzimu: "g" },
  { code: 6718, name: "珙县", pinyin: "gongxian", shouzimu: "g" },
  { code: 8561, name: "共和县", pinyin: "gonghexian", shouzimu: "g" },
  { code: 7742, name: "贡嘎县", pinyin: "gonggaxian", shouzimu: "g" },
  { code: 7722, name: "贡觉县", pinyin: "gongjiaoxian", shouzimu: "g" },
  { code: 7563, name: "贡山县", pinyin: "gongshanxian", shouzimu: "g" },
  { code: 1393, name: "沽源县", pinyin: "guyuanxian", shouzimu: "g" },
  { code: 1610, name: "古交市", pinyin: "gujiaoshi", shouzimu: "g" },
  { code: 8283, name: "古浪县", pinyin: "gulangxian", shouzimu: "g" },
  { code: 6575, name: "古蔺县", pinyin: "gulinxian", shouzimu: "g" },
  { code: 4035, name: "古田县", pinyin: "gutianxian", shouzimu: "g" },
  { code: 1778, name: "古县", pinyin: "guxian", shouzimu: "g" },
  { code: 5696, name: "古丈县", pinyin: "guzhangxian", shouzimu: "g" },
  { code: 5284, name: "谷城县", pinyin: "guchengxian", shouzimu: "g" },
  { code: 5576, name: "汩罗市", pinyin: "guluoshi", shouzimu: "g" },
  { code: 1462, name: "固安县", pinyin: "guanxian", shouzimu: "g" },
  { code: 5157, name: "固始县", pinyin: "gushixian", shouzimu: "g" },
  { code: 1922, name: "固阳县", pinyin: "guyangxian", shouzimu: "g" },
  { code: 8741, name: "固原市", pinyin: "guyuanshi", shouzimu: "g" },
  { code: 3633, name: "固镇县", pinyin: "guzhenxian", shouzimu: "g" },
  { code: 1489, name: "故城县", pinyin: "guchengxian", shouzimu: "g" },
  { code: 7119, name: "关岭县", pinyin: "guanlingxian", shouzimu: "g" },
  { code: 1294, name: "馆陶县", pinyin: "guantaoxian", shouzimu: "g" },
  { code: 4717, name: "冠县", pinyin: "guanxian", shouzimu: "g" },
  { code: 3082, name: "灌南县", pinyin: "guannanxian", shouzimu: "g" },
  { code: 6185, name: "灌阳县", pinyin: "guanyangxian", shouzimu: "g" },
  { code: 3073, name: "灌云县", pinyin: "guanyunxian", shouzimu: "g" },
  { code: 5156, name: "光山县", pinyin: "guangshanxian", shouzimu: "g" },
  { code: 4018, name: "光泽县", pinyin: "guangzexian", shouzimu: "g" },
  { code: 6737, name: "广安市", pinyin: "guanganshi", shouzimu: "g" },
  { code: 4382, name: "广昌县", pinyin: "guangchangxian", shouzimu: "g" },
  { code: 3773, name: "广德县", pinyin: "guangdexian", shouzimu: "g" },
  { code: 4333, name: "广丰县", pinyin: "guangfengxian", shouzimu: "g" },
  { code: 6584, name: "广汉市", pinyin: "guanghanshi", shouzimu: "g" },
  { code: 8365, name: "广河县", pinyin: "guanghexian", shouzimu: "g" },
  { code: 1633, name: "广灵县", pinyin: "guanglingxian", shouzimu: "g" },
  { code: 7457, name: "广南县", pinyin: "guangnanxian", shouzimu: "g" },
  { code: 5933, name: "广宁县", pinyin: "guangningxian", shouzimu: "g" },
  { code: 1287, name: "广平县", pinyin: "guangpingxian", shouzimu: "g" },
  { code: 4553, name: "广饶县", pinyin: "guangraoxian", shouzimu: "g" },
  { code: 5354, name: "广水市", pinyin: "guangshuishi", shouzimu: "g" },
  { code: 6610, name: "广元市", pinyin: "guangyuanshi", shouzimu: "g" },
  { code: 5810, name: "广州市", pinyin: "guangzhoushi", shouzimu: "g" },
  { code: 1333, name: "广宗县", pinyin: "guangzongxian", shouzimu: "g" },
  { code: 8563, name: "贵德县", pinyin: "guidexian", shouzimu: "g" },
  { code: 7153, name: "贵定县", pinyin: "guidingxian", shouzimu: "g" },
  { code: 6242, name: "贵港市", pinyin: "guigangshi", shouzimu: "g" },
  { code: 8565, name: "贵南县", pinyin: "guinanxian", shouzimu: "g" },
  { code: 4271, name: "贵溪市", pinyin: "guixishi", shouzimu: "g" },
  { code: 7010, name: "贵阳市", pinyin: "guiyangshi", shouzimu: "g" },
  { code: 5641, name: "桂东县", pinyin: "guidongxian", shouzimu: "g" },
  { code: 6170, name: "桂林市", pinyin: "guilinshi", shouzimu: "g" },
  { code: 6243, name: "桂平市", pinyin: "guipingshi", shouzimu: "g" },
  { code: 5634, name: "桂阳县", pinyin: "guiyangxian", shouzimu: "g" },
  { code: 4611, name: "衮州市", pinyin: "gunzhoushi", shouzimu: "g" },
  { code: 8570, name: "果洛藏族自治州", pinyin: "guoluozangzuzizhizhou", shouzimu: "g" }],

  H: [
  { code: 9025, name: "哈巴河县", pinyin: "habahexian", shouzimu: "h" },
  { code: 2610, name: "哈尔滨市", pinyin: "haerbinshi", shouzimu: "h" },
  { code: 8840, name: "哈密市", pinyin: "hamishi", shouzimu: "h" },
  { code: 3061, name: "海安县", pinyin: "haianxian", shouzimu: "h" },
  { code: 8540, name: "海北藏族自治州", pinyin: "haibeizangzuzizhizhou", shouzimu: "h" },
  { code: 2232, name: "海城市", pinyin: "haichengshi", shouzimu: "h" },
  { code: 8520, name: "海东地区", pinyin: "haidongdiqu", shouzimu: "h" },
  { code: 5971, name: "海丰县", pinyin: "haifengxian", shouzimu: "h" },
  { code: 6410, name: "海口市", pinyin: "haikoushi", shouzimu: "h" },
  { code: 2752, name: "海林市", pinyin: "hailinshi", shouzimu: "h" },
  { code: 2764, name: "海伦市", pinyin: "hailunshi", shouzimu: "h" },
  { code: 3065, name: "海门市", pinyin: "haimenshi", shouzimu: "h" },
  { code: 8560, name: "海南藏族自治州", pinyin: "hainanzangzuzizhizhou", shouzimu: "h" },
  { code: 3355, name: "海宁市", pinyin: "hainingshi", shouzimu: "h" },
  { code: 8590, name: "海西蒙古族藏族自治州", pinyin: "haiximengguzuzangzuzizhizhou", shouzimu: "h" },
  { code: 1453, name: "海兴县", pinyin: "haixingxian", shouzimu: "h" },
  { code: 3353, name: "海盐县", pinyin: "haiyanxian", shouzimu: "h" },
  { code: 8543, name: "海晏县", pinyin: "haiyanxian", shouzimu: "h" },
  { code: 4564, name: "海阳市", pinyin: "haiyangshi", shouzimu: "h" },
  { code: 8742, name: "海原县", pinyin: "haiyuanxian", shouzimu: "h" },
  { code: 1270, name: "邯郸市", pinyin: "handanshi", shouzimu: "h" },
  { code: 1271, name: "邯郸县", pinyin: "handanxian", shouzimu: "h" },
  { code: 3784, name: "含山县", pinyin: "hanshanxian", shouzimu: "h" },
  { code: 7972, name: "韩城市", pinyin: "hanchengshi", shouzimu: "h" },
  { code: 5357, name: "汉川县", pinyin: "hanchuanxian", shouzimu: "h" },
  { code: 5582, name: "汉寿县", pinyin: "hanshouxian", shouzimu: "h" },
  { code: 8012, name: "汉阴县", pinyin: "hanyinxian", shouzimu: "h" },
  { code: 6774, name: "汉源县", pinyin: "hanyuanxian", shouzimu: "h" },
  { code: 7990, name: "汉中市", pinyin: "hanzhongshi", shouzimu: "h" },
  { code: 1210, name: "行唐县", pinyin: "xingtangxian", shouzimu: "x" },
  { code: 2077, name: "杭锦后旗", pinyin: "hangjinhouqi", shouzimu: "h" },
  { code: 2056, name: "杭锦旗", pinyin: "hangjinqi", shouzimu: "h" },
  { code: 3310, name: "杭州市", pinyin: "hangzhoushi", shouzimu: "h" },
  { code: 6530, name: "合川市", pinyin: "hechuanshi", shouzimu: "h" },
  { code: 3610, name: "合肥市", pinyin: "hefeishi", shouzimu: "h" },
  { code: 6572, name: "合江县", pinyin: "hejiangxian", shouzimu: "h" },
  { code: 6231, name: "合浦县", pinyin: "hepuxian", shouzimu: "h" },
  { code: 6151, name: "合山市", pinyin: "heshanshi", shouzimu: "h" },
  { code: 8345, name: "合水县", pinyin: "heshuixian", shouzimu: "h" },
  { code: 7981, name: "合阳县", pinyin: "heyangxian", shouzimu: "h" },
  { code: 9017, name: "和布克赛尔蒙古自治县", pinyin: "hebukesaiermengguzizhixian", shouzimu: "h" },
  { code: 8887, name: "和静县", pinyin: "hejingxian", shouzimu: "h" },
  { code: 1910, name: "和林格尔县", pinyin: "helingeerxian", shouzimu: "h" },
  { code: 2496, name: "和龙市", pinyin: "helongshi", shouzimu: "h" },
  { code: 5984, name: "和平县", pinyin: "hepingxian", shouzimu: "h" },
  { code: 1754, name: "和顺县", pinyin: "heshunxian", shouzimu: "h" },
  { code: 8888, name: "和硕县", pinyin: "heshuoxian", shouzimu: "h" },
  { code: 8960, name: "和田市", pinyin: "hetianshi", shouzimu: "h" },
  { code: 3785, name: "和县", pinyin: "hexian", shouzimu: "h" },
  { code: 8366, name: "和政县", pinyin: "hezhengxian", shouzimu: "h" },
  { code: 6281, name: "河池市", pinyin: "hechishi", shouzimu: "h" },
  { code: 1443, name: "河间市", pinyin: "hejianshi", shouzimu: "h" },
  { code: 1818, name: "河津市", pinyin: "hejinshi", shouzimu: "h" },
  { code: 7444, name: "河口县", pinyin: "hekouxian", shouzimu: "h" },
  { code: 8554, name: "河南蒙古族自治县", pinyin: "henanmengguzuzizhixian", shouzimu: "h" },
  { code: 1723, name: "河曲县", pinyin: "hequxian", shouzimu: "h" },
  { code: 5980, name: "河源市", pinyin: "heyuanshi", shouzimu: "h" },
  { code: 4750, name: "菏泽市", pinyin: "hezeshi", shouzimu: "h" },
  { code: 8710, name: "贺兰县", pinyin: "helanxian", shouzimu: "h" },
  { code: 6225, name: "贺州市", pinyin: "hezhoushi", shouzimu: "h" },
  { code: 7098, name: "赫章县", pinyin: "hezhangxian", shouzimu: "h" },
  { code: 4970, name: "鹤壁市", pinyin: "hebishi", shouzimu: "h" },
  { code: 5418, name: "鹤峰县", pinyin: "hefengxian", shouzimu: "h" },
  { code: 2670, name: "鹤岗市", pinyin: "hegangshi", shouzimu: "h" },
  { code: 7523, name: "鹤庆县", pinyin: "heqingxian", shouzimu: "h" },
  { code: 5895, name: "鹤山市", pinyin: "heshanshi", shouzimu: "h" },
  { code: 2780, name: "黑河市", pinyin: "heiheshi", shouzimu: "h" },
  { code: 2274, name: "黑山县", pinyin: "heishanxian", shouzimu: "h" },
  { code: 6798, name: "黑水县", pinyin: "heishuixian", shouzimu: "h" },
  { code: 4336, name: "横峰县", pinyin: "hengfengxian", shouzimu: "h" },
  { code: 8064, name: "横山县", pinyin: "hengshanxian", shouzimu: "h" },
  { code: 6110, name: "横县", pinyin: "hengxian", shouzimu: "h" },
  { code: 5544, name: "衡东县", pinyin: "hengdongxian", shouzimu: "h" },
  { code: 5542, name: "衡南县", pinyin: "hengnanxian", shouzimu: "h" },
  { code: 5543, name: "衡山县", pinyin: "hengshanxian", shouzimu: "h" },
  { code: 1480, name: "衡水市", pinyin: "hengshuishi", shouzimu: "h" },
  { code: 5540, name: "衡阳市", pinyin: "hengyangshi", shouzimu: "h" },
  { code: 5541, name: "衡阳县", pinyin: "hengyangxian", shouzimu: "h" },
  { code: 5334, name: "红安县", pinyin: "honganxian", shouzimu: "h" },
  { code: 8210, name: "红古区", pinyin: "hongguqu", shouzimu: "h" },
  { code: 7441, name: "红河县", pinyin: "honghexian", shouzimu: "h" },
  { code: 6804, name: "红原县", pinyin: "hongyuanxian", shouzimu: "h" },
  { code: 1776, name: "洪洞县", pinyin: "hongdongxian", shouzimu: "h" },
  { code: 5373, name: "洪湖市", pinyin: "honghushi", shouzimu: "h" },
  { code: 5672, name: "洪江市", pinyin: "hongjiangshi", shouzimu: "h" },
  { code: 6656, name: "洪雅县", pinyin: "hongyaxian", shouzimu: "h" },
  { code: 3087, name: "洪泽县", pinyin: "hongzexian", shouzimu: "h" },
  { code: 1772, name: "侯马市", pinyin: "houmashi", shouzimu: "h" },
  { code: 1910, name: "呼和浩特市", pinyin: "huhehaoteshi", shouzimu: "h" },
  { code: 2610, name: "呼兰县", pinyin: "hulanxian", shouzimu: "h" },
  { code: 1960, name: "呼伦贝尔市", pinyin: "hulunbeiershi", shouzimu: "h" },
  { code: 2791, name: "呼玛县", pinyin: "humaxian", shouzimu: "h" },
  { code: 8853, name: "呼图壁县", pinyin: "hutubixian", shouzimu: "h" },
  { code: 1667, name: "壶关县", pinyin: "huguanxian", shouzimu: "h" },
  { code: 2276, name: "葫芦岛市", pinyin: "huludaoshi", shouzimu: "h" },
  { code: 4249, name: "湖口县", pinyin: "hukouxian", shouzimu: "h" },
  { code: 3360, name: "湖州市", pinyin: "huzhoushi", shouzimu: "h" },
  { code: 2756, name: "虎林市", pinyin: "hulinshi", shouzimu: "h" },
  { code: 8526, name: "互助土族自治县", pinyin: "huzhutuzuzizhixian", shouzimu: "h" },
  { code: 7911, name: "户县", pinyin: "huxian", shouzimu: "h" },
  { code: 5694, name: "花垣县", pinyin: "huayuanxian", shouzimu: "h" },
  { code: 3999, name: "华安县", pinyin: "huaanxian", shouzimu: "h" },
  { code: 8344, name: "华池县", pinyin: "huachixian", shouzimu: "h" },
  { code: 7415, name: "华宁县", pinyin: "huaningxian", shouzimu: "h" },
  { code: 7553, name: "华坪县", pinyin: "huapingxian", shouzimu: "h" },
  { code: 5573, name: "华容县", pinyin: "huarongxian", shouzimu: "h" },
  { code: 8335, name: "华亭县", pinyin: "huatingxian", shouzimu: "h" },
  { code: 7973, name: "华县", pinyin: "huaxian", shouzimu: "h" },
  { code: 7974, name: "华阴市", pinyin: "huayinshi", shouzimu: "h" },
  { code: 6732, name: "华蓥市", pinyin: "huayingshi", shouzimu: "h" },
  { code: 4964, name: "滑县", pinyin: "huaxian", shouzimu: "h" },
  { code: 2036, name: "化德县", pinyin: "huadexian", shouzimu: "h" },
  { code: 8527, name: "化隆回族自治县", pinyin: "hualonghuizuzizhixian", shouzimu: "h" },
  { code: 5924, name: "化州市", pinyin: "huazhoushi", shouzimu: "h" },
  { code: 2723, name: "桦川县", pinyin: "huachuanxian", shouzimu: "h" },
  { code: 2425, name: "桦甸市", pinyin: "huadianshi", shouzimu: "h" },
  { code: 2721, name: "桦南县", pinyin: "huananxian", shouzimu: "h" },
  { code: 1397, name: "怀安县", pinyin: "huaianxian", shouzimu: "h" },
  { code: 5670, name: "怀化市", pinyin: "huaihuashi", shouzimu: "h" },
  { code: 5934, name: "怀集县", pinyin: "huaijixian", shouzimu: "h" },
  { code: 1399, name: "怀来县", pinyin: "huailaixian", shouzimu: "h" },
  { code: 3682, name: "怀宁县", pinyin: "huainingxian", shouzimu: "h" },
  { code: 1641, name: "怀仁县", pinyin: "huairenxian", shouzimu: "h" },
  { code: 3631, name: "怀远县", pinyin: "huaiyuanxian", shouzimu: "h" },
  { code: 3080, name: "淮安市", pinyin: "huaianshi", shouzimu: "h" },
  { code: 3660, name: "淮北市", pinyin: "huaibeishi", shouzimu: "h" },
  { code: 5153, name: "淮滨县", pinyin: "huaibinxian", shouzimu: "h" },
  { code: 3640, name: "淮南市", pinyin: "huainanshi", shouzimu: "h" },
  { code: 5088, name: "淮阳县", pinyin: "huaiyangxian", shouzimu: "h" },
  { code: 6284, name: "环江县", pinyin: "huanjiangxian", shouzimu: "h" },
  { code: 8343, name: "环县", pinyin: "huanxian", shouzimu: "h" },
  { code: 2252, name: "桓仁县", pinyin: "huanrenxian", shouzimu: "h" },
  { code: 4531, name: "桓台县", pinyin: "huantaixian", shouzimu: "h" },
  { code: 5330, name: "黄冈市", pinyin: "huanggangshi", shouzimu: "h" },
  { code: 1451, name: "黄骅市", pinyin: "huanghuashi", shouzimu: "h" },
  { code: 8054, name: "黄陵县", pinyin: "huanglingxian", shouzimu: "h" },
  { code: 8053, name: "黄龙县", pinyin: "huanglongxian", shouzimu: "h" },
  { code: 5339, name: "黄梅县", pinyin: "huangmeixian", shouzimu: "h" },
  { code: 8550, name: "黄南藏族自治州", pinyin: "huangnanzangzuzizhizhou", shouzimu: "h" },
  { code: 7132, name: "黄平县", pinyin: "huangpingxian", shouzimu: "h" },
  { code: 3710, name: "黄山市", pinyin: "huangshanshi", shouzimu: "h" },
  { code: 5220, name: "黄石市", pinyin: "huangshishi", shouzimu: "h" },
  { code: 8510, name: "湟源县", pinyin: "huangyuanxian", shouzimu: "h" },
  { code: 8510, name: "湟中县", pinyin: "huangzhongxian", shouzimu: "h" },
  { code: 5155, name: "潢川县", pinyin: "huangchuanxian", shouzimu: "h" },
  { code: 2494, name: "珲春市", pinyin: "hunchunshi", shouzimu: "h" },
  { code: 2452, name: "辉南县", pinyin: "huinanxian", shouzimu: "h" },
  { code: 4988, name: "辉县市", pinyin: "huixianshi", shouzimu: "h" },
  { code: 8319, name: "徽县", pinyin: "huixian", shouzimu: "h" },
  { code: 4297, name: "会昌县", pinyin: "huichangxian", shouzimu: "h" },
  { code: 6846, name: "会东县", pinyin: "huidongxian", shouzimu: "h" },
  { code: 6845, name: "会理县", pinyin: "huilixian", shouzimu: "h" },
  { code: 8242, name: "会宁县", pinyin: "huiningxian", shouzimu: "h" },
  { code: 5681, name: "会同县", pinyin: "huitongxian", shouzimu: "h" },
  { code: 7369, name: "会泽县", pinyin: "huizexian", shouzimu: "h" },
  { code: 3971, name: "惠安县", pinyin: "huianxian", shouzimu: "h" },
  { code: 5953, name: "惠东县", pinyin: "huidongxian", shouzimu: "h" },
  { code: 5868, name: "惠来县", pinyin: "huilaixian", shouzimu: "h" },
  { code: 4662, name: "惠民县", pinyin: "huiminxian", shouzimu: "h" },
  { code: 7162, name: "惠水县", pinyin: "huishuixian", shouzimu: "h" },
  { code: 5950, name: "惠州市", pinyin: "huizhoushi", shouzimu: "h" },
  { code: 1635, name: "浑源县", pinyin: "hunyuanxian", shouzimu: "h" },
  { code: 4982, name: "获嘉县", pinyin: "huojiaxian", shouzimu: "h" },
  { code: 8994, name: "霍城县", pinyin: "huochengxian", shouzimu: "h" },
  { code: 1992, name: "霍林郭勒市", pinyin: "huolinguoleshi", shouzimu: "h" },
  { code: 3764, name: "霍邱县", pinyin: "huoqiuxian", shouzimu: "h" },
  { code: 3767, name: "霍山县", pinyin: "huoshanxian", shouzimu: "h" },
  { code: 1777, name: "霍州市", pinyin: "huozhoushi", shouzimu: "h" }],

  J: [
  { code: 8951, name: "伽师县", pinyin: "jiashixian", shouzimu: "j" },
  { code: 2661, name: "鸡东县", pinyin: "jidongxian", shouzimu: "j" },
  { code: 2660, name: "鸡西市", pinyin: "jixishi", shouzimu: "j" },
  { code: 1285, name: "鸡泽县", pinyin: "jizexian", shouzimu: "j" },
  { code: 8368, name: "积石山县", pinyin: "jishishanxian", shouzimu: "j" },
  { code: 4350, name: "吉安市", pinyin: "jianshi", shouzimu: "j" },
  { code: 4353, name: "吉安县", pinyin: "jianxian", shouzimu: "j" },
  { code: 2420, name: "吉林市", pinyin: "jilinshi", shouzimu: "j" },
  { code: 7774, name: "吉隆县", pinyin: "jilongxian", shouzimu: "j" },
  { code: 9027, name: "吉木乃县", pinyin: "jimunaixian", shouzimu: "j" },
  { code: 8857, name: "吉木萨尔县", pinyin: "jimusaerxian", shouzimu: "j" },
  { code: 5690, name: "吉首市", pinyin: "jishoushi", shouzimu: "j" },
  { code: 4354, name: "吉水县", pinyin: "jishuixian", shouzimu: "j" },
  { code: 1782, name: "吉县", pinyin: "jixian", shouzimu: "j" },
  { code: 4521, name: "即墨市", pinyin: "jimoshi", shouzimu: "j" },
  { code: 2455, name: "集安市", pinyin: "jianshi", shouzimu: "j" },
  { code: 2681, name: "集贤县", pinyin: "jixianxian", shouzimu: "j" },
  { code: 4510, name: "济南市", pinyin: "jinanshi", shouzimu: "j" },
  { code: 4610, name: "济宁市", pinyin: "jiningshi", shouzimu: "j" },
  { code: 4510, name: "济阳县", pinyin: "jiyangxian", shouzimu: "j" },
  { code: 4910, name: "济源市", pinyin: "jiyuanshi", shouzimu: "j" },
  { code: 3777, name: "绩溪县", pinyin: "jixixian", shouzimu: "j" },
  { code: 1817, name: "稷山县", pinyin: "jishanxian", shouzimu: "j" },
  { code: 1482, name: "冀州市", pinyin: "jizhoushi", shouzimu: "j" },
  { code: 7748, name: "加查县", pinyin: "jiazhaxian", shouzimu: "j" },
  { code: 2690, name: "佳木斯市", pinyin: "jiamusishi", shouzimu: "j" },
  { code: 8069, name: "佳县", pinyin: "jiaxian", shouzimu: "j" },
  { code: 5637, name: "嘉禾县", pinyin: "jiahexian", shouzimu: "j" },
  { code: 7791, name: "嘉黎县", pinyin: "jialixian", shouzimu: "j" },
  { code: 3351, name: "嘉善县", pinyin: "jiashanxian", shouzimu: "j" },
  { code: 4616, name: "嘉祥县", pinyin: "jiaxiangxian", shouzimu: "j" },
  { code: 3350, name: "嘉兴市", pinyin: "jiaxingshi", shouzimu: "j" },
  { code: 2711, name: "嘉荫县", pinyin: "jiayinxian", shouzimu: "j" },
  { code: 5363, name: "嘉鱼县", pinyin: "jiayuxian", shouzimu: "j" },
  { code: 8220, name: "嘉峪关市", pinyin: "jiayuguanshi", shouzimu: "j" },
  { code: 6655, name: "夹江县", pinyin: "jiajiangxian", shouzimu: "j" },
  { code: 4954, name: "郏县", pinyin: "jiaxian", shouzimu: "j" },
  { code: 8552, name: "尖扎县", pinyin: "jianzhaxian", shouzimu: "j" },
  { code: 5379, name: "监利县", pinyin: "jianlixian", shouzimu: "j" },
  { code: 6653, name: "犍为县", pinyin: "jianweixian", shouzimu: "j" },
  { code: 6637, name: "简阳市", pinyin: "jianyangshi", shouzimu: "j" },
  { code: 2345, name: "建昌县", pinyin: "jianchangxian", shouzimu: "j" },
  { code: 3310, name: "建德市", pinyin: "jiandeshi", shouzimu: "j" },
  { code: 3115, name: "建湖县", pinyin: "jianhuxian", shouzimu: "j" },
  { code: 3959, name: "建宁县", pinyin: "jianningxian", shouzimu: "j" },
  { code: 4015, name: "建瓯市", pinyin: "jianoushi", shouzimu: "j" },
  { code: 2342, name: "建平县", pinyin: "jianpingxian", shouzimu: "j" },
  { code: 5413, name: "建始县", pinyin: "jianshixian", shouzimu: "j" },
  { code: 7435, name: "建水县", pinyin: "jianshuixian", shouzimu: "j" },
  { code: 4014, name: "建阳市", pinyin: "jianyangshi", shouzimu: "j" },
  { code: 7522, name: "剑川县", pinyin: "jianchuanxian", shouzimu: "j" },
  { code: 6613, name: "剑阁县", pinyin: "jiangexian", shouzimu: "j" },
  { code: 7139, name: "剑河县", pinyin: "jianhexian", shouzimu: "j" },
  { code: 6714, name: "江安县", pinyin: "jianganxian", shouzimu: "j" },
  { code: 7477, name: "江城县", pinyin: "jiangchengxian", shouzimu: "j" },
  { code: 7412, name: "江川县", pinyin: "jiangchuanxian", shouzimu: "j" },
  { code: 7721, name: "江达县", pinyin: "jiangdaxian", shouzimu: "j" },
  { code: 3125, name: "江都市", pinyin: "jiangdoushi", shouzimu: "j" },
  { code: 5657, name: "江华县", pinyin: "jianghuaxian", shouzimu: "j" },
  { code: 6530, name: "江津市", pinyin: "jiangjinshi", shouzimu: "j" },
  { code: 7052, name: "江口县", pinyin: "jiangkouxian", shouzimu: "j" },
  { code: 5890, name: "江门市", pinyin: "jiangmenshi", shouzimu: "j" },
  { code: 3415, name: "江山市", pinyin: "jiangshanshi", shouzimu: "j" },
  { code: 3022, name: "江阴市", pinyin: "jiangyinshi", shouzimu: "j" },
  { code: 5656, name: "江永县", pinyin: "jiangyongxian", shouzimu: "j" },
  { code: 6597, name: "江油市", pinyin: "jiangyoushi", shouzimu: "j" },
  { code: 2464, name: "江源县", pinyin: "jiangyuanxian", shouzimu: "j" },
  { code: 7762, name: "江孜县", pinyin: "jiangzixian", shouzimu: "j" },
  { code: 3957, name: "将乐县", pinyin: "jianglexian", shouzimu: "j" },
  { code: 3132, name: "姜堰市", pinyin: "jiangyanshi", shouzimu: "j" },
  { code: 1822, name: "绛县", pinyin: "jiangxian", shouzimu: "j" },
  { code: 1733, name: "交城县", pinyin: "jiaochengxian", shouzimu: "j" },
  { code: 1744, name: "交口县", pinyin: "jiaokouxian", shouzimu: "j" },
  { code: 4522, name: "胶南市", pinyin: "jiaonanshi", shouzimu: "j" },
  { code: 4525, name: "胶州市", pinyin: "jiaozhoushi", shouzimu: "j" },
  { code: 2424, name: "蛟河市", pinyin: "jiaoheshi", shouzimu: "j" },
  { code: 5010, name: "焦作市", pinyin: "jiaozuoshi", shouzimu: "j" },
  { code: 5967, name: "蕉岭县", pinyin: "jiaolingxian", shouzimu: "j" },
  { code: 5871, name: "揭东县", pinyin: "jiedongxian", shouzimu: "j" },
  { code: 5866, name: "揭西县", pinyin: "jiexixian", shouzimu: "j" },
  { code: 5865, name: "揭阳市", pinyin: "jieyangshi", shouzimu: "j" },
  { code: 1761, name: "介休市", pinyin: "jiexiushi", shouzimu: "j" },
  { code: 3731, name: "界首市", pinyin: "jieshoushi", shouzimu: "j" },
  { code: 8230, name: "金昌市", pinyin: "jinchangshi", shouzimu: "j" },
  { code: 6805, name: "金川县", pinyin: "jinchuanxian", shouzimu: "j" },
  { code: 3089, name: "金湖县", pinyin: "jinhuxian", shouzimu: "j" },
  { code: 3380, name: "金华市", pinyin: "jinhuashi", shouzimu: "j" },
  { code: 7442, name: "金平县", pinyin: "jinpingxian", shouzimu: "j" },
  { code: 7094, name: "金沙县", pinyin: "jinshaxian", shouzimu: "j" },
  { code: 8264, name: "金塔县", pinyin: "jintaxian", shouzimu: "j" },
  { code: 3042, name: "金坛市", pinyin: "jintanshi", shouzimu: "j" },
  { code: 6510, name: "金堂县", pinyin: "jintangxian", shouzimu: "j" },
  { code: 4378, name: "金溪县", pinyin: "jinxixian", shouzimu: "j" },
  { code: 4615, name: "金乡县", pinyin: "jinxiangxian", shouzimu: "j" },
  { code: 6159, name: "金秀县", pinyin: "jinxiuxian", shouzimu: "j" },
  { code: 6851, name: "金阳县", pinyin: "jinyangxian", shouzimu: "j" },
  { code: 3766, name: "金寨县", pinyin: "jinzhaixian", shouzimu: "j" },
  { code: 5587, name: "津市市", pinyin: "jinshishi", shouzimu: "j" },
  { code: 7138, name: "锦屏县", pinyin: "jinpingxian", shouzimu: "j" },
  { code: 2270, name: "锦州市", pinyin: "jinzhoushi", shouzimu: "j" },
  { code: 4210, name: "进贤县", pinyin: "jinxianxian", shouzimu: "j" },
  { code: 1680, name: "晋城市", pinyin: "jinchengshi", shouzimu: "j" },
  { code: 3972, name: "晋江市", pinyin: "jinjiangshi", shouzimu: "j" },
  { code: 7310, name: "晋宁县", pinyin: "jinningxian", shouzimu: "j" },
  { code: 1750, name: "晋中市", pinyin: "jinzhongshi", shouzimu: "j" },
  { code: 1210, name: "晋州市", pinyin: "jinzhoushi", shouzimu: "j" },
  { code: 3435, name: "缙云县", pinyin: "jinyunxian", shouzimu: "j" },
  { code: 5382, name: "京山县", pinyin: "jingshanxian", shouzimu: "j" },
  { code: 8332, name: "泾川县", pinyin: "jingchuanxian", shouzimu: "j" },
  { code: 3775, name: "泾县", pinyin: "jingxian", shouzimu: "j" },
  { code: 7953, name: "泾阳县", pinyin: "jingyangxian", shouzimu: "j" },
  { code: 8745, name: "泾源县", pinyin: "jingyuanxian", shouzimu: "j" },
  { code: 5320, name: "荆门市", pinyin: "jingmenshi", shouzimu: "j" },
  { code: 5370, name: "荆州市", pinyin: "jingzhoushi", shouzimu: "j" },
  { code: 3776, name: "旌德县", pinyin: "jingdexian", shouzimu: "j" },
  { code: 8872, name: "精河县", pinyin: "jinghexian", shouzimu: "j" },
  { code: 4352, name: "井冈山市", pinyin: "jinggangshanshi", shouzimu: "j" },
  { code: 1210, name: "井陉县", pinyin: "jingjingxian", shouzimu: "j" },
  { code: 6654, name: "井研县", pinyin: "jingyanxian", shouzimu: "j" },
  { code: 4220, name: "景德镇市", pinyin: "jingdezhenshi", shouzimu: "j" },
  { code: 7474, name: "景东县", pinyin: "jingdongxian", shouzimu: "j" },
  { code: 7475, name: "景谷县", pinyin: "jingguxian", shouzimu: "j" },
  { code: 7490, name: "景洪市", pinyin: "jinghongshi", shouzimu: "j" },
  { code: 3438, name: "景宁县", pinyin: "jingningxian", shouzimu: "j" },
  { code: 8243, name: "景泰县", pinyin: "jingtaixian", shouzimu: "j" },
  { code: 1491, name: "景县", pinyin: "jingxian", shouzimu: "j" },
  { code: 4319, name: "靖安县", pinyin: "jinganxian", shouzimu: "j" },
  { code: 8065, name: "靖边县", pinyin: "jingbianxian", shouzimu: "j" },
  { code: 3123, name: "靖江市", pinyin: "jingjiangshi", shouzimu: "j" },
  { code: 6266, name: "靖西县", pinyin: "jingxixian", shouzimu: "j" },
  { code: 2462, name: "靖宇县", pinyin: "jingyuxian", shouzimu: "j" },
  { code: 8241, name: "靖远县", pinyin: "jingyuanxian", shouzimu: "j" },
  { code: 5682, name: "靖州县", pinyin: "jingzhouxian", shouzimu: "j" },
  { code: 1718, name: "静乐县", pinyin: "jinglexian", shouzimu: "j" },
  { code: 8337, name: "静宁县", pinyin: "jingningxian", shouzimu: "j" },
  { code: 4240, name: "九江市", pinyin: "jiujiangshi", shouzimu: "j" },
  { code: 4241, name: "九江县", pinyin: "jiujiangxian", shouzimu: "j" },
  { code: 6814, name: "九龙县", pinyin: "jiulongxian", shouzimu: "j" },
  { code: 2410, name: "九台市", pinyin: "jiutaishi", shouzimu: "j" },
  { code: 6795, name: "九寨沟县", pinyin: "jiuzhaigouxian", shouzimu: "j" },
  { code: 8575, name: "久治县", pinyin: "jiuzhixian", shouzimu: "j" },
  { code: 8260, name: "酒泉市", pinyin: "jiuquanshi", shouzimu: "j" },
  { code: 4735, name: "莒南县", pinyin: "junanxian", shouzimu: "j" },
  { code: 4736, name: "莒县", pinyin: "juxian", shouzimu: "j" },
  { code: 1331, name: "巨鹿县", pinyin: "juluxian", shouzimu: "j" },
  { code: 4756, name: "巨野县", pinyin: "juyexian", shouzimu: "j" },
  { code: 3142, name: "句容市", pinyin: "jurongshi", shouzimu: "j" },
  { code: 4759, name: "鄄城县", pinyin: "juanchengxian", shouzimu: "j" },
  { code: 6717, name: "筠连县", pinyin: "junlianxian", shouzimu: "j" },
  { code: 4971, name: "浚县", pinyin: "junxian", shouzimu: "j" }],

  K: [
  { code: 1947, name: "喀喇沁旗", pinyin: "kalaqinqi", shouzimu: "k" },
  { code: 8940, name: "喀什市", pinyin: "kashenshi", shouzimu: "k" },
  { code: 2243, name: "喀左县", pinyin: "kazuoxian", shouzimu: "k" },
  { code: 4920, name: "开封市", pinyin: "kaifengshi", shouzimu: "k" },
  { code: 4924, name: "开封县", pinyin: "kaifengxian", shouzimu: "k" },
  { code: 3413, name: "开化县", pinyin: "kaihuaxian", shouzimu: "k" },
  { code: 6754, name: "开江县", pinyin: "kaijiangxian", shouzimu: "k" },
  { code: 1995, name: "开鲁县", pinyin: "kailuxian", shouzimu: "k" },
  { code: 5894, name: "开平市", pinyin: "kaipingshi", shouzimu: "k" },
  { code: 6673, name: "开县", pinyin: "kaixian", shouzimu: "k" },
  { code: 7010, name: "开阳县", pinyin: "kaiyangxian", shouzimu: "k" },
  { code: 2337, name: "开原市", pinyin: "kaiyuanshi", shouzimu: "k" },
  { code: 7432, name: "开远市", pinyin: "kaiyuanshi", shouzimu: "k" },
  { code: 7130, name: "凯里市", pinyin: "kailishi", shouzimu: "k" },
  { code: 1392, name: "康保县", pinyin: "kangbaoxian", shouzimu: "k" },
  { code: 6810, name: "康定县", pinyin: "kangdingxian", shouzimu: "k" },
  { code: 8363, name: "康乐县", pinyin: "kanglexian", shouzimu: "k" },
  { code: 7770, name: "康马县", pinyin: "kangmaxian", shouzimu: "k" },
  { code: 2210, name: "康平县", pinyin: "kangpingxian", shouzimu: "k" },
  { code: 8314, name: "康县", pinyin: "kangxian", shouzimu: "k" },
  { code: 8919, name: "柯坪县", pinyin: "kepingxian", shouzimu: "k" },
  { code: 1983, name: "科尔沁右翼中旗", pinyin: "keerqinyouyizhongqi", shouzimu: "k" },
  { code: 1994, name: "科尔沁左翼后旗", pinyin: "keerqinzuoyihouqi", shouzimu: "k" },
  { code: 1993, name: "科尔沁左翼中旗", pinyin: "keerqinzuoyizhongqi", shouzimu: "k" },
  { code: 1722, name: "岢岚县", pinyin: "kelanxian", shouzimu: "k" },
  { code: 2651, name: "克东县", pinyin: "kedongxian", shouzimu: "k" },
  { code: 8820, name: "克拉玛依市", pinyin: "kelamayishi", shouzimu: "k" },
  { code: 2649, name: "克山县", pinyin: "keshanxian", shouzimu: "k" },
  { code: 1945, name: "克什克腾", pinyin: "keshenketeng", shouzimu: "k" },
  { code: 4551, name: "垦利县", pinyin: "kenlixian", shouzimu: "k" },
  { code: 8913, name: "库车县", pinyin: "kuchexian", shouzimu: "k" },
  { code: 8880, name: "库尔勒市", pinyin: "kuerleshi", shouzimu: "k" },
  { code: 1996, name: "库伦旗", pinyin: "kulunqi", shouzimu: "k" },
  { code: 1421, name: "宽城县", pinyin: "kuanchengxian", shouzimu: "k" },
  { code: 2264, name: "宽甸县", pinyin: "kuandianxian", shouzimu: "k" },
  { code: 8981, name: "奎屯市", pinyin: "kuitunshi", shouzimu: "k" },
  { code: 7310, name: "昆明市", pinyin: "kunmingshi", shouzimu: "k" },
  { code: 3052, name: "昆山市", pinyin: "kunshanshi", shouzimu: "k" }],

  L: [
  { code: 7700, name: "拉萨市", pinyin: "lasashi", shouzimu: "l" },
  { code: 7765, name: "拉孜县", pinyin: "lazixian", shouzimu: "l" },
  { code: 3753, name: "来安县", pinyin: "laianxian", shouzimu: "l" },
  { code: 6155, name: "来宾市", pinyin: "laibinshi", shouzimu: "l" },
  { code: 5417, name: "来凤县", pinyin: "laifengxian", shouzimu: "l" },
  { code: 4634, name: "莱芜市", pinyin: "laiwushi", shouzimu: "l" },
  { code: 4523, name: "莱西市", pinyin: "laixishi", shouzimu: "l" },
  { code: 4568, name: "莱阳市", pinyin: "laiyangshi", shouzimu: "l" },
  { code: 4569, name: "莱州市", pinyin: "laizhoushi", shouzimu: "l" },
  { code: 1361, name: "涞水县", pinyin: "laishuixian", shouzimu: "l" },
  { code: 1355, name: "涞源县", pinyin: "laiyuanxian", shouzimu: "l" },
  { code: 4925, name: "兰考县", pinyin: "lankaoxian", shouzimu: "l" },
  { code: 7564, name: "兰坪县", pinyin: "lanpingxian", shouzimu: "l" },
  { code: 2766, name: "兰西县", pinyin: "lanxixian", shouzimu: "l" },
  { code: 3386, name: "兰溪市", pinyin: "lanxishi", shouzimu: "l" },
  { code: 8210, name: "兰州市", pinyin: "lanzhoushi", shouzimu: "l" },
  { code: 8016, name: "岚皋县", pinyin: "langaoxian", shouzimu: "l" },
  { code: 1739, name: "岚县", pinyin: "lanxian", shouzimu: "l" },
  { code: 5658, name: "蓝山县", pinyin: "lanshanxian", shouzimu: "l" },
  { code: 7912, name: "蓝田县", pinyin: "lantianxian", shouzimu: "l" },
  { code: 7479, name: "澜沧县", pinyin: "lancangxian", shouzimu: "l" },
  { code: 3772, name: "郎溪县", pinyin: "langxixian", shouzimu: "l" },
  { code: 6743, name: "阆中市", pinyin: "langzhongshi", shouzimu: "l" },
  { code: 1460, name: "廊坊市", pinyin: "langfangshi", shouzimu: "l" },
  { code: 7836, name: "朗县", pinyin: "langxian", shouzimu: "l" },
  { code: 7751, name: "浪卡子县", pinyin: "langkazixian", shouzimu: "l" },
  { code: 5287, name: "老河口市", pinyin: "laohekoushi", shouzimu: "l" },
  { code: 4376, name: "乐安县", pinyin: "leanxian", shouzimu: "l" },
  { code: 5825, name: "乐昌市", pinyin: "lechangshi", shouzimu: "l" },
  { code: 6410, name: "乐东县", pinyin: "ledongxian", shouzimu: "l" },
  { code: 8523, name: "乐都县", pinyin: "ledouxian", shouzimu: "l" },
  { code: 4682, name: "乐陵市", pinyin: "lelingshi", shouzimu: "l" },
  { code: 4221, name: "乐平市", pinyin: "lepingshi", shouzimu: "l" },
  { code: 3333, name: "乐清市", pinyin: "leqingshi", shouzimu: "l" },
  { code: 6650, name: "乐山市", pinyin: "leshanshi", shouzimu: "l" },
  { code: 1245, name: "乐亭县", pinyin: "letingxian", shouzimu: "l" },
  { code: 6269, name: "乐业县", pinyin: "leyexian", shouzimu: "l" },
  { code: 6632, name: "乐至县", pinyin: "lezhixian", shouzimu: "l" },
  { code: 6858, name: "雷波县", pinyin: "leiboxian", shouzimu: "l" },
  { code: 7145, name: "雷山县", pinyin: "leishanxian", shouzimu: "l" },
  { code: 5914, name: "雷州市", pinyin: "leizhoushi", shouzimu: "l" },
  { code: 5547, name: "耒阳市", pinyin: "leiyangshi", shouzimu: "l" },
  { code: 7723, name: "类乌齐县", pinyin: "leiwuqixian", shouzimu: "l" },
  { code: 5622, name: "冷水江市", pinyin: "lengshuijiangshi", shouzimu: "l" },
  { code: 2431, name: "梨树县", pinyin: "lishuxian", shouzimu: "l" },
  { code: 1666, name: "黎城县", pinyin: "lichengxian", shouzimu: "l" },
  { code: 4373, name: "黎川县", pinyin: "lichuanxian", shouzimu: "l" },
  { code: 7142, name: "黎平县", pinyin: "lipingxian", shouzimu: "l" },
  { code: 1372, name: "蠡县", pinyin: "lixian", shouzimu: "l" },
  { code: 7955, name: "礼泉县", pinyin: "liquanxian", shouzimu: "l" },
  { code: 8317, name: "礼县", pinyin: "lixian", shouzimu: "l" },
  { code: 6825, name: "理塘县", pinyin: "litangxian", shouzimu: "l" },
  { code: 6792, name: "理县", pinyin: "lixian", shouzimu: "l" },
  { code: 5583, name: "澧县", pinyin: "lixian", shouzimu: "l" },
  { code: 5525, name: "醴陵市", pinyin: "lilingshi", shouzimu: "l" },
  { code: 7550, name: "丽江市", pinyin: "lijiangshi", shouzimu: "l" },
  { code: 3430, name: "丽水市", pinyin: "lishuishi", shouzimu: "l" },
  { code: 5412, name: "利川市", pinyin: "lichuanshi", shouzimu: "l" },
  { code: 4552, name: "利津县", pinyin: "lijinxian", shouzimu: "l" },
  { code: 3732, name: "利辛县", pinyin: "lixinxian", shouzimu: "l" },
  { code: 7152, name: "荔波县", pinyin: "liboxian", shouzimu: "l" },
  { code: 6189, name: "荔浦县", pinyin: "lipuxian", shouzimu: "l" },
  { code: 3010, name: "溧水县", pinyin: "lishuixian", shouzimu: "l" },
  { code: 3043, name: "溧阳市", pinyin: "liyangshi", shouzimu: "l" },
  { code: 4057, name: "连城县", pinyin: "lianchengxian", shouzimu: "l" },
  { code: 3910, name: "连江县", pinyin: "lianjiangxian", shouzimu: "l" },
  { code: 6016, name: "连南县", pinyin: "liannanxian", shouzimu: "l" },
  { code: 5983, name: "连平县", pinyin: "lianpingxian", shouzimu: "l" },
  { code: 6015, name: "连山县", pinyin: "lianshanxian", shouzimu: "l" },
  { code: 3070, name: "连云港市", pinyin: "lianyungangshi", shouzimu: "l" },
  { code: 6014, name: "连州市", pinyin: "lianzhoushi", shouzimu: "l" },
  { code: 4364, name: "莲花县", pinyin: "lianhuaxian", shouzimu: "l" },
  { code: 3085, name: "涟水县", pinyin: "lianshuixian", shouzimu: "l" },
  { code: 5623, name: "涟源市", pinyin: "lianyuanshi", shouzimu: "l" },
  { code: 5912, name: "廉江市", pinyin: "lianjiangshi", shouzimu: "l" },
  { code: 2041, name: "凉城县", pinyin: "liangchengxian", shouzimu: "l" },
  { code: 7543, name: "梁河县", pinyin: "lianghexian", shouzimu: "l" },
  { code: 6675, name: "梁平县", pinyin: "liangpingxian", shouzimu: "l" },
  { code: 4757, name: "梁山县", pinyin: "liangshanxian", shouzimu: "l" },
  { code: 8318, name: "两当县", pinyin: "liangdangxian", shouzimu: "l" },
  { code: 2310, name: "辽阳市", pinyin: "liaoyangshi", shouzimu: "l" },
  { code: 2311, name: "辽阳县", pinyin: "liaoyangxian", shouzimu: "l" },
  { code: 2440, name: "辽源市", pinyin: "liaoyuanshi", shouzimu: "l" },
  { code: 2210, name: "辽中县", pinyin: "liaozhongxian", shouzimu: "l" },
  { code: 4710, name: "聊城市", pinyin: "liaochengshi", shouzimu: "l" },
  { code: 6763, name: "邻水县", pinyin: "linshuixian", shouzimu: "l" },
  { code: 2648, name: "林甸县", pinyin: "lindianxian", shouzimu: "l" },
  { code: 2755, name: "林口县", pinyin: "linkouxian", shouzimu: "l" },
  { code: 1944, name: "林西县", pinyin: "linxixian", shouzimu: "l" },
  { code: 7830, name: "林芝县", pinyin: "linzhixian", shouzimu: "l" },
  { code: 4961, name: "林州市", pinyin: "linzhoushi", shouzimu: "l" },
  { code: 7700, name: "林周县", pinyin: "linzhouxian", shouzimu: "l" },
  { code: 3310, name: "临安市", pinyin: "linanshi", shouzimu: "l" },
  { code: 7580, name: "临沧市", pinyin: "lincangshi", shouzimu: "l" },
  { code: 1323, name: "临城县", pinyin: "linchengxian", shouzimu: "l" },
  { code: 1770, name: "临汾市", pinyin: "linfenshi", shouzimu: "l" },
  { code: 6410, name: "临高县", pinyin: "lingaoxian", shouzimu: "l" },
  { code: 6172, name: "临桂县", pinyin: "linguixian", shouzimu: "l" },
  { code: 3452, name: "临海市", pinyin: "linhaishi", shouzimu: "l" },
  { code: 2422, name: "临江市", pinyin: "linjiangshi", shouzimu: "l" },
  { code: 5584, name: "临澧县", pinyin: "linlixian", shouzimu: "l" },
  { code: 4712, name: "临清市", pinyin: "linqingshi", shouzimu: "l" },
  { code: 4583, name: "临朐县", pinyin: "linquxian", shouzimu: "l" },
  { code: 3724, name: "临泉县", pinyin: "linquanxian", shouzimu: "l" },
  { code: 4744, name: "临沭县", pinyin: "linshuxian", shouzimu: "l" },
  { code: 8381, name: "临潭县", pinyin: "lintanxian", shouzimu: "l" },
  { code: 8295, name: "临洮县", pinyin: "lintaoxian", shouzimu: "l" },
  { code: 5638, name: "临武县", pinyin: "linwuxian", shouzimu: "l" },
  { code: 1337, name: "临西县", pinyin: "linxixian", shouzimu: "l" },
  { code: 8360, name: "临夏州", pinyin: "linxiazhou", shouzimu: "l" },
  { code: 1736, name: "临县", pinyin: "linxian", shouzimu: "l" },
  { code: 5572, name: "临湘市", pinyin: "linxiangshi", shouzimu: "l" },
  { code: 1814, name: "临猗县", pinyin: "linyixian", shouzimu: "l" },
  { code: 4730, name: "临沂市", pinyin: "linyishi", shouzimu: "l" },
  { code: 4689, name: "临邑县", pinyin: "linyixian", shouzimu: "l" },
  { code: 5042, name: "临颍县", pinyin: "linyingxian", shouzimu: "l" },
  { code: 8274, name: "临泽县", pinyin: "linzexian", shouzimu: "l" },
  { code: 1289, name: "临漳县", pinyin: "linzhangxian", shouzimu: "l" },
  { code: 7938, name: "麟游县", pinyin: "linyouxian", shouzimu: "l" },
  { code: 5053, name: "灵宝市", pinyin: "lingbaoshi", shouzimu: "l" },
  { code: 3745, name: "灵璧县", pinyin: "lingbixian", shouzimu: "l" },
  { code: 6181, name: "灵川县", pinyin: "lingchuanxian", shouzimu: "l" },
  { code: 1634, name: "灵邱县", pinyin: "lingqiuxian", shouzimu: "l" },
  { code: 6314, name: "灵山县", pinyin: "lingshanxian", shouzimu: "l" },
  { code: 1762, name: "灵石县", pinyin: "lingshixian", shouzimu: "l" },
  { code: 1210, name: "灵寿县", pinyin: "lingshouxian", shouzimu: "l" },
  { code: 8333, name: "灵台县", pinyin: "lingtaixian", shouzimu: "l" },
  { code: 8710, name: "灵武市", pinyin: "lingwushi", shouzimu: "l" },
  { code: 2272, name: "凌海市", pinyin: "linghaishi", shouzimu: "l" },
  { code: 2343, name: "凌源市", pinyin: "lingyuanshi", shouzimu: "l" },
  { code: 6268, name: "凌云县", pinyin: "lingyunxian", shouzimu: "l" },
  { code: 1684, name: "陵川县", pinyin: "lingchuanxian", shouzimu: "l" },
  { code: 6410, name: "陵水县", pinyin: "lingshuixian", shouzimu: "l" },
  { code: 4683, name: "陵县", pinyin: "lingxian", shouzimu: "l" },
  { code: 5510, name: "浏阳市", pinyin: "liuyangshi", shouzimu: "l" },
  { code: 8001, name: "留坝县", pinyin: "liubaxian", shouzimu: "l" },
  { code: 6142, name: "柳城县", pinyin: "liuchengxian", shouzimu: "l" },
  { code: 2453, name: "柳河县", pinyin: "liuhexian", shouzimu: "l" },
  { code: 6141, name: "柳江县", pinyin: "liujiangxian", shouzimu: "l" },
  { code: 1737, name: "柳林县", pinyin: "liulinxian", shouzimu: "l" },
  { code: 6140, name: "柳州市", pinyin: "liuzhoushi", shouzimu: "l" },
  { code: 3760, name: "六安市", pinyin: "liuanshi", shouzimu: "l" },
  { code: 7020, name: "六盘水市", pinyin: "liupanshuishi", shouzimu: "l" },
  { code: 7021, name: "六枝特区", pinyin: "liuzhitequ", shouzimu: "l" },
  { code: 5982, name: "龙川县", pinyin: "longchuanxian", shouzimu: "l" },
  { code: 3991, name: "龙海市", pinyin: "longhaishi", shouzimu: "l" },
  { code: 2641, name: "龙江县", pinyin: "longjiangxian", shouzimu: "l" },
  { code: 2495, name: "龙井市", pinyin: "longjingshi", shouzimu: "l" },
  { code: 4567, name: "龙口市", pinyin: "longkoushi", shouzimu: "l" },
  { code: 7161, name: "龙里县", pinyin: "longlixian", shouzimu: "l" },
  { code: 7534, name: "龙陵县", pinyin: "longlingxian", shouzimu: "l" },
  { code: 5954, name: "龙门县", pinyin: "longmenxian", shouzimu: "l" },
  { code: 4289, name: "龙南县", pinyin: "longnanxian", shouzimu: "l" },
  { code: 3439, name: "龙泉市", pinyin: "longquanshi", shouzimu: "l" },
  { code: 5698, name: "龙山县", pinyin: "longshanxian", shouzimu: "l" },
  { code: 6186, name: "龙胜县", pinyin: "longshengxian", shouzimu: "l" },
  { code: 4050, name: "龙岩市", pinyin: "longyanshi", shouzimu: "l" },
  { code: 3414, name: "龙游县", pinyin: "longyouxian", shouzimu: "l" },
  { code: 6133, name: "龙州县", pinyin: "longzhouxian", shouzimu: "l" },
  { code: 6110, name: "隆安县", pinyin: "longanxian", shouzimu: "l" },
  { code: 6638, name: "隆昌县", pinyin: "longchangxian", shouzimu: "l" },
  { code: 8744, name: "隆德县", pinyin: "longdexian", shouzimu: "l" },
  { code: 1426, name: "隆化县", pinyin: "longhuaxian", shouzimu: "l" },
  { code: 5554, name: "隆回县", pinyin: "longhuixian", shouzimu: "l" },
  { code: 6272, name: "隆林县", pinyin: "longlinxian", shouzimu: "l" },
  { code: 1326, name: "隆尧县", pinyin: "longyaoxian", shouzimu: "l" },
  { code: 7749, name: "隆子县", pinyin: "longzixian", shouzimu: "l" },
  { code: 7545, name: "陇川县", pinyin: "longchuanxian", shouzimu: "l" },
  { code: 8310, name: "陇南市", pinyin: "longnanshi", shouzimu: "l" },
  { code: 8293, name: "陇西县", pinyin: "longxixian", shouzimu: "l" },
  { code: 7936, name: "陇县", pinyin: "longxian", shouzimu: "l" },
  { code: 5620, name: "娄底市", pinyin: "loudishi", shouzimu: "l" },
  { code: 1610, name: "娄烦县", pinyin: "loufanxian", shouzimu: "l" },
  { code: 1264, name: "卢龙县", pinyin: "lulongxian", shouzimu: "l" },
  { code: 5054, name: "卢氏县", pinyin: "lushixian", shouzimu: "l" },
  { code: 6777, name: "芦山县", pinyin: "lushanxian", shouzimu: "l" },
  { code: 4232, name: "芦溪县", pinyin: "luxixian", shouzimu: "l" },
  { code: 3782, name: "庐江县", pinyin: "lujiangxian", shouzimu: "l" },
  { code: 4240, name: "庐山", pinyin: "lushan", shouzimu: "l" },
  { code: 6817, name: "炉霍县", pinyin: "luhuoxian", shouzimu: "l" },
  { code: 6812, name: "泸定县", pinyin: "ludingxian", shouzimu: "l" },
  { code: 7560, name: "泸水县", pinyin: "lushuixian", shouzimu: "l" },
  { code: 7438, name: "泸西县", pinyin: "luxixian", shouzimu: "l" },
  { code: 5692, name: "泸溪县", pinyin: "luxixian", shouzimu: "l" },
  { code: 6571, name: "泸县", pinyin: "luxian", shouzimu: "l" },
  { code: 6570, name: "泸州市", pinyin: "luzhoushi", shouzimu: "l" },
  { code: 7342, name: "鲁甸县", pinyin: "ludianxian", shouzimu: "l" },
  { code: 4953, name: "鲁山县", pinyin: "lushanxian", shouzimu: "l" },
  { code: 6247, name: "陆川县", pinyin: "luchuanxian", shouzimu: "l" },
  { code: 5972, name: "陆丰市", pinyin: "lufengshi", shouzimu: "l" },
  { code: 5973, name: "陆河县", pinyin: "luhexian", shouzimu: "l" },
  { code: 7367, name: "陆良县", pinyin: "luliangxian", shouzimu: "l" },
  { code: 1210, name: "鹿泉市", pinyin: "luquanshi", shouzimu: "l" },
  { code: 5086, name: "鹿邑县", pinyin: "luyixian", shouzimu: "l" },
  { code: 6152, name: "鹿寨县", pinyin: "luzhaixian", shouzimu: "l" },
  { code: 7391, name: "禄丰县", pinyin: "lufengxian", shouzimu: "l" },
  { code: 7310, name: "禄劝县", pinyin: "luquanxian", shouzimu: "l" },
  { code: 8386, name: "碌曲县", pinyin: "luquxian", shouzimu: "l" },
  { code: 1662, name: "潞城市", pinyin: "luchengshi", shouzimu: "l" },
  { code: 7540, name: "潞西市", pinyin: "luxishi", shouzimu: "l" },
  { code: 1210, name: "栾城县", pinyin: "luanchengxian", shouzimu: "l" },
  { code: 4934, name: "栾川县", pinyin: "luanchuanxian", shouzimu: "l" },
  { code: 1244, name: "滦南县", pinyin: "luannanxian", shouzimu: "l" },
  { code: 1424, name: "滦平县", pinyin: "luanpingxian", shouzimu: "l" },
  { code: 1243, name: "滦县", pinyin: "luanxian", shouzimu: "l" },
  { code: 8882, name: "轮台县", pinyin: "luntaixian", shouzimu: "l" },
  { code: 6283, name: "罗城县", pinyin: "luochengxian", shouzimu: "l" },
  { code: 7158, name: "罗甸县", pinyin: "luodianxian", shouzimu: "l" },
  { code: 5941, name: "罗定市", pinyin: "luodingshi", shouzimu: "l" },
  { code: 6585, name: "罗江县", pinyin: "luojiangxian", shouzimu: "l" },
  { code: 7365, name: "罗平县", pinyin: "luopingxian", shouzimu: "l" },
  { code: 5159, name: "罗山县", pinyin: "luoshanxian", shouzimu: "l" },
  { code: 5335, name: "罗田县", pinyin: "luotianxian", shouzimu: "l" },
  { code: 3910, name: "罗源县", pinyin: "luoyuanxian", shouzimu: "l" },
  { code: 2671, name: "萝北县", pinyin: "luobeixian", shouzimu: "l" },
  { code: 8051, name: "洛川县", pinyin: "luochuanxian", shouzimu: "l" },
  { code: 7729, name: "洛隆县", pinyin: "luolongxian", shouzimu: "l" },
  { code: 8032, name: "洛南县", pinyin: "luonanxian", shouzimu: "l" },
  { code: 4938, name: "洛宁县", pinyin: "luoningxian", shouzimu: "l" },
  { code: 8965, name: "洛浦县", pinyin: "luopuxian", shouzimu: "l" },
  { code: 4930, name: "洛阳市", pinyin: "luoyangshi", shouzimu: "l" },
  { code: 7747, name: "洛扎县", pinyin: "luozhaxian", shouzimu: "l" },
  { code: 5040, name: "漯河市", pinyin: "luoheshi", shouzimu: "l" },
  { code: 1730, name: "吕梁市", pinyin: "lvliangshi", shouzimu: "l" },
  { code: 7443, name: "绿春县", pinyin: "lvchunxian", shouzimu: "l" },
  { code: 7998, name: "略阳县", pinyin: "lveyangxian", shouzimu: "l" }],

  M: [
  { code: 5331, name: "麻城市", pinyin: "machengshi", shouzimu: "m" },
  { code: 7146, name: "麻江县", pinyin: "majiangxian", shouzimu: "m" },
  { code: 7454, name: "麻栗坡县", pinyin: "malipoxian", shouzimu: "m" },
  { code: 5677, name: "麻阳县", pinyin: "mayangxian", shouzimu: "m" },
  { code: 3650, name: "马鞍山市", pinyin: "maanshanshi", shouzimu: "m" },
  { code: 6663, name: "马边县", pinyin: "mabianxian", shouzimu: "m" },
  { code: 6790, name: "马尔康县", pinyin: "maerkangxian", shouzimu: "m" },
  { code: 7455, name: "马关县", pinyin: "maguanxian", shouzimu: "m" },
  { code: 7362, name: "马龙县", pinyin: "malongxian", shouzimu: "m" },
  { code: 6110, name: "马山县", pinyin: "mashanxian", shouzimu: "m" },
  { code: 8576, name: "玛多县", pinyin: "maduoxian", shouzimu: "m" },
  { code: 8854, name: "玛纳斯县", pinyin: "manasixian", shouzimu: "m" },
  { code: 8571, name: "玛沁县", pinyin: "maqinxian", shouzimu: "m" },
  { code: 8385, name: "玛曲县", pinyin: "maquxian", shouzimu: "m" },
  { code: 8948, name: "麦盖提县", pinyin: "maigaitixian", shouzimu: "m" },
  { code: 1341, name: "满城县", pinyin: "manchengxian", shouzimu: "m" },
  { code: 1962, name: "满州里市", pinyin: "manzhoulishi", shouzimu: "m" },
  { code: 7728, name: "芒康县", pinyin: "mangkangxian", shouzimu: "m" },
  { code: 5920, name: "茂名市", pinyin: "maomingshi", shouzimu: "m" },
  { code: 6793, name: "茂县", pinyin: "maoxian", shouzimu: "m" },
  { code: 6652, name: "眉山市", pinyin: "meishanshi", shouzimu: "m" },
  { code: 7935, name: "眉县", pinyin: "meixian", shouzimu: "m" },
  { code: 2454, name: "梅河口市", pinyin: "meihekoushi", shouzimu: "m" },
  { code: 5960, name: "梅州市", pinyin: "meizhoushi", shouzimu: "m" },
  { code: 7039, name: "湄潭县", pinyin: "meitanxian", shouzimu: "m" },
  { code: 6857, name: "美姑县", pinyin: "meiguxian", shouzimu: "m" },
  { code: 8541, name: "门源回族自治县", pinyin: "menyuanhuizuzizhixian", shouzimu: "m" },
  { code: 3727, name: "蒙城县", pinyin: "mengchengxian", shouzimu: "m" },
  { code: 6224, name: "蒙山县", pinyin: "mengshanxian", shouzimu: "m" },
  { code: 4739, name: "蒙阴县", pinyin: "mengyinxian", shouzimu: "m" },
  { code: 7433, name: "蒙自县", pinyin: "mengzixian", shouzimu: "m" },
  { code: 7492, name: "勐海县", pinyin: "menghaixian", shouzimu: "m" },
  { code: 7493, name: "勐腊县", pinyin: "menglaxian", shouzimu: "m" },
  { code: 1452, name: "孟村县", pinyin: "mengcunxian", shouzimu: "m" },
  { code: 4932, name: "孟津县", pinyin: "mengjinxian", shouzimu: "m" },
  { code: 7478, name: "孟连县", pinyin: "menglianxian", shouzimu: "m" },
  { code: 5016, name: "孟州市", pinyin: "mengzhoushi", shouzimu: "m" },
  { code: 7515, name: "弥渡县", pinyin: "miduxian", shouzimu: "m" },
  { code: 7437, name: "弥勒县", pinyin: "milexian", shouzimu: "m" },
  { code: 7832, name: "米林县", pinyin: "milinxian", shouzimu: "m" },
  { code: 8852, name: "米泉市", pinyin: "miquanshi", shouzimu: "m" },
  { code: 6561, name: "米易县", pinyin: "miyixian", shouzimu: "m" },
  { code: 8068, name: "米脂县", pinyin: "mizhixian", shouzimu: "m" },
  { code: 5113, name: "泌阳县", pinyin: "miyangxian", shouzimu: "m" },
  { code: 2758, name: "密山市", pinyin: "mishanshi", shouzimu: "m" },
  { code: 6590, name: "绵阳市", pinyin: "mianyangshi", shouzimu: "m" },
  { code: 6581, name: "绵竹市", pinyin: "mianzhushi", shouzimu: "m" },
  { code: 7996, name: "勉县", pinyin: "mianxian", shouzimu: "m" },
  { code: 6854, name: "冕宁县", pinyin: "mianningxian", shouzimu: "m" },
  { code: 5051, name: "渑池县", pinyin: "mianchixian", shouzimu: "m" },
  { code: 8968, name: "民丰县", pinyin: "minfengxian", shouzimu: "m" },
  { code: 8522, name: "民和回族土族自治县", pinyin: "minhehuizutuzuzizhixian", shouzimu: "m" },
  { code: 8273, name: "民乐县", pinyin: "minlexian", shouzimu: "m" },
  { code: 8282, name: "民勤县", pinyin: "minqinxian", shouzimu: "m" },
  { code: 5064, name: "民权县", pinyin: "minquanxian", shouzimu: "m" },
  { code: 8297, name: "岷县", pinyin: "minxian", shouzimu: "m" },
  { code: 3910, name: "闽侯县", pinyin: "minhouxian", shouzimu: "m" },
  { code: 3910, name: "闽清县", pinyin: "minqingxian", shouzimu: "m" },
  { code: 6772, name: "名山县", pinyin: "mingshanxian", shouzimu: "m" },
  { code: 3757, name: "明光市", pinyin: "mingguangshi", shouzimu: "m" },
  { code: 2772, name: "明水县", pinyin: "mingshuixian", shouzimu: "m" },
  { code: 3951, name: "明溪县", pinyin: "mingxixian", shouzimu: "m" },
  { code: 1966, name: "莫力达瓦达斡尔族自治旗", pinyin: "molidawadawoerzuzizhiqi", shouzimu: "m" },
  { code: 2793, name: "漠河县", pinyin: "mohexian", shouzimu: "m" },
  { code: 7473, name: "墨江县", pinyin: "mojiangxian", shouzimu: "m" },
  { code: 7833, name: "墨脱县", pinyin: "motuoxian", shouzimu: "m" },
  { code: 8963, name: "墨玉县", pinyin: "moyuxian", shouzimu: "m" },
  { code: 7700, name: "墨竹工卡县", pinyin: "mozhugongkaxian", shouzimu: "m" },
  { code: 7383, name: "牟定县", pinyin: "moudingxian", shouzimu: "m" },
  { code: 2720, name: "牡丹江市", pinyin: "mudanjiangshi", shouzimu: "m" },
  { code: 2610, name: "木兰县", pinyin: "mulanxian", shouzimu: "m" },
  { code: 8859, name: "木垒哈萨克自治县", pinyin: "muleihasakezizhixian", shouzimu: "m" },
  { code: 6842, name: "木里县", pinyin: "mulixian", shouzimu: "m" },
  { code: 6658, name: "沐川县", pinyin: "muchuanxian", shouzimu: "m" },
  { code: 2753, name: "穆棱市", pinyin: "mulengshi", shouzimu: "m" }],

  N: [
  { code: 6267, name: "那坡县", pinyin: "napoxian", shouzimu: "n" },
  { code: 7790, name: "那曲县", pinyin: "naquxian", shouzimu: "n" },
  { code: 6573, name: "纳溪区", pinyin: "naxiqu", shouzimu: "n" },
  { code: 7096, name: "纳雍县", pinyin: "nayongxian", shouzimu: "n" },
  { code: 7740, name: "乃东县", pinyin: "naidongxian", shouzimu: "n" },
  { code: 1997, name: "奈曼旗", pinyin: "naimanqi", shouzimu: "n" },
  { code: 3973, name: "南安市", pinyin: "nananshi", shouzimu: "n" },
  { code: 5863, name: "南澳县", pinyin: "nanaoxian", shouzimu: "n" },
  { code: 6734, name: "南部县", pinyin: "nanbuxian", shouzimu: "n" },
  { code: 4210, name: "南昌市", pinyin: "nanchangshi", shouzimu: "n" },
  { code: 4210, name: "南昌县", pinyin: "nanchangxian", shouzimu: "n" },
  { code: 4372, name: "南城县", pinyin: "nanchengxian", shouzimu: "n" },
  { code: 6730, name: "南充市", pinyin: "nanchongshi", shouzimu: "n" },
  { code: 6693, name: "南川市", pinyin: "nanchuanshi", shouzimu: "n" },
  { code: 6285, name: "南丹县", pinyin: "nandanxian", shouzimu: "n" },
  { code: 4374, name: "南丰县", pinyin: "nanfengxian", shouzimu: "n" },
  { code: 1321, name: "南宫市", pinyin: "nangongshi", shouzimu: "n" },
  { code: 1328, name: "南和县", pinyin: "nanhexian", shouzimu: "n" },
  { code: 7384, name: "南华县", pinyin: "nanhuaxian", shouzimu: "n" },
  { code: 7516, name: "南涧县", pinyin: "nanjianxian", shouzimu: "n" },
  { code: 6757, name: "南江县", pinyin: "nanjiangxian", shouzimu: "n" },
  { code: 3010, name: "南京市", pinyin: "nanjingshi", shouzimu: "n" },
  { code: 3997, name: "南靖县", pinyin: "nanjingxian", shouzimu: "n" },
  { code: 4283, name: "南康市", pinyin: "nankangshi", shouzimu: "n" },
  { code: 5022, name: "南乐县", pinyin: "nanlexian", shouzimu: "n" },
  { code: 3623, name: "南陵县", pinyin: "nanlingxian", shouzimu: "n" },
  { code: 7761, name: "南木林县", pinyin: "nanmulinxian", shouzimu: "n" },
  { code: 6110, name: "南宁市", pinyin: "nanningshi", shouzimu: "n" },
  { code: 1448, name: "南皮县", pinyin: "nanpixian", shouzimu: "n" },
  { code: 4010, name: "南平市", pinyin: "nanpingshi", shouzimu: "n" },
  { code: 3060, name: "南通市", pinyin: "nantongshi", shouzimu: "n" },
  { code: 6713, name: "南溪县", pinyin: "nanxixian", shouzimu: "n" },
  { code: 5614, name: "南县", pinyin: "nanxian", shouzimu: "n" },
  { code: 5823, name: "南雄市", pinyin: "nanxiongshi", shouzimu: "n" },
  { code: 5130, name: "南阳市", pinyin: "nanyangshi", shouzimu: "n" },
  { code: 5283, name: "南漳县", pinyin: "nanzhangxian", shouzimu: "n" },
  { code: 5133, name: "南召县", pinyin: "nanzhaoxian", shouzimu: "n" },
  { code: 7992, name: "南郑县", pinyin: "nanzhengxian", shouzimu: "n" },
  { code: 8585, name: "囊谦县", pinyin: "nangqianxian", shouzimu: "n" },
  { code: 2642, name: "讷河市", pinyin: "neheshi", shouzimu: "n" },
  { code: 4965, name: "内黄县", pinyin: "neihuangxian", shouzimu: "n" },
  { code: 6630, name: "内江市", pinyin: "neijiangshi", shouzimu: "n" },
  { code: 1324, name: "内邱县", pinyin: "neiqiuxian", shouzimu: "n" },
  { code: 5138, name: "内乡县", pinyin: "neixiangxian", shouzimu: "n" },
  { code: 2784, name: "嫩江县", pinyin: "nenjiangxian", shouzimu: "n" },
  { code: 8999, name: "尼勒克县", pinyin: "nilekexian", shouzimu: "n" },
  { code: 7799, name: "尼玛县", pinyin: "nimaxian", shouzimu: "n" },
  { code: 7700, name: "尼木县", pinyin: "nimuxian", shouzimu: "n" },
  { code: 7775, name: "聂拉木县", pinyin: "nielamuxian", shouzimu: "n" },
  { code: 7793, name: "聂荣县", pinyin: "nierongxian", shouzimu: "n" },
  { code: 2751, name: "宁安市", pinyin: "ninganshi", shouzimu: "n" },
  { code: 3320, name: "宁波市", pinyin: "ningboshi", shouzimu: "n" },
  { code: 1948, name: "宁城县", pinyin: "ningchengxian", shouzimu: "n" },
  { code: 4030, name: "宁德市", pinyin: "ningdeshi", shouzimu: "n" },
  { code: 4293, name: "宁都县", pinyin: "ningdouxian", shouzimu: "n" },
  { code: 3774, name: "宁国市", pinyin: "ningguoshi", shouzimu: "n" },
  { code: 3322, name: "宁海县", pinyin: "ninghaixian", shouzimu: "n" },
  { code: 3953, name: "宁化县", pinyin: "ninghuaxian", shouzimu: "n" },
  { code: 4693, name: "宁津县", pinyin: "ningjinxian", shouzimu: "n" },
  { code: 1329, name: "宁晋县", pinyin: "ningjinxian", shouzimu: "n" },
  { code: 7554, name: "宁蒗县", pinyin: "ninglangxian", shouzimu: "n" },
  { code: 5065, name: "宁陵县", pinyin: "ninglingxian", shouzimu: "n" },
  { code: 6132, name: "宁明县", pinyin: "ningmingxian", shouzimu: "n" },
  { code: 6847, name: "宁南县", pinyin: "ningnanxian", shouzimu: "n" },
  { code: 7997, name: "宁强县", pinyin: "ningqiangxian", shouzimu: "n" },
  { code: 8014, name: "宁陕县", pinyin: "ningshanxian", shouzimu: "n" },
  { code: 1717, name: "宁武县", pinyin: "ningwuxian", shouzimu: "n" },
  { code: 8347, name: "宁县", pinyin: "ningxian", shouzimu: "n" },
  { code: 5510, name: "宁乡县", pinyin: "ningxiangxian", shouzimu: "n" },
  { code: 4631, name: "宁阳县", pinyin: "ningyangxian", shouzimu: "n" },
  { code: 5655, name: "宁远县", pinyin: "ningyuanxian", shouzimu: "n" },
  { code: 2410, name: "农安县", pinyin: "nonganxian", shouzimu: "n" }],

  P: [
  { code: 6560, name: "攀枝花市", pinyin: "panzhihuashi", shouzimu: "p" },
  { code: 2320, name: "盘锦市", pinyin: "panjinshi", shouzimu: "p" },
  { code: 2322, name: "盘山县", pinyin: "panshanxian", shouzimu: "p" },
  { code: 7022, name: "盘县", pinyin: "panxian", shouzimu: "p" },
  { code: 3385, name: "磐安县", pinyin: "pananxian", shouzimu: "p" },
  { code: 2423, name: "磐石市", pinyin: "panshishi", shouzimu: "p" },
  { code: 3032, name: "沛县", pinyin: "peixian", shouzimu: "p" },
  { code: 6657, name: "彭山县", pinyin: "pengshanxian", shouzimu: "p" },
  { code: 6875, name: "彭水县", pinyin: "pengshuixian", shouzimu: "p" },
  { code: 8746, name: "彭阳县", pinyin: "pengyangxian", shouzimu: "p" },
  { code: 4251, name: "彭泽县", pinyin: "pengzexian", shouzimu: "p" },
  { code: 6510, name: "彭州市", pinyin: "pengzhoushi", shouzimu: "p" },
  { code: 6738, name: "蓬安县", pinyin: "penganxian", shouzimu: "p" },
  { code: 4561, name: "蓬莱市", pinyin: "penglaishi", shouzimu: "p" },
  { code: 6621, name: "蓬溪县", pinyin: "pengxixian", shouzimu: "p" },
  { code: 3035, name: "邳州市", pinyin: "pizhoushi", shouzimu: "p" },
  { code: 8964, name: "皮山县", pinyin: "pishanxian", shouzimu: "p" },
  { code: 6510, name: "郫县", pinyin: "pixian", shouzimu: "p" },
  { code: 1725, name: "偏关县", pinyin: "pianguanxian", shouzimu: "p" },
  { code: 8521, name: "平安县", pinyin: "pinganxian", shouzimu: "p" },
  { code: 7117, name: "平坝县", pinyin: "pingbaxian", shouzimu: "p" },
  { code: 6759, name: "平昌县", pinyin: "pingchangxian", shouzimu: "p" },
  { code: 8244, name: "平川区", pinyin: "pingchuanqu", shouzimu: "p" },
  { code: 4950, name: "平顶山市", pinyin: "pingdingshanshi", shouzimu: "p" },
  { code: 1651, name: "平定县", pinyin: "pingdingxian", shouzimu: "p" },
  { code: 4524, name: "平度市", pinyin: "pingdushi", shouzimu: "p" },
  { code: 6264, name: "平果县", pinyin: "pingguoxian", shouzimu: "p" },
  { code: 3998, name: "平和县", pinyin: "pinghexian", shouzimu: "p" },
  { code: 3352, name: "平湖市", pinyin: "pinghushi", shouzimu: "p" },
  { code: 5575, name: "平江县", pinyin: "pingjiangxian", shouzimu: "p" },
  { code: 6188, name: "平乐县", pinyin: "pinglexian", shouzimu: "p" },
  { code: 8017, name: "平利县", pinyin: "pinglixian", shouzimu: "p" },
  { code: 8330, name: "平凉市", pinyin: "pingliangshi", shouzimu: "p" },
  { code: 1823, name: "平陆县", pinyin: "pingluxian", shouzimu: "p" },
  { code: 8721, name: "平罗县", pinyin: "pingluoxian", shouzimu: "p" },
  { code: 6244, name: "平南县", pinyin: "pingnanxian", shouzimu: "p" },
  { code: 1423, name: "平泉县", pinyin: "pingquanxian", shouzimu: "p" },
  { code: 1210, name: "平山县", pinyin: "pingshanxian", shouzimu: "p" },
  { code: 1665, name: "平顺县", pinyin: "pingshunxian", shouzimu: "p" },
  { code: 3910, name: "平潭县", pinyin: "pingtanxian", shouzimu: "p" },
  { code: 7157, name: "平塘县", pinyin: "pingtangxian", shouzimu: "p" },
  { code: 6596, name: "平武县", pinyin: "pingwuxian", shouzimu: "p" },
  { code: 1334, name: "平乡县", pinyin: "pingxiangxian", shouzimu: "p" },
  { code: 3335, name: "平阳县", pinyin: "pingyangxian", shouzimu: "p" },
  { code: 1759, name: "平遥县", pinyin: "pingyaoxian", shouzimu: "p" },
  { code: 4741, name: "平邑县", pinyin: "pingyixian", shouzimu: "p" },
  { code: 4510, name: "平阴县", pinyin: "pingyinxian", shouzimu: "p" },
  { code: 5118, name: "平舆县", pinyin: "pingyuxian", shouzimu: "p" },
  { code: 4684, name: "平原县", pinyin: "pingyuanxian", shouzimu: "p" },
  { code: 5966, name: "平远县", pinyin: "pingyuanxian", shouzimu: "p" },
  { code: 6121, name: "凭祥市", pinyin: "pingxiangshi", shouzimu: "p" },
  { code: 7434, name: "屏边县", pinyin: "pingbianxian", shouzimu: "p" },
  { code: 4036, name: "屏南县", pinyin: "pingnanxian", shouzimu: "p" },
  { code: 6721, name: "屏山县", pinyin: "pingshanxian", shouzimu: "p" },
  { code: 4230, name: "萍乡市", pinyin: "pingxiangshi", shouzimu: "p" },
  { code: 4339, name: "鄱阳县", pinyin: "poyangxian", shouzimu: "p" },
  { code: 3940, name: "莆田市", pinyin: "putianshi", shouzimu: "p" },
  { code: 7977, name: "蒲城县", pinyin: "puchengxian", shouzimu: "p" },
  { code: 6510, name: "蒲江县", pinyin: "pujiangxian", shouzimu: "p" },
  { code: 1784, name: "蒲县", pinyin: "puxian", shouzimu: "p" },
  { code: 5020, name: "濮阳市", pinyin: "puyangshi", shouzimu: "p" },
  { code: 5025, name: "濮阳县", pinyin: "puyangxian", shouzimu: "p" },
  { code: 6315, name: "浦北县", pinyin: "pubeixian", shouzimu: "p" },
  { code: 4016, name: "浦城县", pinyin: "puchengxian", shouzimu: "p" },
  { code: 3384, name: "浦江县", pinyin: "pujiangxian", shouzimu: "p" },
  { code: 7073, name: "普安县", pinyin: "puanxian", shouzimu: "p" },
  { code: 7118, name: "普定县", pinyin: "pudingxian", shouzimu: "p" },
  { code: 7472, name: "普洱县", pinyin: "puerxian", shouzimu: "p" },
  { code: 6848, name: "普格县", pinyin: "pugexian", shouzimu: "p" },
  { code: 2222, name: "普兰店市", pinyin: "pulandianshi", shouzimu: "p" },
  { code: 7811, name: "普兰县", pinyin: "pulanxian", shouzimu: "p" },
  { code: 5867, name: "普宁市", pinyin: "puningshi", shouzimu: "p" }],

  Q: [
  { code: 2740, name: "七台河市", pinyin: "qitaiheshi", shouzimu: "q" },
  { code: 4563, name: "栖霞市", pinyin: "qixiashi", shouzimu: "q" },
  { code: 4687, name: "齐河县", pinyin: "qihexian", shouzimu: "q" },
  { code: 2640, name: "齐齐哈尔市", pinyin: "qiqihaershi", shouzimu: "q" },
  { code: 5546, name: "祁东县", pinyin: "qidongxian", shouzimu: "q" },
  { code: 8542, name: "祁连县", pinyin: "qilianxian", shouzimu: "q" },
  { code: 3714, name: "祁门县", pinyin: "qimenxian", shouzimu: "q" },
  { code: 1758, name: "祁县", pinyin: "qixian", shouzimu: "q" },
  { code: 5662, name: "祁阳县", pinyin: "qiyangxian", shouzimu: "q" },
  { code: 7933, name: "岐山县", pinyin: "qishanxian", shouzimu: "q" },
  { code: 8855, name: "奇台县", pinyin: "qitaixian", shouzimu: "q" },
  { code: 4972, name: "淇县", pinyin: "qixian", shouzimu: "q" },
  { code: 6530, name: "綦江县", pinyin: "qijiangxian", shouzimu: "q" },
  { code: 5338, name: "蕲春县", pinyin: "qichunxian", shouzimu: "q" },
  { code: 4921, name: "杞县", pinyin: "qixian", shouzimu: "q" },
  { code: 3066, name: "启东市", pinyin: "qidongshi", shouzimu: "q" },
  { code: 7937, name: "千阳县", pinyin: "qianyangxian", shouzimu: "q" },
  { code: 1246, name: "迁安市", pinyin: "qiananshi", shouzimu: "q" },
  { code: 1247, name: "迁西县", pinyin: "qianxixian", shouzimu: "q" },
  { code: 4335, name: "铅山县", pinyin: "qianshanxian", shouzimu: "q" },
  { code: 2476, name: "前郭县", pinyin: "qianguoxian", shouzimu: "q" },
  { code: 2479, name: "乾安县", pinyin: "qiananxian", shouzimu: "q" },
  { code: 7954, name: "乾县", pinyin: "qianxian", shouzimu: "q" },
  { code: 5375, name: "潜江市", pinyin: "qianjiangshi", shouzimu: "q" },
  { code: 3684, name: "潜山县", pinyin: "qianshanxian", shouzimu: "q" },
  { code: 6870, name: "黔江区", pinyin: "qianjiangqu", shouzimu: "q" },
  { code: 7093, name: "黔西县", pinyin: "qianxixian", shouzimu: "q" },
  { code: 7343, name: "巧家县", pinyin: "qiaojiaxian", shouzimu: "q" },
  { code: 8885, name: "且末县", pinyin: "qiemoxian", shouzimu: "q" },
  { code: 6311, name: "钦州市", pinyin: "qinzhoushi", shouzimu: "q" },
  { code: 8252, name: "秦安县", pinyin: "qinanxian", shouzimu: "q" },
  { code: 1260, name: "秦皇岛市", pinyin: "qinhuangdaoshi", shouzimu: "q" },
  { code: 1681, name: "沁水县", pinyin: "qinshuixian", shouzimu: "q" },
  { code: 1671, name: "沁县", pinyin: "qinxian", shouzimu: "q" },
  { code: 5014, name: "沁阳市", pinyin: "qinyangshi", shouzimu: "q" },
  { code: 1672, name: "沁源县", pinyin: "qinyuanxian", shouzimu: "q" },
  { code: 6612, name: "青川县", pinyin: "qingchuanxian", shouzimu: "q" },
  { code: 4520, name: "青岛市", pinyin: "qingdaoshi", shouzimu: "q" },
  { code: 2767, name: "青冈县", pinyin: "qinggangxian", shouzimu: "q" },
  { code: 9026, name: "青河县", pinyin: "qinghexian", shouzimu: "q" },
  { code: 1261, name: "青龙县", pinyin: "qinglongxian", shouzimu: "q" },
  { code: 6659, name: "青神县", pinyin: "qingshenxian", shouzimu: "q" },
  { code: 3432, name: "青田县", pinyin: "qingtianxian", shouzimu: "q" },
  { code: 8732, name: "青铜峡市", pinyin: "qingtongxiashi", shouzimu: "q" },
  { code: 1432, name: "青县", pinyin: "qingxian", shouzimu: "q" },
  { code: 3794, name: "青阳县", pinyin: "qingyangxian", shouzimu: "q" },
  { code: 4588, name: "青州市", pinyin: "qingzhoushi", shouzimu: "q" },
  { code: 5021, name: "清丰县", pinyin: "qingfengxian", shouzimu: "q" },
  { code: 1336, name: "清河县", pinyin: "qinghexian", shouzimu: "q" },
  { code: 8072, name: "清涧县", pinyin: "qingjianxian", shouzimu: "q" },
  { code: 3952, name: "清流县", pinyin: "qingliuxian", shouzimu: "q" },
  { code: 1910, name: "清水河县", pinyin: "qingshuihexian", shouzimu: "q" },
  { code: 8251, name: "清水县", pinyin: "qingshuixian", shouzimu: "q" },
  { code: 6017, name: "清新县", pinyin: "qingxinxian", shouzimu: "q" },
  { code: 1610, name: "清徐县", pinyin: "qingxuxian", shouzimu: "q" },
  { code: 2344, name: "清原县", pinyin: "qingyuanxian", shouzimu: "q" },
  { code: 6010, name: "清远市", pinyin: "qingyuanshi", shouzimu: "q" },
  { code: 1342, name: "清苑县", pinyin: "qingyuanxian", shouzimu: "q" },
  { code: 7010, name: "清镇市", pinyin: "qingzhenshi", shouzimu: "q" },
  { code: 7074, name: "晴隆县", pinyin: "qinglongxian", shouzimu: "q" },
  { code: 2771, name: "庆安县", pinyin: "qinganxian", shouzimu: "q" },
  { code: 8342, name: "庆城县", pinyin: "qingchengxian", shouzimu: "q" },
  { code: 8340, name: "庆阳市", pinyin: "qingyangshi", shouzimu: "q" },
  { code: 3434, name: "庆元县", pinyin: "qingyuanxian", shouzimu: "q" },
  { code: 4694, name: "庆云县", pinyin: "qingyunxian", shouzimu: "q" },
  { code: 6510, name: "邛崃市", pinyin: "daolaishi", shouzimu: "d" },
  { code: 6410, name: "琼海市", pinyin: "qionghaishi", shouzimu: "q" },
  { code: 7744, name: "琼结县", pinyin: "qiongjiexian", shouzimu: "q" },
  { code: 6410, name: "琼山市", pinyin: "qiongshanshi", shouzimu: "q" },
  { code: 6410, name: "琼中县", pinyin: "qiongzhongxian", shouzimu: "q" },
  { code: 7456, name: "邱北县", pinyin: "qiubeixian", shouzimu: "q" },
  { code: 1284, name: "邱县", pinyin: "qiuxian", shouzimu: "q" },
  { code: 6762, name: "渠县", pinyin: "quxian", shouzimu: "q" },
  { code: 3410, name: "衢州市", pinyin: "quzhoushi", shouzimu: "q" },
  { code: 4619, name: "曲阜市", pinyin: "qufushi", shouzimu: "q" },
  { code: 5820, name: "曲江县", pinyin: "qujiangxian", shouzimu: "q" },
  { code: 7360, name: "曲靖市", pinyin: "qujingshi", shouzimu: "q" },
  { code: 8586, name: "曲麻莱县", pinyin: "qumalaixian", shouzimu: "q" },
  { code: 7700, name: "曲水县", pinyin: "qushuixian", shouzimu: "q" },
  { code: 7745, name: "曲松县", pinyin: "qusongxian", shouzimu: "q" },
  { code: 1773, name: "曲沃县", pinyin: "quwoxian", shouzimu: "q" },
  { code: 1367, name: "曲阳县", pinyin: "quyangxian", shouzimu: "q" },
  { code: 1283, name: "曲周县", pinyin: "quzhouxian", shouzimu: "q" },
  { code: 3754, name: "全椒县", pinyin: "quanjiaoxian", shouzimu: "q" },
  { code: 4292, name: "全南县", pinyin: "quannanxian", shouzimu: "q" },
  { code: 6182, name: "全州县", pinyin: "quanzhouxian", shouzimu: "q" },
  { code: 3970, name: "泉州市", pinyin: "quanzhoushi", shouzimu: "q" },
  { code: 5112, name: "确山县", pinyin: "queshanxian", shouzimu: "q" }],

  R: [
  { code: 6801, name: "壤塘县", pinyin: "rangtangxian", shouzimu: "r" },
  { code: 2726, name: "饶河县", pinyin: "raohexian", shouzimu: "r" },
  { code: 5862, name: "饶平县", pinyin: "raopingxian", shouzimu: "r" },
  { code: 1487, name: "饶阳县", pinyin: "raoyangxian", shouzimu: "r" },
  { code: 7769, name: "仁布县", pinyin: "renbuxian", shouzimu: "r" },
  { code: 5824, name: "仁化县", pinyin: "renhuaxian", shouzimu: "r" },
  { code: 7042, name: "仁怀市", pinyin: "renhuaishi", shouzimu: "r" },
  { code: 6651, name: "仁寿县", pinyin: "renshouxian", shouzimu: "r" },
  { code: 1442, name: "任丘市", pinyin: "renqiushi", shouzimu: "r" },
  { code: 1327, name: "任县", pinyin: "renxian", shouzimu: "r" },
  { code: 7760, name: "日喀则市", pinyin: "rikazeshi", shouzimu: "r" },
  { code: 7813, name: "日土县", pinyin: "rituxian", shouzimu: "r" },
  { code: 4732, name: "日照市", pinyin: "rizhaoshi", shouzimu: "r" },
  { code: 6530, name: "荣昌县", pinyin: "rongchangxian", shouzimu: "r" },
  { code: 4653, name: "荣成市", pinyin: "rongchengshi", shouzimu: "r" },
  { code: 6551, name: "荣县", pinyin: "rongxian", shouzimu: "r" },
  { code: 1365, name: "容城县", pinyin: "rongchengxian", shouzimu: "r" },
  { code: 6245, name: "容县", pinyin: "rongxian", shouzimu: "r" },
  { code: 7143, name: "榕江县", pinyin: "rongjiangxian", shouzimu: "r" },
  { code: 6156, name: "融安县", pinyin: "ronganxian", shouzimu: "r" },
  { code: 6158, name: "融水县", pinyin: "rongshuixian", shouzimu: "r" },
  { code: 3063, name: "如东县", pinyin: "rudongxian", shouzimu: "r" },
  { code: 3062, name: "如皋市", pinyin: "rugaoshi", shouzimu: "r" },
  { code: 5639, name: "汝城县", pinyin: "ruchengxian", shouzimu: "r" },
  { code: 5117, name: "汝南县", pinyin: "runanxian", shouzimu: "r" },
  { code: 4936, name: "汝阳县", pinyin: "ruyangxian", shouzimu: "r" },
  { code: 4956, name: "汝州市", pinyin: "ruzhoushi", shouzimu: "r" },
  { code: 4651, name: "乳山市", pinyin: "rushanshi", shouzimu: "r" },
  { code: 5827, name: "乳源县", pinyin: "ruyuanxian", shouzimu: "r" },
  { code: 1813, name: "芮城县", pinyin: "ruichengxian", shouzimu: "r" },
  { code: 3339, name: "瑞安市", pinyin: "ruianshi", shouzimu: "r" },
  { code: 4242, name: "瑞昌市", pinyin: "ruichangshi", shouzimu: "r" },
  { code: 4296, name: "瑞金市", pinyin: "ruijinshi", shouzimu: "r" },
  { code: 7546, name: "瑞丽市", pinyin: "ruilishi", shouzimu: "r" },
  { code: 6803, name: "若尔盖县", pinyin: "ruoergaixian", shouzimu: "r" },
  { code: 8884, name: "若羌县", pinyin: "ruoqiangxian", shouzimu: "r" }],

  S: [
  { code: 7776, name: "萨嘎县", pinyin: "sagaxian", shouzimu: "s" },
  { code: 7764, name: "萨迦县", pinyin: "sajiaxian", shouzimu: "s" },
  { code: 9029, name: "三道岭地区", pinyin: "sandaolingdiqu", shouzimu: "s" },
  { code: 7163, name: "三都县", pinyin: "sandouxian", shouzimu: "s" },
  { code: 1461, name: "三河市", pinyin: "sanheshi", shouzimu: "s" },
  { code: 6157, name: "三江县", pinyin: "sanjiangxian", shouzimu: "s" },
  { code: 5050, name: "三门峡市", pinyin: "sanmenxiashi", shouzimu: "s" },
  { code: 3457, name: "三门县", pinyin: "sanmenxian", shouzimu: "s" },
  { code: 3950, name: "三明市", pinyin: "sanmingshi", shouzimu: "s" },
  { code: 7134, name: "三穗县", pinyin: "sansuixian", shouzimu: "s" },
  { code: 6591, name: "三台县", pinyin: "santaixian", shouzimu: "s" },
  { code: 6420, name: "三亚市", pinyin: "sanyashi", shouzimu: "s" },
  { code: 7952, name: "三原县", pinyin: "sanyuanxian", shouzimu: "s" },
  { code: 7743, name: "桑日县", pinyin: "sangrixian", shouzimu: "s" },
  { code: 5592, name: "桑植县", pinyin: "sangzhixian", shouzimu: "s" },
  { code: 6824, name: "色达县", pinyin: "sedaxian", shouzimu: "s" },
  { code: 1322, name: "沙河市", pinyin: "shaheshi", shouzimu: "s" },
  { code: 9014, name: "沙湾县", pinyin: "shawanxian", shouzimu: "s" },
  { code: 3956, name: "沙县", pinyin: "shaxian", shouzimu: "s" },
  { code: 8914, name: "沙雅县", pinyin: "shayaxian", shouzimu: "s" },
  { code: 5383, name: "沙洋县", pinyin: "shayangxian", shouzimu: "s" },
  { code: 8946, name: "莎车县", pinyin: "shachexian", shouzimu: "s" },
  { code: 3930, name: "厦门市", pinyin: "shamenshi", shouzimu: "s" },
  { code: 8276, name: "山丹县", pinyin: "shandanxian", shouzimu: "s" },
  { code: 8035, name: "山阳县", pinyin: "shanyangxian", shouzimu: "s" },
  { code: 1691, name: "山阴县", pinyin: "shanyinxian", shouzimu: "s" },
  { code: 5052, name: "陕县", pinyin: "shanxian", shouzimu: "s" },
  { code: 5860, name: "汕头市", pinyin: "shantoushi", shouzimu: "s" },
  { code: 5970, name: "汕尾市", pinyin: "shanweishi", shouzimu: "s" },
  { code: 8832, name: "鄯善县", pinyin: "shanshanxian", shouzimu: "s" },
  { code: 5158, name: "商城县", pinyin: "shangchengxian", shouzimu: "s" },
  { code: 2037, name: "商都县", pinyin: "shangdouxian", shouzimu: "s" },
  { code: 4510, name: "商河县", pinyin: "shanghexian", shouzimu: "s" },
  { code: 8030, name: "商洛市", pinyin: "shangluoshi", shouzimu: "s" },
  { code: 8034, name: "商南县", pinyin: "shangnanxian", shouzimu: "s" },
  { code: 5060, name: "商丘市", pinyin: "shangqiushi", shouzimu: "s" },
  { code: 5084, name: "商水县", pinyin: "shangshuixian", shouzimu: "s" },
  { code: 5116, name: "上蔡县", pinyin: "shangcaixian", shouzimu: "s" },
  { code: 4317, name: "上高县", pinyin: "shanggaoxian", shouzimu: "s" },
  { code: 2900, name: "上海市", pinyin: "shanghaishi", shouzimu: "s" },
  { code: 4054, name: "上杭县", pinyin: "shanghangxian", shouzimu: "s" },
  { code: 4231, name: "上栗县", pinyin: "shanglixian", shouzimu: "s" },
  { code: 6110, name: "上林县", pinyin: "shanglinxian", shouzimu: "s" },
  { code: 4330, name: "上饶市", pinyin: "shangraoshi", shouzimu: "s" },
  { code: 4332, name: "上饶县", pinyin: "shangraoxian", shouzimu: "s" },
  { code: 6312, name: "上思县", pinyin: "shangsixian", shouzimu: "s" },
  { code: 4286, name: "上犹县", pinyin: "shangyouxian", shouzimu: "s" },
  { code: 3372, name: "上虞市", pinyin: "shangyushi", shouzimu: "s" },
  { code: 1394, name: "尚义县", pinyin: "shangyixian", shouzimu: "s" },
  { code: 2610, name: "尚志市", pinyin: "shangzhishi", shouzimu: "s" },
  { code: 5820, name: "韶关市", pinyin: "shaoguanshi", shouzimu: "s" },
  { code: 5533, name: "韶山市", pinyin: "shaoshanshi", shouzimu: "s" },
  { code: 5551, name: "邵东县", pinyin: "shaodongxian", shouzimu: "s" },
  { code: 4012, name: "邵武市", pinyin: "shaowushi", shouzimu: "s" },
  { code: 5550, name: "邵阳市", pinyin: "shaoyangshi", shouzimu: "s" },
  { code: 5553, name: "邵阳县", pinyin: "shaoyangxian", shouzimu: "s" },
  { code: 3370, name: "绍兴市", pinyin: "shaoxingshi", shouzimu: "s" },
  { code: 3371, name: "绍兴县", pinyin: "shaoxingxian", shouzimu: "s" },
  { code: 5141, name: "社旗县", pinyin: "sheqixian", shouzimu: "s" },
  { code: 6622, name: "射洪县", pinyin: "shehongxian", shouzimu: "s" },
  { code: 3114, name: "射阳县", pinyin: "sheyangxian", shouzimu: "s" },
  { code: 1292, name: "涉县", pinyin: "shexian", shouzimu: "s" },
  { code: 7795, name: "申扎县", pinyin: "shenzhaxian", shouzimu: "s" },
  { code: 4714, name: "莘县", pinyin: "shenxian", shouzimu: "s" },
  { code: 1210, name: "深泽县", pinyin: "shenzexian", shouzimu: "s" },
  { code: 5840, name: "深圳市", pinyin: "shenzhenshi", shouzimu: "s" },
  { code: 1485, name: "深州市", pinyin: "shenzhoushi", shouzimu: "s" },
  { code: 6583, name: "什邡市", pinyin: "shenfangshi", shouzimu: "s" },
  { code: 1719, name: "神池县", pinyin: "shenchixian", shouzimu: "s" },
  { code: 8062, name: "神木县", pinyin: "shenmuxian", shouzimu: "s" },
  { code: 5311, name: "神农架林区", pinyin: "shennongjialinqu", shouzimu: "s" },
  { code: 5089, name: "沈丘县", pinyin: "shenqiuxian", shouzimu: "s" },
  { code: 2210, name: "沈阳市", pinyin: "shenyangshi", shouzimu: "s" },
  { code: 3422, name: "嵊泗县", pinyin: "shengsixian", shouzimu: "s" },
  { code: 3373, name: "嵊州市", pinyin: "shengzhoushi", shouzimu: "s" },
  { code: 7366, name: "师宗县", pinyin: "shizongxian", shouzimu: "s" },
  { code: 7133, name: "施秉县", pinyin: "shibingxian", shouzimu: "s" },
  { code: 7532, name: "施甸县", pinyin: "shidianxian", shouzimu: "s" },
  { code: 5230, name: "十堰市", pinyin: "shiyanshi", shouzimu: "s" },
  { code: 4299, name: "石城县", pinyin: "shichengxian", shouzimu: "s" },
  { code: 9028, name: "石河子市", pinyin: "shihezishi", shouzimu: "s" },
  { code: 1210, name: "石家庄市", pinyin: "shijiazhuangshi", shouzimu: "s" },
  { code: 7310, name: "石林县", pinyin: "shilinxian", shouzimu: "s" },
  { code: 1738, name: "石楼县", pinyin: "shilouxian", shouzimu: "s" },
  { code: 5586, name: "石门县", pinyin: "shimenxian", shouzimu: "s" },
  { code: 6775, name: "石棉县", pinyin: "shimianxian", shouzimu: "s" },
  { code: 7436, name: "石屏县", pinyin: "shipingxian", shouzimu: "s" },
  { code: 7054, name: "石阡县", pinyin: "shiqianxian", shouzimu: "s" },
  { code: 6823, name: "石渠县", pinyin: "shiquxian", shouzimu: "s" },
  { code: 8013, name: "石泉县", pinyin: "shiquanxian", shouzimu: "s" },
  { code: 3978, name: "石狮市", pinyin: "shishishi", shouzimu: "s" },
  { code: 5372, name: "石首市", pinyin: "shishoushi", shouzimu: "s" },
  { code: 3793, name: "石台县", pinyin: "shitaixian", shouzimu: "s" },
  { code: 6871, name: "石柱县", pinyin: "shizhuxian", shouzimu: "s" },
  { code: 8720, name: "石嘴山市", pinyin: "shizuishanshi", shouzimu: "s" },
  { code: 5822, name: "始兴县", pinyin: "shixingxian", shouzimu: "s" },
  { code: 4582, name: "寿光市", pinyin: "shouguangshi", shouzimu: "s" },
  { code: 4037, name: "寿宁县", pinyin: "shouningxian", shouzimu: "s" },
  { code: 3763, name: "寿县", pinyin: "shouxian", shouzimu: "s" },
  { code: 1756, name: "寿阳县", pinyin: "shouyangxian", shouzimu: "s" },
  { code: 3765, name: "舒城县", pinyin: "shuchengxian", shouzimu: "s" },
  { code: 2426, name: "舒兰市", pinyin: "shulanshi", shouzimu: "s" },
  { code: 8942, name: "疏附县", pinyin: "shufuxian", shouzimu: "s" },
  { code: 8943, name: "疏勒县", pinyin: "shulexian", shouzimu: "s" },
  { code: 3083, name: "沭阳县", pinyin: "shuyangxian", shouzimu: "s" },
  { code: 7382, name: "双柏县", pinyin: "shuangbaixian", shouzimu: "s" },
  { code: 2610, name: "双城市", pinyin: "shuangchengshi", shouzimu: "s" },
  { code: 5624, name: "双峰县", pinyin: "shuangfengxian", shouzimu: "s" },
  { code: 7800, name: "双湖特别行政区", pinyin: "shuanghutebeixingzhengqu", shouzimu: "s" },
  { code: 7586, name: "双江县", pinyin: "shuangjiangxian", shouzimu: "s" },
  { code: 2433, name: "双辽市", pinyin: "shuangliaoshi", shouzimu: "s" },
  { code: 6510, name: "双流县", pinyin: "shuangliuxian", shouzimu: "s" },
  { code: 5661, name: "双牌县", pinyin: "shuangpaixian", shouzimu: "s" },
  { code: 6530, name: "双桥区", pinyin: "shuangqiaoqu", shouzimu: "s" },
  { code: 2680, name: "双鸭山市", pinyin: "shuangyashanshi", shouzimu: "s" },
  { code: 7352, name: "水富县", pinyin: "shuifuxian", shouzimu: "s" },
  { code: 4013, name: "顺昌县", pinyin: "shunchangxian", shouzimu: "s" },
  { code: 1357, name: "顺平县", pinyin: "shunpingxian", shouzimu: "s" },
  { code: 1690, name: "朔州市", pinyin: "shuozhoushi", shouzimu: "s" },
  { code: 7470, name: "思茅市", pinyin: "simaoshi", shouzimu: "s" },
  { code: 7055, name: "思南县", pinyin: "sinanxian", shouzimu: "s" },
  { code: 5932, name: "四会市", pinyin: "sihuishi", shouzimu: "s" },
  { code: 2430, name: "四平市", pinyin: "sipingshi", shouzimu: "s" },
  { code: 2046, name: "四子王旗", pinyin: "siziwangqi", shouzimu: "s" },
  { code: 3086, name: "泗洪县", pinyin: "sihongxian", shouzimu: "s" },
  { code: 4618, name: "泗水县", pinyin: "sishuixian", shouzimu: "s" },
  { code: 3746, name: "泗县", pinyin: "sixian", shouzimu: "s" },
  { code: 3093, name: "泗阳县", pinyin: "siyangxian", shouzimu: "s" },
  { code: 6794, name: "松潘县", pinyin: "songpanxian", shouzimu: "s" },
  { code: 7059, name: "松桃县", pinyin: "songtaoxian", shouzimu: "s" },
  { code: 4019, name: "松溪县", pinyin: "songxixian", shouzimu: "s" },
  { code: 3437, name: "松阳县", pinyin: "songyangxian", shouzimu: "s" },
  { code: 2520, name: "松原市", pinyin: "songyuanshi", shouzimu: "s" },
  { code: 5377, name: "松滋市", pinyin: "songzishi", shouzimu: "s" },
  { code: 7310, name: "嵩明县", pinyin: "songmingxian", shouzimu: "s" },
  { code: 4935, name: "嵩县", pinyin: "songxian", shouzimu: "s" },
  { code: 2015, name: "苏尼特右旗", pinyin: "suniteyouqi", shouzimu: "s" },
  { code: 2014, name: "苏尼特左旗", pinyin: "sunitezuoqi", shouzimu: "s" },
  { code: 3050, name: "苏州市", pinyin: "suzhoushi", shouzimu: "s" },
  { code: 8265, name: "肃北县", pinyin: "subeixian", shouzimu: "s" },
  { code: 8272, name: "肃南县", pinyin: "sunanxian", shouzimu: "s" },
  { code: 1444, name: "肃宁县", pinyin: "suningxian", shouzimu: "s" },
  { code: 3034, name: "睢宁县", pinyin: "suiningxian", shouzimu: "s" },
  { code: 5066, name: "睢县", pinyin: "suixian", shouzimu: "s" },
  { code: 3661, name: "濉溪县", pinyin: "suixixian", shouzimu: "s" },
  { code: 2672, name: "绥滨县", pinyin: "suibinxian", shouzimu: "s" },
  { code: 8067, name: "绥德县", pinyin: "suidexian", shouzimu: "s" },
  { code: 2757, name: "绥芬河市", pinyin: "suifenheshi", shouzimu: "s" },
  { code: 2760, name: "绥化市", pinyin: "suihuashi", shouzimu: "s" },
  { code: 7347, name: "绥江县", pinyin: "suijiangxian", shouzimu: "s" },
  { code: 2773, name: "绥棱县", pinyin: "suilengxian", shouzimu: "s" },
  { code: 5557, name: "绥宁县", pinyin: "suiningxian", shouzimu: "s" },
  { code: 7034, name: "绥阳县", pinyin: "suiyangxian", shouzimu: "s" },
  { code: 2271, name: "绥中县", pinyin: "suizhongxian", shouzimu: "s" },
  { code: 5286, name: "随州市", pinyin: "suizhoushi", shouzimu: "s" },
  { code: 3436, name: "遂昌县", pinyin: "suichangxian", shouzimu: "s" },
  { code: 4359, name: "遂川县", pinyin: "suichuanxian", shouzimu: "s" },
  { code: 6620, name: "遂宁市", pinyin: "suiningshi", shouzimu: "s" },
  { code: 5114, name: "遂平县", pinyin: "suipingxian", shouzimu: "s" },
  { code: 5913, name: "遂溪县", pinyin: "suixixian", shouzimu: "s" },
  { code: 2787, name: "孙吴县", pinyin: "sunwuxian", shouzimu: "s" },
  { code: 7796, name: "索县", pinyin: "suoxian", shouzimu: "s" }],

  T: [
  { code: 9010, name: "塔城市", pinyin: "tachengshi", shouzimu: "t" },
  { code: 2792, name: "塔河县", pinyin: "tahexian", shouzimu: "t" },
  { code: 8953, name: "塔什库尔干塔吉克自治县", pinyin: "tashenkuergantajikezizhixian", shouzimu: "t" },
  { code: 2231, name: "台安县", pinyin: "taianxian", shouzimu: "t" },
  { code: 7141, name: "台江县", pinyin: "taijiangxian", shouzimu: "t" },
  { code: 5024, name: "台前县", pinyin: "taiqianxian", shouzimu: "t" },
  { code: 5892, name: "台山市", pinyin: "taishanshi", shouzimu: "t" },
  { code: 3450, name: "台州市", pinyin: "taizhoushi", shouzimu: "t" },
  { code: 7941, name: "太白县", pinyin: "taibaixian", shouzimu: "t" },
  { code: 3051, name: "太仓市", pinyin: "taicangshi", shouzimu: "t" },
  { code: 1757, name: "太谷县", pinyin: "taiguxian", shouzimu: "t" },
  { code: 3725, name: "太和县", pinyin: "taihexian", shouzimu: "t" },
  { code: 3685, name: "太湖县", pinyin: "taihuxian", shouzimu: "t" },
  { code: 5085, name: "太康县", pinyin: "taikangxian", shouzimu: "t" },
  { code: 2018, name: "太仆寺旗", pinyin: "taipusiqi", shouzimu: "t" },
  { code: 1610, name: "太原市", pinyin: "taiyuanshi", shouzimu: "t" },
  { code: 4630, name: "泰安市", pinyin: "taianshi", shouzimu: "t" },
  { code: 4358, name: "泰和县", pinyin: "taihexian", shouzimu: "t" },
  { code: 2644, name: "泰来县", pinyin: "tailaixian", shouzimu: "t" },
  { code: 3958, name: "泰宁县", pinyin: "tainingxian", shouzimu: "t" },
  { code: 3338, name: "泰顺县", pinyin: "taishunxian", shouzimu: "t" },
  { code: 3124, name: "泰兴市", pinyin: "taixingshi", shouzimu: "t" },
  { code: 3128, name: "泰州市", pinyin: "taizhoushi", shouzimu: "t" },
  { code: 4733, name: "郯城县", pinyin: "tanchengxian", shouzimu: "t" },
  { code: 4963, name: "汤阴县", pinyin: "tangyinxian", shouzimu: "t" },
  { code: 2725, name: "汤原县", pinyin: "tangyuanxian", shouzimu: "t" },
  { code: 1251, name: "唐海县", pinyin: "tanghaixian", shouzimu: "t" },
  { code: 5142, name: "唐河县", pinyin: "tanghexian", shouzimu: "t" },
  { code: 1240, name: "唐山市", pinyin: "tangshanshi", shouzimu: "t" },
  { code: 1358, name: "唐县", pinyin: "tangxian", shouzimu: "t" },
  { code: 2472, name: "洮南市", pinyin: "taonanshi", shouzimu: "t" },
  { code: 5615, name: "桃江县", pinyin: "taojiangxian", shouzimu: "t" },
  { code: 5585, name: "桃源县", pinyin: "taoyuanxian", shouzimu: "t" },
  { code: 8998, name: "特克斯县", pinyin: "tekesixian", shouzimu: "t" },
  { code: 7533, name: "腾冲县", pinyin: "tengchongxian", shouzimu: "t" },
  { code: 4541, name: "滕州市", pinyin: "tengzhoushi", shouzimu: "t" },
  { code: 6222, name: "藤县", pinyin: "tengxian", shouzimu: "t" },
  { code: 6131, name: "天等县", pinyin: "tiandengxian", shouzimu: "t" },
  { code: 6286, name: "天峨县", pinyin: "tianexian", shouzimu: "t" },
  { code: 1100, name: "天津市", pinyin: "tianjinshi", shouzimu: "t" },
  { code: 8595, name: "天峻县", pinyin: "tianjunxian", shouzimu: "t" },
  { code: 5374, name: "天门市", pinyin: "tianmenshi", shouzimu: "t" },
  { code: 6776, name: "天全县", pinyin: "tianquanxian", shouzimu: "t" },
  { code: 8250, name: "天水市", pinyin: "tianshuishi", shouzimu: "t" },
  { code: 3456, name: "天台县", pinyin: "tiantaixian", shouzimu: "t" },
  { code: 3752, name: "天长市", pinyin: "tianchangshi", shouzimu: "t" },
  { code: 1632, name: "天镇县", pinyin: "tianzhenxian", shouzimu: "t" },
  { code: 7137, name: "天柱县", pinyin: "tianzhuxian", shouzimu: "t" },
  { code: 8284, name: "天祝县", pinyin: "tianzhuxian", shouzimu: "t" },
  { code: 6263, name: "田东县", pinyin: "tiandongxian", shouzimu: "t" },
  { code: 6271, name: "田林县", pinyin: "tianlinxian", shouzimu: "t" },
  { code: 6262, name: "田阳县", pinyin: "tianyangxian", shouzimu: "t" },
  { code: 2336, name: "调兵山市", pinyin: "tiaobingshanshi", shouzimu: "t" },
  { code: 2712, name: "铁力市", pinyin: "tielishi", shouzimu: "t" },
  { code: 2330, name: "铁岭市", pinyin: "tielingshi", shouzimu: "t" },
  { code: 5364, name: "通城县", pinyin: "tongchengxian", shouzimu: "t" },
  { code: 5683, name: "通道县", pinyin: "tongdaoxian", shouzimu: "t" },
  { code: 7414, name: "通海县", pinyin: "tonghaixian", shouzimu: "t" },
  { code: 2610, name: "通河县", pinyin: "tonghexian", shouzimu: "t" },
  { code: 2450, name: "通化市", pinyin: "tonghuashi", shouzimu: "t" },
  { code: 2451, name: "通化县", pinyin: "tonghuaxian", shouzimu: "t" },
  { code: 6756, name: "通江县", pinyin: "tongjiangxian", shouzimu: "t" },
  { code: 1990, name: "通辽市", pinyin: "tongliaoshi", shouzimu: "t" },
  { code: 5366, name: "通山县", pinyin: "tongshanxian", shouzimu: "t" },
  { code: 8292, name: "通渭县", pinyin: "tongweixian", shouzimu: "t" },
  { code: 4922, name: "通许县", pinyin: "tongxuxian", shouzimu: "t" },
  { code: 2478, name: "通榆县", pinyin: "tongyuxian", shouzimu: "t" },
  { code: 3084, name: "通州市", pinyin: "tongzhoushi", shouzimu: "t" },
  { code: 8562, name: "同德县", pinyin: "tongdexian", shouzimu: "t" },
  { code: 2729, name: "同江市", pinyin: "tongjiangshi", shouzimu: "t" },
  { code: 8551, name: "同仁县", pinyin: "tongrenxian", shouzimu: "t" },
  { code: 8737, name: "同心县", pinyin: "tongxinxian", shouzimu: "t" },
  { code: 5144, name: "桐柏县", pinyin: "tongbaixian", shouzimu: "t" },
  { code: 3681, name: "桐城市", pinyin: "tongchengshi", shouzimu: "t" },
  { code: 3310, name: "桐庐县", pinyin: "tongluxian", shouzimu: "t" },
  { code: 3354, name: "桐乡市", pinyin: "tongxiangshi", shouzimu: "t" },
  { code: 7033, name: "桐梓县", pinyin: "tongzixian", shouzimu: "t" },
  { code: 7920, name: "铜川市", pinyin: "tongchuanshi", shouzimu: "t" },
  { code: 4321, name: "铜鼓县", pinyin: "tongguxian", shouzimu: "t" },
  { code: 6530, name: "铜梁县", pinyin: "tongliangxian", shouzimu: "t" },
  { code: 3670, name: "铜陵市", pinyin: "tonglingshi", shouzimu: "t" },
  { code: 3671, name: "铜陵县", pinyin: "tonglingxian", shouzimu: "t" },
  { code: 7050, name: "铜仁市", pinyin: "tongrenshi", shouzimu: "t" },
  { code: 3033, name: "铜山县", pinyin: "tongshanxian", shouzimu: "t" },
  { code: 7975, name: "潼关县", pinyin: "tongguanxian", shouzimu: "t" },
  { code: 6530, name: "潼南县", pinyin: "tongnanxian", shouzimu: "t" },
  { code: 1985, name: "突泉县", pinyin: "tuquanxian", shouzimu: "t" },
  { code: 2492, name: "图们市", pinyin: "tumenshi", shouzimu: "t" },
  { code: 9032, name: "图木舒克市", pinyin: "tumushukeshi", shouzimu: "t" },
  { code: 1921, name: "土默特右旗", pinyin: "tumoteyouqi", shouzimu: "t" },
  { code: 1910, name: "土左旗", pinyin: "tuzuoqi", shouzimu: "t" },
  { code: 8830, name: "吐鲁番市", pinyin: "tulufanshi", shouzimu: "t" },
  { code: 5333, name: "团风县", pinyin: "tuanfengxian", shouzimu: "t" },
  { code: 6410, name: "屯昌县", pinyin: "tunchangxian", shouzimu: "t" },
  { code: 1664, name: "屯留县", pinyin: "tunliuxian", shouzimu: "t" },
  { code: 1910, name: "托克托县", pinyin: "tuoketuoxian", shouzimu: "t" },
  { code: 8833, name: "托克逊县", pinyin: "tuokexunxian", shouzimu: "t" },
  { code: 9015, name: "托里县", pinyin: "tuolixian", shouzimu: "t" }],

  W: [
  { code: 2224, name: "瓦房店市", pinyin: "wafangdianshi", shouzimu: "w" },
  { code: 4210, name: "湾里", pinyin: "wanli", shouzimu: "w" },
  { code: 4361, name: "万安县", pinyin: "wananxian", shouzimu: "w" },
  { code: 4341, name: "万年县", pinyin: "wannianxian", shouzimu: "w" },
  { code: 6410, name: "万宁市", pinyin: "wanningshi", shouzimu: "w" },
  { code: 1398, name: "万全县", pinyin: "wanquanxian", shouzimu: "w" },
  { code: 1815, name: "万荣县", pinyin: "wanrongxian", shouzimu: "w" },
  { code: 7061, name: "万山特区", pinyin: "wanshantequ", shouzimu: "w" },
  { code: 6530, name: "万盛区", pinyin: "wanshengqu", shouzimu: "w" },
  { code: 6755, name: "万源市", pinyin: "wanyuanshi", shouzimu: "w" },
  { code: 4316, name: "万载县", pinyin: "wanzaixian", shouzimu: "w" },
  { code: 6670, name: "万州区", pinyin: "wanzhouqu", shouzimu: "w" },
  { code: 2497, name: "汪清县", pinyin: "wangqingxian", shouzimu: "w" },
  { code: 6611, name: "旺苍县", pinyin: "wangcangxian", shouzimu: "w" },
  { code: 5510, name: "望城县", pinyin: "wangchengxian", shouzimu: "w" },
  { code: 1359, name: "望都县", pinyin: "wangdouxian", shouzimu: "w" },
  { code: 3687, name: "望江县", pinyin: "wangjiangxian", shouzimu: "w" },
  { code: 2765, name: "望奎县", pinyin: "wangkuixian", shouzimu: "w" },
  { code: 7076, name: "望漠县", pinyin: "wangmoxian", shouzimu: "w" },
  { code: 4650, name: "威海市", pinyin: "weihaishi", shouzimu: "w" },
  { code: 7097, name: "威宁县", pinyin: "weiningxian", shouzimu: "w" },
  { code: 1335, name: "威县", pinyin: "weixian", shouzimu: "w" },
  { code: 7351, name: "威信县", pinyin: "weixinxian", shouzimu: "w" },
  { code: 6634, name: "威远县", pinyin: "weiyuanxian", shouzimu: "w" },
  { code: 4613, name: "微山县", pinyin: "weishanxian", shouzimu: "w" },
  { code: 7517, name: "巍山县", pinyin: "weishanxian", shouzimu: "w" },
  { code: 1427, name: "围场县", pinyin: "weichangxian", shouzimu: "w" },
  { code: 7573, name: "维西县", pinyin: "weixixian", shouzimu: "w" },
  { code: 4580, name: "潍坊市", pinyin: "weifangshi", shouzimu: "w" },
  { code: 4987, name: "卫辉市", pinyin: "weihuishi", shouzimu: "w" },
  { code: 8883, name: "尉犁县", pinyin: "weilixian", shouzimu: "w" },
  { code: 4923, name: "尉氏县", pinyin: "weishixian", shouzimu: "w" },
  { code: 7970, name: "渭南市", pinyin: "weinanshi", shouzimu: "w" },
  { code: 8294, name: "渭源县", pinyin: "weiyuanxian", shouzimu: "w" },
  { code: 1395, name: "蔚县", pinyin: "weixian", shouzimu: "w" },
  { code: 1282, name: "魏县", pinyin: "weixian", shouzimu: "w" },
  { code: 6510, name: "温江区", pinyin: "wenjiangqu", shouzimu: "w" },
  { code: 3454, name: "温岭市", pinyin: "wenlingshi", shouzimu: "w" },
  { code: 8873, name: "温泉县", pinyin: "wenquanxian", shouzimu: "w" },
  { code: 5015, name: "温县", pinyin: "wenxian", shouzimu: "w" },
  { code: 8912, name: "温宿县", pinyin: "wenxiuxian", shouzimu: "w" },
  { code: 3330, name: "温州市", pinyin: "wenzhoushi", shouzimu: "w" },
  { code: 1466, name: "文安县", pinyin: "wenanxian", shouzimu: "w" },
  { code: 6410, name: "文昌市", pinyin: "wenchangshi", shouzimu: "w" },
  { code: 3337, name: "文成县", pinyin: "wenchengxian", shouzimu: "w" },
  { code: 4652, name: "文登市", pinyin: "wendengshi", shouzimu: "w" },
  { code: 7450, name: "文山县", pinyin: "wenshanxian", shouzimu: "w" },
  { code: 1732, name: "文水县", pinyin: "wenshuixian", shouzimu: "w" },
  { code: 8315, name: "文县", pinyin: "wenxian", shouzimu: "w" },
  { code: 1819, name: "闻喜县", pinyin: "wenxixian", shouzimu: "w" },
  { code: 6791, name: "汶川县", pinyin: "wenchuanxian", shouzimu: "w" },
  { code: 4617, name: "汶上县", pinyin: "wenshangxian", shouzimu: "w" },
  { code: 1946, name: "翁牛特旗", pinyin: "wengniuteqi", shouzimu: "w" },
  { code: 5826, name: "翁源县", pinyin: "wengyuanxian", shouzimu: "w" },
  { code: 7155, name: "瓮安县", pinyin: "wenganxian", shouzimu: "w" },
  { code: 3726, name: "涡阳县", pinyin: "woyangxian", shouzimu: "w" },
  { code: 1930, name: "乌海市", pinyin: "wuhaishi", shouzimu: "w" },
  { code: 2076, name: "乌拉特后旗", pinyin: "wulatehouqi", shouzimu: "w" },
  { code: 2074, name: "乌拉特前旗", pinyin: "wulateqianqi", shouzimu: "w" },
  { code: 2075, name: "乌拉特中旗", pinyin: "wulatezhongqi", shouzimu: "w" },
  { code: 2030, name: "乌兰察布盟", pinyin: "wulanchabumeng", shouzimu: "w" },
  { code: 8593, name: "乌兰县", pinyin: "wulanxian", shouzimu: "w" },
  { code: 8810, name: "乌鲁木齐市", pinyin: "wulumuqishi", shouzimu: "w" },
  { code: 8810, name: "乌鲁木齐县", pinyin: "wulumuqixian", shouzimu: "w" },
  { code: 8934, name: "乌恰县", pinyin: "wuqiaxian", shouzimu: "w" },
  { code: 8917, name: "乌什县", pinyin: "wushenxian", shouzimu: "w" },
  { code: 2057, name: "乌审旗", pinyin: "wushenqi", shouzimu: "w" },
  { code: 9013, name: "乌苏市", pinyin: "wusushi", shouzimu: "w" },
  { code: 6678, name: "巫山县", pinyin: "wushanxian", shouzimu: "w" },
  { code: 6679, name: "巫溪县", pinyin: "wuxixian", shouzimu: "w" },
  { code: 4664, name: "无棣县", pinyin: "wudixian", shouzimu: "w" },
  { code: 1210, name: "无极县", pinyin: "wujixian", shouzimu: "w" },
  { code: 3783, name: "无为县", pinyin: "wuweixian", shouzimu: "w" },
  { code: 3020, name: "无锡市", pinyin: "wuxishi", shouzimu: "w" },
  { code: 3620, name: "芜湖市", pinyin: "wuhushi", shouzimu: "w" },
  { code: 3621, name: "芜湖县", pinyin: "wuhuxian", shouzimu: "w" },
  { code: 8071, name: "吴堡县", pinyin: "wubaoxian", shouzimu: "w" },
  { code: 5911, name: "吴川市", pinyin: "wuchuanshi", shouzimu: "w" },
  { code: 3054, name: "吴江市", pinyin: "wujiangshi", shouzimu: "w" },
  { code: 8047, name: "吴旗县", pinyin: "wuqixian", shouzimu: "w" },
  { code: 1446, name: "吴桥县", pinyin: "wuqiaoxian", shouzimu: "w" },
  { code: 8731, name: "吴忠市", pinyin: "wuzhongshi", shouzimu: "w" },
  { code: 6210, name: "梧州市", pinyin: "wuzhoushi", shouzimu: "w" },
  { code: 2610, name: "五常市", pinyin: "wuchangshi", shouzimu: "w" },
  { code: 2783, name: "五大连池市", pinyin: "wudalianchishi", shouzimu: "w" },
  { code: 5269, name: "五峰县", pinyin: "wufengxian", shouzimu: "w" },
  { code: 3632, name: "五河县", pinyin: "wuhexian", shouzimu: "w" },
  { code: 5964, name: "五华县", pinyin: "wuhuaxian", shouzimu: "w" },
  { code: 8858, name: "五家渠市", pinyin: "wujiaqushi", shouzimu: "w" },
  { code: 4587, name: "五莲县", pinyin: "wulianxian", shouzimu: "w" },
  { code: 1713, name: "五台县", pinyin: "wutaixian", shouzimu: "w" },
  { code: 2072, name: "五原县", pinyin: "wuyuanxian", shouzimu: "w" },
  { code: 1721, name: "五寨县", pinyin: "wuzhaixian", shouzimu: "w" },
  { code: 6410, name: "五指山市", pinyin: "wuzhishanshi", shouzimu: "w" },
  { code: 1295, name: "武安市", pinyin: "wuanshi", shouzimu: "w" },
  { code: 4686, name: "武城县", pinyin: "wuchengxian", shouzimu: "w" },
  { code: 1910, name: "武川县", pinyin: "wuchuanxian", shouzimu: "w" },
  { code: 7389, name: "武定县", pinyin: "wudingxian", shouzimu: "w" },
  { code: 5556, name: "武冈市", pinyin: "wugangshi", shouzimu: "w" },
  { code: 7962, name: "武功县", pinyin: "wugongxian", shouzimu: "w" },
  { code: 5210, name: "武汉市", pinyin: "wuhanshi", shouzimu: "w" },
  { code: 6695, name: "武隆县", pinyin: "wulongxian", shouzimu: "w" },
  { code: 6110, name: "武鸣县", pinyin: "wumingxian", shouzimu: "w" },
  { code: 4243, name: "武宁县", pinyin: "wuningxian", shouzimu: "w" },
  { code: 4055, name: "武平县", pinyin: "wupingxian", shouzimu: "w" },
  { code: 1486, name: "武强县", pinyin: "wuqiangxian", shouzimu: "w" },
  { code: 8254, name: "武山县", pinyin: "wushanxian", shouzimu: "w" },
  { code: 6741, name: "武胜县", pinyin: "wushengxian", shouzimu: "w" },
  { code: 8280, name: "武威市", pinyin: "wuweishi", shouzimu: "w" },
  { code: 1669, name: "武乡县", pinyin: "wuxiangxian", shouzimu: "w" },
  { code: 6154, name: "武宣县", pinyin: "wuxuanxian", shouzimu: "w" },
  { code: 5332, name: "武穴市", pinyin: "wuxueshi", shouzimu: "w" },
  { code: 4022, name: "武夷山市", pinyin: "wuyishanshi", shouzimu: "w" },
  { code: 3383, name: "武义县", pinyin: "wuyixian", shouzimu: "w" },
  { code: 1484, name: "武邑县", pinyin: "wuyixian", shouzimu: "w" },
  { code: 5013, name: "武陟县", pinyin: "wuzhixian", shouzimu: "w" },
  { code: 5044, name: "舞钢市", pinyin: "wugangshi", shouzimu: "w" },
  { code: 5041, name: "舞阳县", pinyin: "wuyangxian", shouzimu: "w" },
  { code: 7037, name: "务川县", pinyin: "wuchuanxian", shouzimu: "w" },
  { code: 4343, name: "婺源县", pinyin: "wuyuanxian", shouzimu: "w" }],

  X: [
  { code: 7910, name: "西安市", pinyin: "xianshi", shouzimu: "x" },
  { code: 6840, name: "西昌市", pinyin: "xichangshi", shouzimu: "x" },
  { code: 6742, name: "西充县", pinyin: "xichongxian", shouzimu: "x" },
  { code: 7453, name: "西畴县", pinyin: "xichouxian", shouzimu: "x" },
  { code: 2332, name: "西丰县", pinyin: "xifengxian", shouzimu: "x" },
  { code: 8316, name: "西和县", pinyin: "xihexian", shouzimu: "x" },
  { code: 5083, name: "西华县", pinyin: "xihuaxian", shouzimu: "x" },
  { code: 8743, name: "西吉县", pinyin: "xijixian", shouzimu: "x" },
  { code: 6273, name: "西林县", pinyin: "xilinxian", shouzimu: "x" },
  { code: 7481, name: "西盟县", pinyin: "ximengxian", shouzimu: "x" },
  { code: 8510, name: "西宁市", pinyin: "xiningshi", shouzimu: "x" },
  { code: 5115, name: "西平县", pinyin: "xipingxian", shouzimu: "x" },
  { code: 2017, name: "西乌珠穆沁旗", pinyin: "xiwuzhumuqinqi", shouzimu: "x" },
  { code: 5135, name: "西峡县", pinyin: "xixiaxian", shouzimu: "x" },
  { code: 7995, name: "西乡县", pinyin: "xixiangxian", shouzimu: "x" },
  { code: 1755, name: "昔阳县", pinyin: "xiyangxian", shouzimu: "x" },
  { code: 7010, name: "息烽县", pinyin: "xifengxian", shouzimu: "x" },
  { code: 5152, name: "息县", pinyin: "xixian", shouzimu: "x" },
  { code: 5337, name: "浠水县", pinyin: "xishuixian", shouzimu: "x" },
  { code: 5139, name: "淅川县", pinyin: "xichuanxian", shouzimu: "x" },
  { code: 2010, name: "锡林郭勒盟", pinyin: "xilinguolemeng", shouzimu: "x" },
  { code: 3711, name: "歙县", pinyin: "xixian", shouzimu: "x" },
  { code: 7044, name: "习水县", pinyin: "xishuixian", shouzimu: "x" },
  { code: 1787, name: "隰县", pinyin: "xixian", shouzimu: "x" },
  { code: 6853, name: "喜德县", pinyin: "xidexian", shouzimu: "x" },
  { code: 4355, name: "峡江县", pinyin: "xiajiangxian", shouzimu: "x" },
  { code: 4033, name: "霞浦县", pinyin: "xiapuxian", shouzimu: "x" },
  { code: 8387, name: "夏河县", pinyin: "xiahexian", shouzimu: "x" },
  { code: 4685, name: "夏津县", pinyin: "xiajinxian", shouzimu: "x" },
  { code: 1821, name: "夏县", pinyin: "xiaxian", shouzimu: "x" },
  { code: 5067, name: "夏邑县", pinyin: "xiayixian", shouzimu: "x" },
  { code: 3455, name: "仙居县", pinyin: "xianjuxian", shouzimu: "x" },
  { code: 5371, name: "仙桃市", pinyin: "xiantaoshi", shouzimu: "x" },
  { code: 3942, name: "仙游县", pinyin: "xianyouxian", shouzimu: "x" },
  { code: 5416, name: "咸丰县", pinyin: "xianfengxian", shouzimu: "x" },
  { code: 5360, name: "咸宁市", pinyin: "xianningshi", shouzimu: "x" },
  { code: 7950, name: "咸阳市", pinyin: "xianyangshi", shouzimu: "x" },
  { code: 1445, name: "献县", pinyin: "xianxian", shouzimu: "x" },
  { code: 6827, name: "乡城县", pinyin: "xiangchengxian", shouzimu: "x" },
  { code: 1783, name: "乡宁县", pinyin: "xiangningxian", shouzimu: "x" },
  { code: 7570, name: "香格里拉县", pinyin: "xianggelilaxian", shouzimu: "x" },
  { code: 1464, name: "香河县", pinyin: "xianghexian", shouzimu: "x" },
  { code: 5530, name: "湘潭市", pinyin: "xiangtanshi", shouzimu: "x" },
  { code: 5532, name: "湘乡市", pinyin: "xiangxiangshi", shouzimu: "x" },
  { code: 5574, name: "湘阴县", pinyin: "xiangyinxian", shouzimu: "x" },
  { code: 4955, name: "襄城县", pinyin: "xiangchengxian", shouzimu: "x" },
  { code: 5280, name: "襄樊市", pinyin: "xiangfanshi", shouzimu: "x" },
  { code: 1775, name: "襄汾县", pinyin: "xiangfenxian", shouzimu: "x" },
  { code: 1663, name: "襄垣县", pinyin: "xiangyuanxian", shouzimu: "x" },
  { code: 2019, name: "镶黄旗", pinyin: "xianghuangqi", shouzimu: "x" },
  { code: 7513, name: "祥云县", pinyin: "xiangyunxian", shouzimu: "x" },
  { code: 3111, name: "响水县", pinyin: "xiangshuixian", shouzimu: "x" },
  { code: 5091, name: "项城市", pinyin: "xiangchengshi", shouzimu: "x" },
  { code: 3321, name: "象山县", pinyin: "xiangshanxian", shouzimu: "x" },
  { code: 6153, name: "象州县", pinyin: "xiangzhouxian", shouzimu: "x" },
  { code: 3743, name: "萧县", pinyin: "xiaoxian", shouzimu: "x" },
  { code: 6797, name: "小金县", pinyin: "xiaojinxian", shouzimu: "x" },
  { code: 5351, name: "孝昌县", pinyin: "xiaochangxian", shouzimu: "x" },
  { code: 5350, name: "孝感市", pinyin: "xiaoganshi", shouzimu: "x" },
  { code: 1734, name: "孝义市", pinyin: "xiaoyishi", shouzimu: "x" },
  { code: 7767, name: "谢通门县", pinyin: "xietongmenxian", shouzimu: "x" },
  { code: 1210, name: "辛集市", pinyin: "xinjishi", shouzimu: "x" },
  { code: 6161, name: "忻城县", pinyin: "xinchengxian", shouzimu: "x" },
  { code: 1710, name: "忻州市", pinyin: "xinzhoushi", shouzimu: "x" },
  { code: 4933, name: "新安县", pinyin: "xinanxian", shouzimu: "x" },
  { code: 1972, name: "新巴尔虎右旗", pinyin: "xinbaerhuyouqi", shouzimu: "x" },
  { code: 1973, name: "新巴尔虎左旗", pinyin: "xinbaerhuzuoqi", shouzimu: "x" },
  { code: 2242, name: "新宾县", pinyin: "xinbinxian", shouzimu: "x" },
  { code: 5119, name: "新蔡县", pinyin: "xincaixian", shouzimu: "x" },
  { code: 3374, name: "新昌县", pinyin: "xinchangxian", shouzimu: "x" },
  { code: 6510, name: "新都区", pinyin: "xindouqu", shouzimu: "x" },
  { code: 5828, name: "新丰县", pinyin: "xinfengxian", shouzimu: "x" },
  { code: 4356, name: "新干县", pinyin: "xinganxian", shouzimu: "x" },
  { code: 8915, name: "新和县", pinyin: "xinhexian", shouzimu: "x" },
  { code: 1332, name: "新河县", pinyin: "xinhexian", shouzimu: "x" },
  { code: 5625, name: "新化县", pinyin: "xinhuaxian", shouzimu: "x" },
  { code: 5678, name: "新晃县", pinyin: "xinhuangxian", shouzimu: "x" },
  { code: 4210, name: "新建县", pinyin: "xinjianxian", shouzimu: "x" },
  { code: 1816, name: "新绛县", pinyin: "xinjiangxian", shouzimu: "x" },
  { code: 6510, name: "新津县", pinyin: "xinjinxian", shouzimu: "x" },
  { code: 1210, name: "新乐市", pinyin: "xinleshi", shouzimu: "x" },
  { code: 6819, name: "新龙县", pinyin: "xinlongxian", shouzimu: "x" },
  { code: 4910, name: "新密市", pinyin: "xinmishi", shouzimu: "x" },
  { code: 2210, name: "新民市", pinyin: "xinminshi", shouzimu: "x" },
  { code: 5558, name: "新宁县", pinyin: "xinningxian", shouzimu: "x" },
  { code: 7418, name: "新平县", pinyin: "xinpingxian", shouzimu: "x" },
  { code: 5552, name: "新邵县", pinyin: "xinshaoxian", shouzimu: "x" },
  { code: 4635, name: "新泰市", pinyin: "xintaishi", shouzimu: "x" },
  { code: 5659, name: "新田县", pinyin: "xintianxian", shouzimu: "x" },
  { code: 5161, name: "新县", pinyin: "xinxian", shouzimu: "x" },
  { code: 4980, name: "新乡市", pinyin: "xinxiangshi", shouzimu: "x" },
  { code: 4981, name: "新乡县", pinyin: "xinxiangxian", shouzimu: "x" },
  { code: 5938, name: "新兴县", pinyin: "xinxingxian", shouzimu: "x" },
  { code: 5143, name: "新野县", pinyin: "xinyexian", shouzimu: "x" },
  { code: 3036, name: "新沂市", pinyin: "xinyishi", shouzimu: "x" },
  { code: 4260, name: "新余市", pinyin: "xinyushi", shouzimu: "x" },
  { code: 8996, name: "新源县", pinyin: "xinyuanxian", shouzimu: "x" },
  { code: 4910, name: "新郑市", pinyin: "xinzhengshi", shouzimu: "x" },
  { code: 4284, name: "信丰县", pinyin: "xinfengxian", shouzimu: "x" },
  { code: 5150, name: "信阳市", pinyin: "xinyangshi", shouzimu: "x" },
  { code: 5921, name: "信宜市", pinyin: "xinyishi", shouzimu: "x" },
  { code: 4247, name: "星子县", pinyin: "xingzixian", shouzimu: "x" },
  { code: 1310, name: "邢台市", pinyin: "xingtaishi", shouzimu: "x" },
  { code: 1311, name: "邢台县", pinyin: "xingtaixian", shouzimu: "x" },
  { code: 1980, name: "兴安盟", pinyin: "xinganmeng", shouzimu: "x" },
  { code: 6183, name: "兴安县", pinyin: "xinganxian", shouzimu: "x" },
  { code: 2277, name: "兴城市", pinyin: "xingchengshi", shouzimu: "x" },
  { code: 4295, name: "兴国县", pinyin: "xingguoxian", shouzimu: "x" },
  { code: 8564, name: "兴海县", pinyin: "xinghaixian", shouzimu: "x" },
  { code: 2038, name: "兴和县", pinyin: "xinghexian", shouzimu: "x" },
  { code: 3131, name: "兴化市", pinyin: "xinghuashi", shouzimu: "x" },
  { code: 1422, name: "兴隆县", pinyin: "xinglongxian", shouzimu: "x" },
  { code: 5965, name: "兴宁市", pinyin: "xingningshi", shouzimu: "x" },
  { code: 7951, name: "兴平市", pinyin: "xingpingshi", shouzimu: "x" },
  { code: 7072, name: "兴仁县", pinyin: "xingrenxian", shouzimu: "x" },
  { code: 5266, name: "兴山县", pinyin: "xingshanxian", shouzimu: "x" },
  { code: 6719, name: "兴文县", pinyin: "xingwenxian", shouzimu: "x" },
  { code: 1735, name: "兴县", pinyin: "xingxian", shouzimu: "x" },
  { code: 6249, name: "兴业县", pinyin: "xingyexian", shouzimu: "x" },
  { code: 7070, name: "兴义市", pinyin: "xingyishi", shouzimu: "x" },
  { code: 1364, name: "雄县", pinyin: "xiongxian", shouzimu: "x" },
  { code: 3712, name: "休宁县", pinyin: "xiuningxian", shouzimu: "x" },
  { code: 4244, name: "修水县", pinyin: "xiushuixian", shouzimu: "x" },
  { code: 7010, name: "修文县", pinyin: "xiuwenxian", shouzimu: "x" },
  { code: 5011, name: "修武县", pinyin: "xiuwuxian", shouzimu: "x" },
  { code: 3090, name: "宿迁市", pinyin: "xiuqianshi", shouzimu: "x" },
  { code: 3686, name: "宿松县", pinyin: "xiusongxian", shouzimu: "x" },
  { code: 3094, name: "宿豫县", pinyin: "xiuyuxian", shouzimu: "x" },
  { code: 3740, name: "宿州市", pinyin: "xiuzhoushi", shouzimu: "x" },
  { code: 6872, name: "秀山县", pinyin: "xiushanxian", shouzimu: "x" },
  { code: 2265, name: "岫岩县", pinyin: "xiuyanxian", shouzimu: "x" },
  { code: 3088, name: "盱眙县", pinyin: "xuyixian", shouzimu: "x" },
  { code: 1354, name: "徐水县", pinyin: "xushuixian", shouzimu: "x" },
  { code: 5915, name: "徐闻县", pinyin: "xuwenxian", shouzimu: "x" },
  { code: 3030, name: "徐州市", pinyin: "xuzhoushi", shouzimu: "x" },
  { code: 5030, name: "许昌市", pinyin: "xuchangshi", shouzimu: "x" },
  { code: 5032, name: "许昌县", pinyin: "xuchangxian", shouzimu: "x" },
  { code: 6574, name: "叙永县", pinyin: "xuyongxian", shouzimu: "x" },
  { code: 5676, name: "溆浦县", pinyin: "xupuxian", shouzimu: "x" },
  { code: 3771, name: "宣城市", pinyin: "xuanchengshi", shouzimu: "x" },
  { code: 5415, name: "宣恩县", pinyin: "xuanenxian", shouzimu: "x" },
  { code: 6753, name: "宣汉县", pinyin: "xuanhanxian", shouzimu: "x" },
  { code: 1381, name: "宣化县", pinyin: "xuanhuaxian", shouzimu: "x" },
  { code: 7363, name: "宣威市", pinyin: "xuanweishi", shouzimu: "x" },
  { code: 8019, name: "旬阳县", pinyin: "xunyangxian", shouzimu: "x" },
  { code: 7959, name: "旬邑县", pinyin: "xunyixian", shouzimu: "x" },
  { code: 7310, name: "寻甸县", pinyin: "xundianxian", shouzimu: "x" },
  { code: 4298, name: "寻乌县", pinyin: "xunwuxian", shouzimu: "x" },
  { code: 8528, name: "循化撒拉族自治县", pinyin: "xunhuasalazuzizhixian", shouzimu: "x" },
  { code: 2786, name: "逊克县", pinyin: "xunkexian", shouzimu: "x" }],

  Y: [
  { code: 1964, name: "牙克石市", pinyin: "yakeshishi", shouzimu: "y" },
  { code: 6770, name: "雅安市", pinyin: "yaanshi", shouzimu: "y" },
  { code: 6815, name: "雅江县", pinyin: "yajiangxian", shouzimu: "y" },
  { code: 7773, name: "亚东县", pinyin: "yadongxian", shouzimu: "y" },
  { code: 4560, name: "烟台市", pinyin: "yantaishi", shouzimu: "y" },
  { code: 8886, name: "焉耆回族自治县", pinyin: "yanqihuizuzizhixian", shouzimu: "y" },
  { code: 5033, name: "鄢陵县", pinyin: "yanlingxian", shouzimu: "y" },
  { code: 8040, name: "延安市", pinyin: "yananshi", shouzimu: "y" },
  { code: 8043, name: "延川县", pinyin: "yanchuanxian", shouzimu: "y" },
  { code: 2490, name: "延吉市", pinyin: "yanjishi", shouzimu: "y" },
  { code: 4984, name: "延津县", pinyin: "yanjinxian", shouzimu: "y" },
  { code: 2610, name: "延寿县", pinyin: "yanshouxian", shouzimu: "y" },
  { code: 8042, name: "延长县", pinyin: "yanchangxian", shouzimu: "y" },
  { code: 5526, name: "炎陵县", pinyin: "yanlingxian", shouzimu: "y" },
  { code: 7058, name: "沿河县", pinyin: "yanhexian", shouzimu: "y" },
  { code: 6562, name: "盐边县", pinyin: "yanbianxian", shouzimu: "y" },
  { code: 3110, name: "盐城市", pinyin: "yanchengshi", shouzimu: "y" },
  { code: 8736, name: "盐池县", pinyin: "yanchixian", shouzimu: "y" },
  { code: 3118, name: "盐都县", pinyin: "yandouxian", shouzimu: "y" },
  { code: 7344, name: "盐津县", pinyin: "yanjinxian", shouzimu: "y" },
  { code: 1449, name: "盐山县", pinyin: "yanshanxian", shouzimu: "y" },
  { code: 6592, name: "盐亭县", pinyin: "yantingxian", shouzimu: "y" },
  { code: 6843, name: "盐源县", pinyin: "yanyuanxian", shouzimu: "y" },
  { code: 5043, name: "郾城县", pinyin: "yanchengxian", shouzimu: "y" },
  { code: 4931, name: "偃师市", pinyin: "yanshishi", shouzimu: "y" },
  { code: 7452, name: "砚山县", pinyin: "yanshanxian", shouzimu: "y" },
  { code: 3143, name: "扬中市", pinyin: "yangzhongshi", shouzimu: "y" },
  { code: 3120, name: "扬州市", pinyin: "yangzhoushi", shouzimu: "y" },
  { code: 1682, name: "阳城县", pinyin: "yangchengxian", shouzimu: "y" },
  { code: 5992, name: "阳春市", pinyin: "yangchunshi", shouzimu: "y" },
  { code: 5993, name: "阳东县", pinyin: "yangdongxian", shouzimu: "y" },
  { code: 1631, name: "阳高县", pinyin: "yanggaoxian", shouzimu: "y" },
  { code: 4713, name: "阳谷县", pinyin: "yangguxian", shouzimu: "y" },
  { code: 5990, name: "阳江市", pinyin: "yangjiangshi", shouzimu: "y" },
  { code: 1610, name: "阳曲县", pinyin: "yangquxian", shouzimu: "y" },
  { code: 1630, name: "阳泉市", pinyin: "yangquanshi", shouzimu: "y" },
  { code: 6013, name: "阳山县", pinyin: "yangshanxian", shouzimu: "y" },
  { code: 6171, name: "阳朔县", pinyin: "yangshuoxian", shouzimu: "y" },
  { code: 5991, name: "阳西县", pinyin: "yangxixian", shouzimu: "y" },
  { code: 5367, name: "阳新县", pinyin: "yangxinxian", shouzimu: "y" },
  { code: 4663, name: "阳信县", pinyin: "yangxinxian", shouzimu: "y" },
  { code: 1396, name: "阳原县", pinyin: "yangyuanxian", shouzimu: "y" },
  { code: 7994, name: "洋县", pinyin: "yangxian", shouzimu: "y" },
  { code: 7512, name: "漾濞县", pinyin: "yangbixian", shouzimu: "y" },
  { code: 7385, name: "姚安县", pinyin: "yaoanxian", shouzimu: "y" },
  { code: 7921, name: "耀县", pinyin: "yaoxian", shouzimu: "y" },
  { code: 8947, name: "叶城县", pinyin: "yechengxian", shouzimu: "y" },
  { code: 4952, name: "叶县", pinyin: "yexian", shouzimu: "y" },
  { code: 4939, name: "伊川县", pinyin: "yichuanxian", shouzimu: "y" },
  { code: 2710, name: "伊春市", pinyin: "yichunshi", shouzimu: "y" },
  { code: 2058, name: "伊金霍勒旗", pinyin: "yijinhuoleqi", shouzimu: "y" },
  { code: 8980, name: "伊宁市", pinyin: "yiningshi", shouzimu: "y" },
  { code: 8992, name: "伊宁县", pinyin: "yiningxian", shouzimu: "y" },
  { code: 2432, name: "伊通县", pinyin: "yitongxian", shouzimu: "y" },
  { code: 8843, name: "伊吾县", pinyin: "yiwuxian", shouzimu: "y" },
  { code: 2643, name: "依安县", pinyin: "yianxian", shouzimu: "y" },
  { code: 2610, name: "依兰县", pinyin: "yilanxian", shouzimu: "y" },
  { code: 3713, name: "黟县", pinyin: "yixian", shouzimu: "y" },
  { code: 6739, name: "仪陇县", pinyin: "yilongxian", shouzimu: "y" },
  { code: 3129, name: "仪征市", pinyin: "yizhengshi", shouzimu: "y" },
  { code: 4743, name: "沂南县", pinyin: "yinanxian", shouzimu: "y" },
  { code: 4737, name: "沂水县", pinyin: "yishuixian", shouzimu: "y" },
  { code: 4536, name: "沂源县", pinyin: "yiyuanxian", shouzimu: "y" },
  { code: 6710, name: "宜宾市", pinyin: "yibinshi", shouzimu: "y" },
  { code: 6712, name: "宜宾县", pinyin: "yibinxian", shouzimu: "y" },
  { code: 5260, name: "宜昌市", pinyin: "yichangshi", shouzimu: "y" },
  { code: 5263, name: "宜昌县", pinyin: "yichangxian", shouzimu: "y" },
  { code: 5282, name: "宜城县", pinyin: "yichengxian", shouzimu: "y" },
  { code: 8052, name: "宜川县", pinyin: "yichuanxian", shouzimu: "y" },
  { code: 4310, name: "宜春市", pinyin: "yichunshi", shouzimu: "y" },
  { code: 5261, name: "宜都市", pinyin: "yidoushi", shouzimu: "y" },
  { code: 4318, name: "宜丰县", pinyin: "yifengxian", shouzimu: "y" },
  { code: 4377, name: "宜黄县", pinyin: "yihuangxian", shouzimu: "y" },
  { code: 7922, name: "宜君县", pinyin: "yijunxian", shouzimu: "y" },
  { code: 7310, name: "宜良县", pinyin: "yiliangxian", shouzimu: "y" },
  { code: 3023, name: "宜兴市", pinyin: "yixingshi", shouzimu: "y" },
  { code: 4937, name: "宜阳县", pinyin: "yiyangxian", shouzimu: "y" },
  { code: 5636, name: "宜章县", pinyin: "yizhangxian", shouzimu: "y" },
  { code: 6282, name: "宜州市", pinyin: "yizhoushi", shouzimu: "y" },
  { code: 7349, name: "彝良县", pinyin: "yiliangxian", shouzimu: "y" },
  { code: 4337, name: "弋阳县", pinyin: "yiyangxian", shouzimu: "y" },
  { code: 5055, name: "义马市", pinyin: "yimashi", shouzimu: "y" },
  { code: 3387, name: "义乌市", pinyin: "yiwushi", shouzimu: "y" },
  { code: 2275, name: "义县", pinyin: "yixian", shouzimu: "y" },
  { code: 7416, name: "易门县", pinyin: "yimenxian", shouzimu: "y" },
  { code: 1353, name: "易县", pinyin: "yixian", shouzimu: "y" },
  { code: 5610, name: "益阳市", pinyin: "yiyangshi", shouzimu: "y" },
  { code: 1774, name: "翼城县", pinyin: "yichengxian", shouzimu: "y" },
  { code: 8710, name: "银川市", pinyin: "yinchuanshi", shouzimu: "y" },
  { code: 7056, name: "印江县", pinyin: "yinjiangxian", shouzimu: "y" },
  { code: 6012, name: "英德市", pinyin: "yingdeshi", shouzimu: "y" },
  { code: 8944, name: "英吉沙县", pinyin: "yingjishaxian", shouzimu: "y" },
  { code: 5336, name: "英山县", pinyin: "yingshanxian", shouzimu: "y" },
  { code: 4270, name: "鹰潭市", pinyin: "yingtanshi", shouzimu: "y" },
  { code: 6773, name: "荥经县", pinyin: "yingjingxian", shouzimu: "y" },
  { code: 4910, name: "荥阳市", pinyin: "yingyangshi", shouzimu: "y" },
  { code: 7544, name: "盈江县", pinyin: "yingjiangxian", shouzimu: "y" },
  { code: 2280, name: "营口市", pinyin: "yingkoushi", shouzimu: "y" },
  { code: 6736, name: "营山县", pinyin: "yingshanxian", shouzimu: "y" },
  { code: 3729, name: "颍上县", pinyin: "yingshangxian", shouzimu: "y" },
  { code: 5352, name: "应城市", pinyin: "yingchengshi", shouzimu: "y" },
  { code: 1636, name: "应县", pinyin: "yingxian", shouzimu: "y" },
  { code: 3960, name: "永安市", pinyin: "yonganshi", shouzimu: "y" },
  { code: 8231, name: "永昌县", pinyin: "yongchangxian", shouzimu: "y" },
  { code: 5069, name: "永城市", pinyin: "yongchengshi", shouzimu: "y" },
  { code: 6530, name: "永川市", pinyin: "yongchuanshi", shouzimu: "y" },
  { code: 3975, name: "永春县", pinyin: "yongchunxian", shouzimu: "y" },
  { code: 7584, name: "永德县", pinyin: "yongdexian", shouzimu: "y" },
  { code: 8210, name: "永登县", pinyin: "yongdengxian", shouzimu: "y" },
  { code: 4053, name: "永定县", pinyin: "yongdingxian", shouzimu: "y" },
  { code: 4357, name: "永丰县", pinyin: "yongfengxian", shouzimu: "y" },
  { code: 6184, name: "永福县", pinyin: "yongfuxian", shouzimu: "y" },
  { code: 1786, name: "永和县", pinyin: "yonghexian", shouzimu: "y" },
  { code: 2421, name: "永吉县", pinyin: "yongjixian", shouzimu: "y" },
  { code: 1812, name: "永济市", pinyin: "yongjishi", shouzimu: "y" },
  { code: 3334, name: "永嘉县", pinyin: "yongjiaxian", shouzimu: "y" },
  { code: 8364, name: "永靖县", pinyin: "yongjingxian", shouzimu: "y" },
  { code: 3382, name: "永康市", pinyin: "yongkangshi", shouzimu: "y" },
  { code: 1293, name: "永年县", pinyin: "yongnianxian", shouzimu: "y" },
  { code: 8710, name: "永宁县", pinyin: "yongningxian", shouzimu: "y" },
  { code: 7518, name: "永平县", pinyin: "yongpingxian", shouzimu: "y" },
  { code: 1463, name: "永清县", pinyin: "yongqingxian", shouzimu: "y" },
  { code: 7387, name: "永仁县", pinyin: "yongrenxian", shouzimu: "y" },
  { code: 7346, name: "永善县", pinyin: "yongshanxian", shouzimu: "y" },
  { code: 7552, name: "永胜县", pinyin: "yongshengxian", shouzimu: "y" },
  { code: 7956, name: "永寿县", pinyin: "yongshouxian", shouzimu: "y" },
  { code: 5697, name: "永顺县", pinyin: "yongshunxian", shouzimu: "y" },
  { code: 3910, name: "永泰县", pinyin: "yongtaixian", shouzimu: "y" },
  { code: 4363, name: "永新县", pinyin: "yongxinxian", shouzimu: "y" },
  { code: 5635, name: "永兴县", pinyin: "yongxingxian", shouzimu: "y" },
  { code: 4245, name: "永修县", pinyin: "yongxiuxian", shouzimu: "y" },
  { code: 5650, name: "永州市", pinyin: "yongzhoushi", shouzimu: "y" },
  { code: 5522, name: "攸县", pinyin: "youxian", shouzimu: "y" },
  { code: 3955, name: "尤溪县", pinyin: "youxixian", shouzimu: "y" },
  { code: 2728, name: "友谊县", pinyin: "youyixian", shouzimu: "y" },
  { code: 6874, name: "酉阳县", pinyin: "youyangxian", shouzimu: "y" },
  { code: 1638, name: "右玉县", pinyin: "youyuxian", shouzimu: "y" },
  { code: 4294, name: "于都县", pinyin: "yudouxian", shouzimu: "y" },
  { code: 8967, name: "于田县", pinyin: "yutianxian", shouzimu: "y" },
  { code: 4338, name: "余干县", pinyin: "yuganxian", shouzimu: "y" },
  { code: 4272, name: "余江县", pinyin: "yujiangxian", shouzimu: "y" },
  { code: 7041, name: "余庆县", pinyin: "yuqingxian", shouzimu: "y" },
  { code: 3324, name: "余姚市", pinyin: "yuyaoshi", shouzimu: "y" },
  { code: 1652, name: "盂县", pinyin: "yuxian", shouzimu: "y" },
  { code: 4614, name: "鱼台县", pinyin: "yutaixian", shouzimu: "y" },
  { code: 6530, name: "渝北区", pinyin: "yubeiqu", shouzimu: "y" },
  { code: 8060, name: "榆林市", pinyin: "yulinshi", shouzimu: "y" },
  { code: 1752, name: "榆社县", pinyin: "yushexian", shouzimu: "y" },
  { code: 2410, name: "榆树市", pinyin: "yushushi", shouzimu: "y" },
  { code: 8210, name: "榆中县", pinyin: "yuzhongxian", shouzimu: "y" },
  { code: 5062, name: "虞城县", pinyin: "yuchengxian", shouzimu: "y" },
  { code: 4688, name: "禹城市", pinyin: "yuchengshi", shouzimu: "y" },
  { code: 5034, name: "禹州市", pinyin: "yuzhoushi", shouzimu: "y" },
  { code: 3458, name: "玉环县", pinyin: "yuhuanxian", shouzimu: "y" },
  { code: 6240, name: "玉林市", pinyin: "yulinshi", shouzimu: "y" },
  { code: 8261, name: "玉门市", pinyin: "yumenshi", shouzimu: "y" },
  { code: 7053, name: "玉屏县", pinyin: "yupingxian", shouzimu: "y" },
  { code: 4334, name: "玉山县", pinyin: "yushanxian", shouzimu: "y" },
  { code: 8580, name: "玉树藏族自治州", pinyin: "yushuzangzuzizhizhou", shouzimu: "y" },
  { code: 8581, name: "玉树县", pinyin: "yushuxian", shouzimu: "y" },
  { code: 1249, name: "玉田县", pinyin: "yutianxian", shouzimu: "y" },
  { code: 7410, name: "玉溪市", pinyin: "yuxishi", shouzimu: "y" },
  { code: 5939, name: "郁南县", pinyin: "yunanxian", shouzimu: "y" },
  { code: 9016, name: "裕民县", pinyin: "yuminxian", shouzimu: "y" },
  { code: 7419, name: "元江县", pinyin: "yuanjiangxian", shouzimu: "y" },
  { code: 7388, name: "元谋县", pinyin: "yuanmouxian", shouzimu: "y" },
  { code: 1210, name: "元氏县", pinyin: "yuanshixian", shouzimu: "y" },
  { code: 7439, name: "元阳县", pinyin: "yuanyangxian", shouzimu: "y" },
  { code: 5612, name: "沅江市", pinyin: "yuanjiangshi", shouzimu: "y" },
  { code: 5674, name: "沅陵县", pinyin: "yuanlingxian", shouzimu: "y" },
  { code: 1824, name: "垣曲县", pinyin: "yuanquxian", shouzimu: "y" },
  { code: 1714, name: "原平市", pinyin: "yuanpingshi", shouzimu: "y" },
  { code: 4983, name: "原阳县", pinyin: "yuanyangxian", shouzimu: "y" },
  { code: 5265, name: "远安县", pinyin: "yuananxian", shouzimu: "y" },
  { code: 6735, name: "岳池县", pinyin: "yuechixian", shouzimu: "y" },
  { code: 8949, name: "岳普湖县", pinyin: "yuepuhuxian", shouzimu: "y" },
  { code: 3688, name: "岳西县", pinyin: "yuexixian", shouzimu: "y" },
  { code: 5570, name: "岳阳市", pinyin: "yueyangshi", shouzimu: "y" },
  { code: 5571, name: "岳阳县", pinyin: "yueyangxian", shouzimu: "y" },
  { code: 6855, name: "越西县", pinyin: "yuexixian", shouzimu: "y" },
  { code: 5937, name: "云浮市", pinyin: "yunfushi", shouzimu: "y" },
  { code: 3433, name: "云和县", pinyin: "yunhexian", shouzimu: "y" },
  { code: 7519, name: "云龙县", pinyin: "yunlongxian", shouzimu: "y" },
  { code: 5356, name: "云梦县", pinyin: "yunmengxian", shouzimu: "y" },
  { code: 7583, name: "云县", pinyin: "yunxian", shouzimu: "y" },
  { code: 3992, name: "云霄县", pinyin: "yunxiaoxian", shouzimu: "y" },
  { code: 6676, name: "云阳县", pinyin: "yunyangxian", shouzimu: "y" },
  { code: 5393, name: "郧西县", pinyin: "yunxixian", shouzimu: "y" },
  { code: 5392, name: "郧县", pinyin: "yunxian", shouzimu: "y" },
  { code: 1810, name: "运城市", pinyin: "yunchengshi", shouzimu: "y" },
  { code: 4758, name: "郓城县", pinyin: "yunchengxian", shouzimu: "y" },
  { code: 8582, name: "杂多县", pinyin: "zaduoxian", shouzimu: "z" },
  { code: 1210, name: "赞皇县", pinyin: "zanhuangxian", shouzimu: "z" },
  { code: 1483, name: "枣强县", pinyin: "zaoqiangxian", shouzimu: "z" },
  { code: 5288, name: "枣阳市", pinyin: "zaoyangshi", shouzimu: "z" },
  { code: 4540, name: "枣庄市", pinyin: "zaozhuangshi", shouzimu: "z" },
  { code: 8553, name: "泽库县", pinyin: "zekuxian", shouzimu: "z" },
  { code: 8945, name: "泽普县", pinyin: "zepuxian", shouzimu: "z" },
  { code: 5810, name: "增城市", pinyin: "zengchengshi", shouzimu: "z" },
  { code: 1984, name: "扎赉特旗", pinyin: "zhalaiteqi", shouzimu: "z" },
  { code: 1963, name: "扎兰屯市", pinyin: "zhalantunshi", shouzimu: "z" },
  { code: 1998, name: "扎鲁特旗", pinyin: "zhaluteqi", shouzimu: "z" },
  { code: 7741, name: "扎囊县", pinyin: "zhanangxian", shouzimu: "z" },
  { code: 7812, name: "札达县", pinyin: "zhadaxian", shouzimu: "z" },
  { code: 8037, name: "柞水县", pinyin: "zhashuixian", shouzimu: "z" },
  { code: 4665, name: "沾化县", pinyin: "zhanhuaxian", shouzimu: "z" },
  { code: 7371, name: "沾益县", pinyin: "zhanyixian", shouzimu: "z" },
  { code: 5910, name: "湛江市", pinyin: "zhanjiangshi", shouzimu: "z" },
  { code: 1391, name: "张北县", pinyin: "zhangbeixian", shouzimu: "z" },
  { code: 8255, name: "张家川县", pinyin: "zhangjiachuanxian", shouzimu: "z" },
  { code: 3056, name: "张家港市", pinyin: "zhangjiagangshi", shouzimu: "z" },
  { code: 5590, name: "张家界市", pinyin: "zhangjiajieshi", shouzimu: "z" },
  { code: 1380, name: "张家口市", pinyin: "zhangjiakoushi", shouzimu: "z" },
  { code: 8270, name: "张掖市", pinyin: "zhangyeshi", shouzimu: "z" },
  { code: 4510, name: "章丘市", pinyin: "zhangqiushi", shouzimu: "z" },
  { code: 2292, name: "彰武县", pinyin: "zhangwuxian", shouzimu: "z" },
  { code: 4056, name: "漳平市", pinyin: "zhangpingshi", shouzimu: "z" },
  { code: 3993, name: "漳浦县", pinyin: "zhangpuxian", shouzimu: "z" },
  { code: 8296, name: "漳县", pinyin: "zhangxian", shouzimu: "z" },
  { code: 3990, name: "漳州市", pinyin: "zhangzhoushi", shouzimu: "z" },
  { code: 7711, name: "樟木口岸镇", pinyin: "zhangmukouanzhen", shouzimu: "z" },
  { code: 4313, name: "樟树市", pinyin: "zhangshushi", shouzimu: "z" }],

  Z: [
  { code: 4562, name: "招远市", pinyin: "zhaoyuanshi", shouzimu: "z" },
  { code: 6852, name: "昭觉县", pinyin: "zhaojiaoxian", shouzimu: "z" },
  { code: 6223, name: "昭平县", pinyin: "zhaopingxian", shouzimu: "z" },
  { code: 8997, name: "昭苏县", pinyin: "zhaosuxian", shouzimu: "z" },
  { code: 7340, name: "昭通市", pinyin: "zhaotongshi", shouzimu: "z" },
  { code: 3994, name: "诏安县", pinyin: "zhaoanxian", shouzimu: "z" },
  { code: 1210, name: "赵县", pinyin: "zhaoxian", shouzimu: "z" },
  { code: 2763, name: "肇东市", pinyin: "zhaodongshi", shouzimu: "z" },
  { code: 5930, name: "肇庆市", pinyin: "zhaoqingshi", shouzimu: "z" },
  { code: 2768, name: "肇源县", pinyin: "zhaoyuanxian", shouzimu: "z" },
  { code: 2769, name: "肇州县", pinyin: "zhaozhouxian", shouzimu: "z" },
  { code: 5068, name: "柘城县", pinyin: "zhechengxian", shouzimu: "z" },
  { code: 4039, name: "柘荣县", pinyin: "zherongxian", shouzimu: "z" },
  { code: 7075, name: "贞丰县", pinyin: "zhenfengxian", shouzimu: "z" },
  { code: 8036, name: "镇安县", pinyin: "zhenanxian", shouzimu: "z" },
  { code: 7999, name: "镇巴县", pinyin: "zhenbaxian", shouzimu: "z" },
  { code: 3140, name: "镇江市", pinyin: "zhenjiangshi", shouzimu: "z" },
  { code: 7585, name: "镇康县", pinyin: "zhenkangxian", shouzimu: "z" },
  { code: 2477, name: "镇赉县", pinyin: "zhenlaixian", shouzimu: "z" },
  { code: 7121, name: "镇宁县", pinyin: "zhenningxian", shouzimu: "z" },
  { code: 5137, name: "镇平县", pinyin: "zhenpingxian", shouzimu: "z" },
  { code: 8018, name: "镇坪县", pinyin: "zhenpingxian", shouzimu: "z" },
  { code: 7348, name: "镇雄县", pinyin: "zhenxiongxian", shouzimu: "z" },
  { code: 7476, name: "镇沅县", pinyin: "zhenyuanxian", shouzimu: "z" },
  { code: 8348, name: "镇原县", pinyin: "zhenyuanxian", shouzimu: "z" },
  { code: 7135, name: "镇远县", pinyin: "zhenyuanxian", shouzimu: "z" },
  { code: 7035, name: "正安县", pinyin: "zhenganxian", shouzimu: "z" },
  { code: 1210, name: "正定县", pinyin: "zhengdingxian", shouzimu: "z" },
  { code: 2022, name: "正蓝旗", pinyin: "zhenglanqi", shouzimu: "z" },
  { code: 8346, name: "正宁县", pinyin: "zhengningxian", shouzimu: "z" },
  { code: 2021, name: "正镶白旗", pinyin: "zhengxiangbaiqi", shouzimu: "z" },
  { code: 5121, name: "正阳县", pinyin: "zhengyangxian", shouzimu: "z" },
  { code: 4910, name: "郑州市", pinyin: "zhengzhoushi", shouzimu: "z" },
  { code: 4021, name: "政和县", pinyin: "zhenghexian", shouzimu: "z" },
  { code: 5264, name: "枝江市", pinyin: "zhijiangshi", shouzimu: "z" },
  { code: 7095, name: "织金县", pinyin: "zhijinxian", shouzimu: "z" },
  { code: 5679, name: "芷江县", pinyin: "zhijiangxian", shouzimu: "z" },
  { code: 8046, name: "志丹县", pinyin: "zhidanxian", shouzimu: "z" },
  { code: 8584, name: "治多县", pinyin: "zhiduoxian", shouzimu: "z" },
  { code: 6582, name: "中江县", pinyin: "zhongjiangxian", shouzimu: "z" },
  { code: 4910, name: "中牟县", pinyin: "zhongmouxian", shouzimu: "z" },
  { code: 8734, name: "中宁县", pinyin: "zhongningxian", shouzimu: "z" },
  { code: 6030, name: "中山市", pinyin: "zhongshanshi", shouzimu: "z" },
  { code: 8733, name: "中卫市", pinyin: "zhongweishi", shouzimu: "z" },
  { code: 1743, name: "中阳县", pinyin: "zhongyangxian", shouzimu: "z" },
  { code: 6674, name: "忠县", pinyin: "zhongxian", shouzimu: "z" },
  { code: 6226, name: "钟山县", pinyin: "zhongshanxian", shouzimu: "z" },
  { code: 5381, name: "钟祥市", pinyin: "zhongxiangshi", shouzimu: "z" },
  { code: 7772, name: "仲巴县", pinyin: "zhongbaxian", shouzimu: "z" },
  { code: 6530, name: "重庆市", pinyin: "zhongqingshi", shouzimu: "z" },
  { code: 8383, name: "舟曲县", pinyin: "zhouquxian", shouzimu: "z" },
  { code: 3420, name: "舟山市", pinyin: "zhoushanshi", shouzimu: "z" },
  { code: 5080, name: "周口市", pinyin: "zhoukoushi", shouzimu: "z" },
  { code: 4038, name: "周宁县", pinyin: "zhouningxian", shouzimu: "z" },
  { code: 7913, name: "周至县", pinyin: "zhouzhixian", shouzimu: "z" },
  { code: 5850, name: "珠海市", pinyin: "zhuhaishi", shouzimu: "z" },
  { code: 5520, name: "株州市", pinyin: "zhuzhoushi", shouzimu: "z" },
  { code: 5521, name: "株洲县", pinyin: "zhuzhouxian", shouzimu: "z" },
  { code: 4589, name: "诸城市", pinyin: "zhuchengshi", shouzimu: "z" },
  { code: 3375, name: "诸暨市", pinyin: "zhujishi", shouzimu: "z" },
  { code: 5394, name: "竹山县", pinyin: "zhushanxian", shouzimu: "z" },
  { code: 5395, name: "竹溪县", pinyin: "zhuxixian", shouzimu: "z" },
  { code: 5110, name: "驻马店市", pinyin: "zhumadianshi", shouzimu: "z" },
  { code: 2223, name: "庄河市", pinyin: "zhuangheshi", shouzimu: "z" },
  { code: 8336, name: "庄浪县", pinyin: "zhuanglangxian", shouzimu: "z" },
  { code: 2053, name: "准格尔旗", pinyin: "zhungeerqi", shouzimu: "z" },
  { code: 1401, name: "涿鹿县", pinyin: "zhuoluxian", shouzimu: "z" },
  { code: 1352, name: "涿州市", pinyin: "zhuozhoushi", shouzimu: "z" },
  { code: 8382, name: "卓尼县", pinyin: "zhuonixian", shouzimu: "z" },
  { code: 2035, name: "卓资县", pinyin: "zhuozixian", shouzimu: "z" },
  { code: 4379, name: "资溪县", pinyin: "zixixian", shouzimu: "z" },
  { code: 5632, name: "资兴市", pinyin: "zixingshi", shouzimu: "z" },
  { code: 6636, name: "资阳市", pinyin: "ziyangshi", shouzimu: "z" },
  { code: 6187, name: "资源县", pinyin: "ziyuanxian", shouzimu: "z" },
  { code: 6635, name: "资中县", pinyin: "zizhongxian", shouzimu: "z" },
  { code: 4530, name: "淄博市", pinyin: "ziboshi", shouzimu: "z" },
  { code: 8044, name: "子长县", pinyin: "zichangxian", shouzimu: "z" },
  { code: 8073, name: "子洲县", pinyin: "zizhouxian", shouzimu: "z" },
  { code: 5267, name: "秭归县", pinyin: "ziguixian", shouzimu: "z" },
  { code: 6594, name: "梓潼县", pinyin: "zitongxian", shouzimu: "z" },
  { code: 5981, name: "紫金县", pinyin: "zijinxian", shouzimu: "z" },
  { code: 8015, name: "紫阳县", pinyin: "ziyangxian", shouzimu: "z" },
  { code: 7122, name: "紫云县", pinyin: "ziyunxian", shouzimu: "z" },
  { code: 6550, name: "自贡市", pinyin: "zigongshi", shouzimu: "z" },
  { code: 4612, name: "邹城市", pinyin: "zouchengshi", shouzimu: "z" },
  { code: 4667, name: "邹平县", pinyin: "zoupingxian", shouzimu: "z" },
  { code: 1248, name: "遵化市", pinyin: "zunhuashi", shouzimu: "z" },
  { code: 7030, name: "遵义市", pinyin: "zunyishi", shouzimu: "z" },
  { code: 7032, name: "遵义县", pinyin: "zunyixian", shouzimu: "z" },
  { code: 7727, name: "左贡县", pinyin: "zuogongxian", shouzimu: "z" },
  { code: 1753, name: "左权县", pinyin: "zuoquanxian", shouzimu: "z" },
  { code: 1637, name: "左云县", pinyin: "zuoyunxian", shouzimu: "z" }] };


var AZ = ['A', 'B', 'C', 'D', 'E', 'F', 'G', 'H', 'I', 'J', 'K', 'L', 'M', 'N', 'P', 'Q', 'R', 'S', 'T', 'W', 'X', 'Y', 'Z'];
module.exports = {
  cityData: cityData,
  AZ: AZ };

/***/ }),

/***/ "../../../workspace/vue-ele/main.js":
/*!************************************!*\
  !*** D:/workspace/vue-ele/main.js ***!
  \************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(createApp) {__webpack_require__(/*! uni-pages */ "../../../workspace/vue-ele/pages.json");
var _vue = _interopRequireDefault(__webpack_require__(/*! vue */ "./node_modules/@dcloudio/vue-cli-plugin-uni/packages/mp-vue/dist/mp.runtime.esm.js"));
var _App = _interopRequireDefault(__webpack_require__(/*! ./App */ "../../../workspace/vue-ele/App.vue"));function _interopRequireDefault(obj) {return obj && obj.__esModule ? obj : { default: obj };}function _objectSpread(target) {for (var i = 1; i < arguments.length; i++) {var source = arguments[i] != null ? arguments[i] : {};var ownKeys = Object.keys(source);if (typeof Object.getOwnPropertySymbols === 'function') {ownKeys = ownKeys.concat(Object.getOwnPropertySymbols(source).filter(function (sym) {return Object.getOwnPropertyDescriptor(source, sym).enumerable;}));}ownKeys.forEach(function (key) {_defineProperty(target, key, source[key]);});}return target;}function _defineProperty(obj, key, value) {if (key in obj) {Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true });} else {obj[key] = value;}return obj;}var cmdNavBar = function cmdNavBar() {return __webpack_require__.e(/*! import() | components/cmd-nav-bar/cmd-nav-bar */ "components/cmd-nav-bar/cmd-nav-bar").then(__webpack_require__.bind(null, /*! @/components/cmd-nav-bar/cmd-nav-bar.vue */ "../../../workspace/vue-ele/components/cmd-nav-bar/cmd-nav-bar.vue"));};


_vue.default.component('cmd-nav-bar', cmdNavBar);

_vue.default.config.productionTip = false;

_App.default.mpType = 'app';

var app = new _vue.default(_objectSpread({},
_App.default));

createApp(app).$mount();
/* WEBPACK VAR INJECTION */}.call(this, __webpack_require__(/*! ./node_modules/@dcloudio/uni-mp-weixin/dist/index.js */ "./node_modules/@dcloudio/uni-mp-weixin/dist/index.js")["createApp"]))

/***/ }),

/***/ "../../../workspace/vue-ele/main.js?{\"page\":\"pages%2Faddress%2Faddress\"}":
/*!*************************************************************************!*\
  !*** D:/workspace/vue-ele/main.js?{"page":"pages%2Faddress%2Faddress"} ***!
  \*************************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(createPage) {__webpack_require__(/*! uni-pages */ "../../../workspace/vue-ele/pages.json");

var _vue = _interopRequireDefault(__webpack_require__(/*! vue */ "./node_modules/@dcloudio/vue-cli-plugin-uni/packages/mp-vue/dist/mp.runtime.esm.js"));
var _address = _interopRequireDefault(__webpack_require__(/*! ./pages/address/address.vue */ "../../../workspace/vue-ele/pages/address/address.vue"));function _interopRequireDefault(obj) {return obj && obj.__esModule ? obj : { default: obj };}
createPage(_address.default);
/* WEBPACK VAR INJECTION */}.call(this, __webpack_require__(/*! ./node_modules/@dcloudio/uni-mp-weixin/dist/index.js */ "./node_modules/@dcloudio/uni-mp-weixin/dist/index.js")["createPage"]))

/***/ }),

/***/ "../../../workspace/vue-ele/main.js?{\"page\":\"pages%2Fcity%2Fcity\"}":
/*!*******************************************************************!*\
  !*** D:/workspace/vue-ele/main.js?{"page":"pages%2Fcity%2Fcity"} ***!
  \*******************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(createPage) {__webpack_require__(/*! uni-pages */ "../../../workspace/vue-ele/pages.json");

var _vue = _interopRequireDefault(__webpack_require__(/*! vue */ "./node_modules/@dcloudio/vue-cli-plugin-uni/packages/mp-vue/dist/mp.runtime.esm.js"));
var _city = _interopRequireDefault(__webpack_require__(/*! ./pages/city/city.vue */ "../../../workspace/vue-ele/pages/city/city.vue"));function _interopRequireDefault(obj) {return obj && obj.__esModule ? obj : { default: obj };}
createPage(_city.default);
/* WEBPACK VAR INJECTION */}.call(this, __webpack_require__(/*! ./node_modules/@dcloudio/uni-mp-weixin/dist/index.js */ "./node_modules/@dcloudio/uni-mp-weixin/dist/index.js")["createPage"]))

/***/ }),

/***/ "../../../workspace/vue-ele/main.js?{\"page\":\"pages%2Findex%2Findex\"}":
/*!*********************************************************************!*\
  !*** D:/workspace/vue-ele/main.js?{"page":"pages%2Findex%2Findex"} ***!
  \*********************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(createPage) {__webpack_require__(/*! uni-pages */ "../../../workspace/vue-ele/pages.json");

var _vue = _interopRequireDefault(__webpack_require__(/*! vue */ "./node_modules/@dcloudio/vue-cli-plugin-uni/packages/mp-vue/dist/mp.runtime.esm.js"));
var _index = _interopRequireDefault(__webpack_require__(/*! ./pages/index/index.vue */ "../../../workspace/vue-ele/pages/index/index.vue"));function _interopRequireDefault(obj) {return obj && obj.__esModule ? obj : { default: obj };}
createPage(_index.default);
/* WEBPACK VAR INJECTION */}.call(this, __webpack_require__(/*! ./node_modules/@dcloudio/uni-mp-weixin/dist/index.js */ "./node_modules/@dcloudio/uni-mp-weixin/dist/index.js")["createPage"]))

/***/ }),

/***/ "../../../workspace/vue-ele/main.js?{\"page\":\"pages%2Fmy%2Fmy\"}":
/*!***************************************************************!*\
  !*** D:/workspace/vue-ele/main.js?{"page":"pages%2Fmy%2Fmy"} ***!
  \***************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(createPage) {__webpack_require__(/*! uni-pages */ "../../../workspace/vue-ele/pages.json");

var _vue = _interopRequireDefault(__webpack_require__(/*! vue */ "./node_modules/@dcloudio/vue-cli-plugin-uni/packages/mp-vue/dist/mp.runtime.esm.js"));
var _my = _interopRequireDefault(__webpack_require__(/*! ./pages/my/my.vue */ "../../../workspace/vue-ele/pages/my/my.vue"));function _interopRequireDefault(obj) {return obj && obj.__esModule ? obj : { default: obj };}
createPage(_my.default);
/* WEBPACK VAR INJECTION */}.call(this, __webpack_require__(/*! ./node_modules/@dcloudio/uni-mp-weixin/dist/index.js */ "./node_modules/@dcloudio/uni-mp-weixin/dist/index.js")["createPage"]))

/***/ }),

/***/ "../../../workspace/vue-ele/main.js?{\"page\":\"pages%2Forder%2Forder\"}":
/*!*********************************************************************!*\
  !*** D:/workspace/vue-ele/main.js?{"page":"pages%2Forder%2Forder"} ***!
  \*********************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(createPage) {__webpack_require__(/*! uni-pages */ "../../../workspace/vue-ele/pages.json");

var _vue = _interopRequireDefault(__webpack_require__(/*! vue */ "./node_modules/@dcloudio/vue-cli-plugin-uni/packages/mp-vue/dist/mp.runtime.esm.js"));
var _order = _interopRequireDefault(__webpack_require__(/*! ./pages/order/order.vue */ "../../../workspace/vue-ele/pages/order/order.vue"));function _interopRequireDefault(obj) {return obj && obj.__esModule ? obj : { default: obj };}
createPage(_order.default);
/* WEBPACK VAR INJECTION */}.call(this, __webpack_require__(/*! ./node_modules/@dcloudio/uni-mp-weixin/dist/index.js */ "./node_modules/@dcloudio/uni-mp-weixin/dist/index.js")["createPage"]))

/***/ }),

/***/ "../../../workspace/vue-ele/main.js?{\"page\":\"pages%2Fsearch%2Fsearch\"}":
/*!***********************************************************************!*\
  !*** D:/workspace/vue-ele/main.js?{"page":"pages%2Fsearch%2Fsearch"} ***!
  \***********************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(createPage) {__webpack_require__(/*! uni-pages */ "../../../workspace/vue-ele/pages.json");

var _vue = _interopRequireDefault(__webpack_require__(/*! vue */ "./node_modules/@dcloudio/vue-cli-plugin-uni/packages/mp-vue/dist/mp.runtime.esm.js"));
var _search = _interopRequireDefault(__webpack_require__(/*! ./pages/search/search.vue */ "../../../workspace/vue-ele/pages/search/search.vue"));function _interopRequireDefault(obj) {return obj && obj.__esModule ? obj : { default: obj };}
createPage(_search.default);
/* WEBPACK VAR INJECTION */}.call(this, __webpack_require__(/*! ./node_modules/@dcloudio/uni-mp-weixin/dist/index.js */ "./node_modules/@dcloudio/uni-mp-weixin/dist/index.js")["createPage"]))

/***/ }),

/***/ "../../../workspace/vue-ele/pages.json":
/*!***************************************!*\
  !*** D:/workspace/vue-ele/pages.json ***!
  \***************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


/***/ }),

/***/ "./node_modules/@dcloudio/uni-mp-weixin/dist/index.js":
/*!************************************************************!*\
  !*** ./node_modules/@dcloudio/uni-mp-weixin/dist/index.js ***!
  \************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
Object.defineProperty(exports, "__esModule", { value: true });exports.createApp = createApp;exports.createPage = createPage;exports.createComponent = createComponent;exports.default = void 0;var _vue = _interopRequireDefault(__webpack_require__(/*! vue */ "./node_modules/@dcloudio/vue-cli-plugin-uni/packages/mp-vue/dist/mp.runtime.esm.js"));function _interopRequireDefault(obj) {return obj && obj.__esModule ? obj : { default: obj };}function _defineProperty(obj, key, value) {if (key in obj) {Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true });} else {obj[key] = value;}return obj;}

var _toString = Object.prototype.toString;
var hasOwnProperty = Object.prototype.hasOwnProperty;

function isFn(fn) {
  return typeof fn === 'function';
}

function isStr(str) {
  return typeof str === 'string';
}

function isPlainObject(obj) {
  return _toString.call(obj) === '[object Object]';
}

function hasOwn(obj, key) {
  return hasOwnProperty.call(obj, key);
}

function noop() {}

var SYNC_API_RE = /requireNativePlugin|upx2px|hideKeyboard|canIUse|^create|Sync$|Manager$/;

var CONTEXT_API_RE = /^create|Manager$/;

var CALLBACK_API_RE = /^on/;

function isContextApi(name) {
  return CONTEXT_API_RE.test(name);
}
function isSyncApi(name) {
  return SYNC_API_RE.test(name);
}

function isCallbackApi(name) {
  return CALLBACK_API_RE.test(name);
}

function handlePromise(promise) {
  return promise.then(function (data) {
    return [null, data];
  }).
  catch(function (err) {return [err];});
}

function shouldPromise(name) {
  if (isSyncApi(name)) {
    return false;
  }
  if (isCallbackApi(name)) {
    return false;
  }
  return true;
}

function promisify(name, api) {
  if (!shouldPromise(name)) {
    return api;
  }
  return function promiseApi() {var options = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};for (var _len = arguments.length, params = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {params[_key - 1] = arguments[_key];}
    if (isFn(options.success) || isFn(options.fail) || isFn(options.complete)) {
      return api.apply(void 0, [options].concat(params));
    }
    return handlePromise(new Promise(function (resolve, reject) {
      api.apply(void 0, [Object.assign({}, options, {
        success: resolve,
        fail: reject })].concat(
      params));
      /* eslint-disable no-extend-native */
      Promise.prototype.finally = function (callback) {
        var promise = this.constructor;
        return this.then(
        function (value) {return promise.resolve(callback()).then(function () {return value;});},
        function (reason) {return promise.resolve(callback()).then(function () {
            throw reason;
          });});

      };
    }));
  };
}

var EPS = 1e-4;
var BASE_DEVICE_WIDTH = 750;
var isIOS = false;
var deviceWidth = 0;
var deviceDPR = 0;

function checkDeviceWidth() {var _wx$getSystemInfoSync =




  wx.getSystemInfoSync(),platform = _wx$getSystemInfoSync.platform,pixelRatio = _wx$getSystemInfoSync.pixelRatio,windowWidth = _wx$getSystemInfoSync.windowWidth; // uni=>wx runtime 编译目标是 uni 对象，内部不允许直接使用 uni

  deviceWidth = windowWidth;
  deviceDPR = pixelRatio;
  isIOS = platform === 'ios';
}

function upx2px(number, newDeviceWidth) {
  if (deviceWidth === 0) {
    checkDeviceWidth();
  }

  number = Number(number);
  if (number === 0) {
    return 0;
  }
  var result = number / BASE_DEVICE_WIDTH * (newDeviceWidth || deviceWidth);
  if (result < 0) {
    result = -result;
  }
  result = Math.floor(result + EPS);
  if (result === 0) {
    if (deviceDPR === 1 || !isIOS) {
      return 1;
    } else {
      return 0.5;
    }
  }
  return number < 0 ? -result : result;
}

var protocols = {};

var CALLBACKS = ['success', 'fail', 'cancel', 'complete'];

function processCallback(methodName, method, returnValue) {
  return function (res) {
    return method(processReturnValue(methodName, res, returnValue));
  };
}

function processArgs(methodName, fromArgs) {var argsOption = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};var returnValue = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : {};var keepFromArgs = arguments.length > 4 && arguments[4] !== undefined ? arguments[4] : false;
  if (isPlainObject(fromArgs)) {// 一般 api 的参数解析
    var toArgs = keepFromArgs === true ? fromArgs : {}; // returnValue 为 false 时，说明是格式化返回值，直接在返回值对象上修改赋值
    if (isFn(argsOption)) {
      argsOption = argsOption(fromArgs, toArgs) || {};
    }
    for (var key in fromArgs) {
      if (hasOwn(argsOption, key)) {
        var keyOption = argsOption[key];
        if (isFn(keyOption)) {
          keyOption = keyOption(fromArgs[key], fromArgs, toArgs);
        }
        if (!keyOption) {// 不支持的参数
          console.warn("\u5FAE\u4FE1\u5C0F\u7A0B\u5E8F ".concat(methodName, "\u6682\u4E0D\u652F\u6301").concat(key));
        } else if (isStr(keyOption)) {// 重写参数 key
          toArgs[keyOption] = fromArgs[key];
        } else if (isPlainObject(keyOption)) {// {name:newName,value:value}可重新指定参数 key:value
          toArgs[keyOption.name ? keyOption.name : key] = keyOption.value;
        }
      } else if (CALLBACKS.includes(key)) {
        toArgs[key] = processCallback(methodName, fromArgs[key], returnValue);
      } else {
        if (!keepFromArgs) {
          toArgs[key] = fromArgs[key];
        }
      }
    }
    return toArgs;
  } else if (isFn(fromArgs)) {
    fromArgs = processCallback(methodName, fromArgs, returnValue);
  }
  return fromArgs;
}

function processReturnValue(methodName, res, returnValue) {var keepReturnValue = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : false;
  if (isFn(protocols.returnValue)) {// 处理通用 returnValue
    res = protocols.returnValue(methodName, res);
  }
  return processArgs(methodName, res, returnValue, {}, keepReturnValue);
}

function wrapper(methodName, method) {
  if (hasOwn(protocols, methodName)) {
    var protocol = protocols[methodName];
    if (!protocol) {// 暂不支持的 api
      return function () {
        console.error("\u5FAE\u4FE1\u5C0F\u7A0B\u5E8F \u6682\u4E0D\u652F\u6301".concat(methodName));
      };
    }
    return function (arg1, arg2) {// 目前 api 最多两个参数
      var options = protocol;
      if (isFn(protocol)) {
        options = protocol(arg1);
      }

      arg1 = processArgs(methodName, arg1, options.args, options.returnValue);

      var returnValue = wx[options.name || methodName](arg1, arg2);
      if (isSyncApi(methodName)) {// 同步 api
        return processReturnValue(methodName, returnValue, options.returnValue, isContextApi(methodName));
      }
      return returnValue;
    };
  }
  return method;
}

var todoApis = Object.create(null);

var TODOS = [
'subscribePush',
'unsubscribePush',
'onPush',
'offPush',
'share'];


function createTodoApi(name) {
  return function todoApi(_ref)


  {var fail = _ref.fail,complete = _ref.complete;
    var res = {
      errMsg: "".concat(name, ":fail:\u6682\u4E0D\u652F\u6301 ").concat(name, " \u65B9\u6CD5") };

    isFn(fail) && fail(res);
    isFn(complete) && complete(res);
  };
}

TODOS.forEach(function (name) {
  todoApis[name] = createTodoApi(name);
});

var providers = {
  oauth: ['weixin'],
  share: ['weixin'],
  payment: ['wxpay'],
  push: ['weixin'] };


function getProvider(_ref2)




{var service = _ref2.service,success = _ref2.success,fail = _ref2.fail,complete = _ref2.complete;
  var res = false;
  if (providers[service]) {
    res = {
      errMsg: 'getProvider:ok',
      service: service,
      provider: providers[service] };

    isFn(success) && success(res);
  } else {
    res = {
      errMsg: 'getProvider:fail:服务[' + service + ']不存在' };

    isFn(fail) && fail(res);
  }
  isFn(complete) && complete(res);
}

var extraApi = /*#__PURE__*/Object.freeze({
  getProvider: getProvider });




var api = /*#__PURE__*/Object.freeze({});



var MOCKS = ['__route__', '__wxExparserNodeId__', '__wxWebviewId__'];

function initMocks(vm) {
  var mpInstance = vm.$mp[vm.mpType];
  MOCKS.forEach(function (mock) {
    if (hasOwn(mpInstance, mock)) {
      vm[mock] = mpInstance[mock];
    }
  });
}

function initHooks(mpOptions, hooks) {
  hooks.forEach(function (hook) {
    mpOptions[hook] = function (args) {
      return this.$vm.__call_hook(hook, args);
    };
  });
}

function getData(vueOptions, context) {
  var data = vueOptions.data || {};
  var methods = vueOptions.methods || {};

  if (typeof data === 'function') {
    try {
      data = data.call(context); // 支持 Vue.prototype 上挂的数据
    } catch (e) {
      if (Object({"VUE_APP_PLATFORM":"mp-weixin","NODE_ENV":"development","BASE_URL":"/"}).VUE_APP_DEBUG) {
        console.warn('根据 Vue 的 data 函数初始化小程序 data 失败，请尽量确保 data 函数中不访问 vm 对象，否则可能影响首次数据渲染速度。', data);
      }
    }
  } else {
    try {
      // 对 data 格式化
      data = JSON.parse(JSON.stringify(data));
    } catch (e) {}
  }

  Object.keys(methods).forEach(function (methodName) {
    if (!hasOwn(data, methodName)) {
      data[methodName] = methods[methodName];
    }
  });

  return data;
}

var PROP_TYPES = [String, Number, Boolean, Object, Array, null];

function createObserver(name) {
  return function observer(newVal, oldVal) {
    if (this.$vm) {
      this.$vm[name] = newVal; // 为了触发其他非 render watcher
    }
  };
}

function getProperties(props) {
  var properties = {
    vueSlots: { // 小程序不能直接定义 $slots 的 props，所以通过 vueSlots 转换到 $slots
      type: null,
      value: [],
      observer: function observer(newVal, oldVal) {
        var $slots = Object.create(null);
        newVal.forEach(function (slotName) {
          $slots[slotName] = true;
        });
        this.setData({
          $slots: $slots });

      } } };


  if (Array.isArray(props)) {// ['title']
    props.forEach(function (key) {
      properties[key] = {
        type: null,
        observer: createObserver(key) };

    });
  } else if (isPlainObject(props)) {// {title:{type:String,default:''},content:String}
    Object.keys(props).forEach(function (key) {
      var opts = props[key];
      if (isPlainObject(opts)) {// title:{type:String,default:''}
        var value = opts['default'];
        if (isFn(value)) {
          value = value();
        }
        properties[key] = {
          type: PROP_TYPES.includes(opts.type) ? opts.type : null,
          value: value,
          observer: createObserver(key) };

      } else {// content:String
        properties[key] = {
          type: PROP_TYPES.includes(opts) ? opts : null,
          observer: createObserver(key) };

      }
    });
  }
  return properties;
}

function wrapper$1(event) {
  event.stopPropagation = noop;
  event.preventDefault = noop;

  event.target = event.target || {};
  event.detail = event.detail || {};

  // TODO 又得兼容 mpvue 的 mp 对象
  event.mp = event;
  event.target = Object.assign({}, event.target, event.detail);
  return event;
}

function processEventArgs(event) {var args = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : [];var isCustom = arguments.length > 2 ? arguments[2] : undefined;var methodName = arguments.length > 3 ? arguments[3] : undefined;
  if (isCustom && !args.length) {// 无参数，直接传入 detail 数组
    if (!Array.isArray(event.detail)) {// 应该是使用了 wxcomponent 原生组件，为了向前兼容，传递原始 event 对象
      return [event];
    }
    return event.detail;
  }
  var ret = [];
  args.forEach(function (arg) {
    if (arg === '$event') {
      if (methodName === '__set_model' && !isCustom) {// input v-model value
        ret.push(event.target.value);
      } else {
        ret.push(isCustom ? event.detail[0] : event);
      }
    } else {
      ret.push(arg);
    }
  });

  return ret;
}

var ONCE = '~';
var CUSTOM = '^';

function handleEvent(event) {var _this = this;
  event = wrapper$1(event);

  // [['tap',[['handle',[1,2,a]],['handle1',[1,2,a]]]]]
  var eventOpts = (event.currentTarget || event.target).dataset.eventOpts;
  if (!eventOpts) {
    return console.warn("\u4E8B\u4EF6\u4FE1\u606F\u4E0D\u5B58\u5728");
  }

  // [['handle',[1,2,a]],['handle1',[1,2,a]]]
  var eventType = event.type;
  eventOpts.forEach(function (eventOpt) {
    var type = eventOpt[0];
    var eventsArray = eventOpt[1];

    var isCustom = type.charAt(0) === CUSTOM;
    type = isCustom ? type.slice(1) : type;
    var isOnce = type.charAt(0) === ONCE;
    type = isOnce ? type.slice(1) : type;

    if (eventsArray && eventType === type) {
      eventsArray.forEach(function (eventArray) {
        var methodName = eventArray[0];
        var handler = _this.$vm[methodName];
        if (!isFn(handler)) {
          throw new Error(" _vm.".concat(methodName, " is not a function"));
        }
        if (isOnce) {
          if (handler.once) {
            return;
          }
          handler.once = true;
        }
        handler.apply(_this.$vm, processEventArgs(event, eventArray[1], isCustom, methodName));
      });
    }
  });
}

function initRefs(vm) {
  var mpInstance = vm.$mp[vm.mpType];
  Object.defineProperty(vm, '$refs', {
    get: function get() {
      var $refs = Object.create(null);
      var components = mpInstance.selectAllComponents('.vue-ref');
      components.forEach(function (component) {
        var ref = component.dataset.ref;
        $refs[ref] = component.$vm;
      });
      var forComponents = mpInstance.selectAllComponents('.vue-ref-in-for');
      forComponents.forEach(function (component) {
        var ref = component.dataset.ref;
        if (!$refs[ref]) {
          $refs[ref] = [];
        }
        $refs[ref].push(component.$vm);
      });
      return $refs;
    } });

}

var hooks = [
'onShow',
'onHide',
'onError',
'onPageNotFound'];


function createApp(vm) {
  // 外部初始化时 Vue 还未初始化，放到 createApp 内部初始化 mixin
  _vue.default.mixin({
    beforeCreate: function beforeCreate() {
      if (!this.$options.mpType) {
        return;
      }
      this.mpType = this.$options.mpType;
      this.$mp = _defineProperty({
        data: {} },
      this.mpType, this.$options.mpInstance);

      delete this.$options.mpType;
      delete this.$options.mpInstance;

      if (this.mpType !== 'app') {
        initRefs(this);
        initMocks(this);
      }
    },
    created: function created() {// 处理 injections
      this.__init_injections(this);
      this.__init_provide(this);
    } });


  var appOptions = {
    onLaunch: function onLaunch(args) {
      this.$vm = vm;

      this.$vm._isMounted = true;
      this.$vm.__call_hook('mounted');

      this.$vm.__call_hook('onLaunch', args);
    } };


  initHooks(appOptions, hooks); // 延迟执行，因为 App 的注册在 main.js 之前，可能导致生命周期内 Vue 原型上开发者注册的属性无法访问

  App(appOptions);

  return vm;
}

function triggerLink(mpInstance, vueOptions) {
  mpInstance.triggerEvent('__l', mpInstance.$vm || vueOptions, {
    bubbles: true,
    composed: true });

}

function handleLink(event) {
  if (event.detail.$mp) {// vm
    if (!event.detail.$parent) {
      event.detail.$parent = this.$vm;
      event.detail.$parent.$children.push(event.detail);

      event.detail.$root = this.$vm.$root;
    }
  } else {// vueOptions
    if (!event.detail.parent) {
      event.detail.parent = this.$vm;
    }
  }
}

var hooks$1 = [
'onShow',
'onHide',
'onPullDownRefresh',
'onReachBottom',
'onShareAppMessage',
'onPageScroll',
'onResize',
'onTabItemTap',
'onBackPress',
'onNavigationBarButtonTap',
'onNavigationBarSearchInputChanged',
'onNavigationBarSearchInputConfirmed',
'onNavigationBarSearchInputClicked'];


function createPage(vueOptions) {
  vueOptions = vueOptions.default || vueOptions;
  var VueComponent;
  if (isFn(vueOptions)) {
    VueComponent = vueOptions;
    vueOptions = VueComponent.extendOptions;
  } else {
    VueComponent = _vue.default.extend(vueOptions);
  }
  var pageOptions = {
    options: {
      multipleSlots: true,
      addGlobalClass: true },

    data: getData(vueOptions, _vue.default.prototype),
    lifetimes: { // 当页面作为组件时
      attached: function attached() {

        this.$vm = new VueComponent({
          mpType: 'page',
          mpInstance: this });


        this.$vm.__call_hook('created');
        this.$vm.$mount();
      },
      ready: function ready() {
        this.$vm.__call_hook('beforeMount');
        this.$vm._isMounted = true;
        this.$vm.__call_hook('mounted');
        this.$vm.__call_hook('onReady');
      },
      detached: function detached() {
        this.$vm.$destroy();
      } },

    methods: { // 作为页面时
      onLoad: function onLoad(args) {
        this.$vm.$mp.query = args; // 又要兼容 mpvue
        this.$vm.__call_hook('onLoad', args); // 开发者可能会在 onLoad 时赋值，提前到 mount 之前
      },
      onUnload: function onUnload() {
        this.$vm.__call_hook('onUnload');
      },
      __e: handleEvent,
      __l: handleLink } };



  initHooks(pageOptions.methods, hooks$1);

  return Component(pageOptions);
}

function initVueComponent(mpInstace, VueComponent) {var extraOptions = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};
  if (mpInstace.$vm) {
    return;
  }

  var options = Object.assign({
    mpType: 'component',
    mpInstance: mpInstace,
    propsData: mpInstace.properties },
  extraOptions);
  // 初始化 vue 实例
  mpInstace.$vm = new VueComponent(options);

  // 处理$slots,$scopedSlots（暂不支持动态变化$slots）
  var vueSlots = mpInstace.properties.vueSlots;
  if (Array.isArray(vueSlots) && vueSlots.length) {
    var $slots = Object.create(null);
    vueSlots.forEach(function (slotName) {
      $slots[slotName] = true;
    });
    mpInstace.$vm.$scopedSlots = mpInstace.$vm.$slots = $slots;
  }
  // 性能优先，mount 提前到 attached 中，保证组件首次渲染数据被合并
  // 导致与标准 Vue 的差异，data 和 computed 中不能使用$parent，provide等组件属性
  mpInstace.$vm.$mount();
}

function createComponent(vueOptions) {
  vueOptions = vueOptions.default || vueOptions;

  var properties = getProperties(vueOptions.props);

  var VueComponent = _vue.default.extend(vueOptions);

  var componentOptions = {
    options: {
      multipleSlots: true,
      addGlobalClass: true },

    data: getData(vueOptions, _vue.default.prototype),
    properties: properties,
    lifetimes: {
      attached: function attached() {
        initVueComponent(this, VueComponent);
      },
      ready: function ready() {
        initVueComponent(this, VueComponent); // 目前发现部分情况小程序 attached 不触发
        triggerLink(this); // 处理 parent,children

        // 补充生命周期
        this.$vm.__call_hook('created');
        this.$vm.__call_hook('beforeMount');
        this.$vm._isMounted = true;
        this.$vm.__call_hook('mounted');
        this.$vm.__call_hook('onReady');
      },
      detached: function detached() {
        this.$vm.$destroy();
      } },

    pageLifetimes: {
      show: function show(args) {
        this.$vm.__call_hook('onPageShow', args);
      },
      hide: function hide() {
        this.$vm && this.$vm.__call_hook('onPageHide');
      },
      resize: function resize(size) {
        this.$vm && this.$vm.__call_hook('onPageResize', size);
      } },

    methods: {
      __e: handleEvent,
      __l: handleLink } };



  return Component(componentOptions);
}

var uni = {};

if (typeof Proxy !== 'undefined') {
  uni = new Proxy({}, {
    get: function get(target, name) {
      if (name === 'upx2px') {
        return upx2px;
      }
      if (api[name]) {
        return promisify(name, api[name]);
      }
      {
        if (extraApi[name]) {
          return promisify(name, extraApi[name]);
        }
        if (todoApis[name]) {
          return promisify(name, todoApis[name]);
        }
      }
      if (!hasOwn(wx, name) && !hasOwn(protocols, name)) {
        return;
      }
      return promisify(name, wrapper(name, wx[name]));
    } });

} else {
  uni.upx2px = upx2px;

  {
    Object.keys(todoApis).forEach(function (name) {
      uni[name] = promisify(name, todoApis[name]);
    });
    Object.keys(extraApi).forEach(function (name) {
      uni[name] = promisify(name, todoApis[name]);
    });
  }

  Object.keys(api).forEach(function (name) {
    uni[name] = promisify(name, api[name]);
  });

  Object.keys(wx).forEach(function (name) {
    if (hasOwn(wx, name) || hasOwn(protocols, name)) {
      uni[name] = promisify(name, wrapper(name, wx[name]));
    }
  });
}

var uni$1 = uni;var _default =

uni$1;exports.default = _default;

/***/ }),

/***/ "./node_modules/@dcloudio/vue-cli-plugin-uni/packages/mp-vue/dist/mp.runtime.esm.js":
/*!******************************************************************************************!*\
  !*** ./node_modules/@dcloudio/vue-cli-plugin-uni/packages/mp-vue/dist/mp.runtime.esm.js ***!
  \******************************************************************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* WEBPACK VAR INJECTION */(function(global) {/*!
 * Vue.js v2.6.10
 * (c) 2014-2019 Evan You
 * Released under the MIT License.
 */
/*  */

var emptyObject = Object.freeze({});

// These helpers produce better VM code in JS engines due to their
// explicitness and function inlining.
function isUndef (v) {
  return v === undefined || v === null
}

function isDef (v) {
  return v !== undefined && v !== null
}

function isTrue (v) {
  return v === true
}

function isFalse (v) {
  return v === false
}

/**
 * Check if value is primitive.
 */
function isPrimitive (value) {
  return (
    typeof value === 'string' ||
    typeof value === 'number' ||
    // $flow-disable-line
    typeof value === 'symbol' ||
    typeof value === 'boolean'
  )
}

/**
 * Quick object check - this is primarily used to tell
 * Objects from primitive values when we know the value
 * is a JSON-compliant type.
 */
function isObject (obj) {
  return obj !== null && typeof obj === 'object'
}

/**
 * Get the raw type string of a value, e.g., [object Object].
 */
var _toString = Object.prototype.toString;

function toRawType (value) {
  return _toString.call(value).slice(8, -1)
}

/**
 * Strict object type check. Only returns true
 * for plain JavaScript objects.
 */
function isPlainObject (obj) {
  return _toString.call(obj) === '[object Object]'
}

function isRegExp (v) {
  return _toString.call(v) === '[object RegExp]'
}

/**
 * Check if val is a valid array index.
 */
function isValidArrayIndex (val) {
  var n = parseFloat(String(val));
  return n >= 0 && Math.floor(n) === n && isFinite(val)
}

function isPromise (val) {
  return (
    isDef(val) &&
    typeof val.then === 'function' &&
    typeof val.catch === 'function'
  )
}

/**
 * Convert a value to a string that is actually rendered.
 */
function toString (val) {
  return val == null
    ? ''
    : Array.isArray(val) || (isPlainObject(val) && val.toString === _toString)
      ? JSON.stringify(val, null, 2)
      : String(val)
}

/**
 * Convert an input value to a number for persistence.
 * If the conversion fails, return original string.
 */
function toNumber (val) {
  var n = parseFloat(val);
  return isNaN(n) ? val : n
}

/**
 * Make a map and return a function for checking if a key
 * is in that map.
 */
function makeMap (
  str,
  expectsLowerCase
) {
  var map = Object.create(null);
  var list = str.split(',');
  for (var i = 0; i < list.length; i++) {
    map[list[i]] = true;
  }
  return expectsLowerCase
    ? function (val) { return map[val.toLowerCase()]; }
    : function (val) { return map[val]; }
}

/**
 * Check if a tag is a built-in tag.
 */
var isBuiltInTag = makeMap('slot,component', true);

/**
 * Check if an attribute is a reserved attribute.
 */
var isReservedAttribute = makeMap('key,ref,slot,slot-scope,is');

/**
 * Remove an item from an array.
 */
function remove (arr, item) {
  if (arr.length) {
    var index = arr.indexOf(item);
    if (index > -1) {
      return arr.splice(index, 1)
    }
  }
}

/**
 * Check whether an object has the property.
 */
var hasOwnProperty = Object.prototype.hasOwnProperty;
function hasOwn (obj, key) {
  return hasOwnProperty.call(obj, key)
}

/**
 * Create a cached version of a pure function.
 */
function cached (fn) {
  var cache = Object.create(null);
  return (function cachedFn (str) {
    var hit = cache[str];
    return hit || (cache[str] = fn(str))
  })
}

/**
 * Camelize a hyphen-delimited string.
 */
var camelizeRE = /-(\w)/g;
var camelize = cached(function (str) {
  return str.replace(camelizeRE, function (_, c) { return c ? c.toUpperCase() : ''; })
});

/**
 * Capitalize a string.
 */
var capitalize = cached(function (str) {
  return str.charAt(0).toUpperCase() + str.slice(1)
});

/**
 * Hyphenate a camelCase string.
 */
var hyphenateRE = /\B([A-Z])/g;
var hyphenate = cached(function (str) {
  return str.replace(hyphenateRE, '-$1').toLowerCase()
});

/**
 * Simple bind polyfill for environments that do not support it,
 * e.g., PhantomJS 1.x. Technically, we don't need this anymore
 * since native bind is now performant enough in most browsers.
 * But removing it would mean breaking code that was able to run in
 * PhantomJS 1.x, so this must be kept for backward compatibility.
 */

/* istanbul ignore next */
function polyfillBind (fn, ctx) {
  function boundFn (a) {
    var l = arguments.length;
    return l
      ? l > 1
        ? fn.apply(ctx, arguments)
        : fn.call(ctx, a)
      : fn.call(ctx)
  }

  boundFn._length = fn.length;
  return boundFn
}

function nativeBind (fn, ctx) {
  return fn.bind(ctx)
}

var bind = Function.prototype.bind
  ? nativeBind
  : polyfillBind;

/**
 * Convert an Array-like object to a real Array.
 */
function toArray (list, start) {
  start = start || 0;
  var i = list.length - start;
  var ret = new Array(i);
  while (i--) {
    ret[i] = list[i + start];
  }
  return ret
}

/**
 * Mix properties into target object.
 */
function extend (to, _from) {
  for (var key in _from) {
    to[key] = _from[key];
  }
  return to
}

/**
 * Merge an Array of Objects into a single Object.
 */
function toObject (arr) {
  var res = {};
  for (var i = 0; i < arr.length; i++) {
    if (arr[i]) {
      extend(res, arr[i]);
    }
  }
  return res
}

/* eslint-disable no-unused-vars */

/**
 * Perform no operation.
 * Stubbing args to make Flow happy without leaving useless transpiled code
 * with ...rest (https://flow.org/blog/2017/05/07/Strict-Function-Call-Arity/).
 */
function noop (a, b, c) {}

/**
 * Always return false.
 */
var no = function (a, b, c) { return false; };

/* eslint-enable no-unused-vars */

/**
 * Return the same value.
 */
var identity = function (_) { return _; };

/**
 * Check if two values are loosely equal - that is,
 * if they are plain objects, do they have the same shape?
 */
function looseEqual (a, b) {
  if (a === b) { return true }
  var isObjectA = isObject(a);
  var isObjectB = isObject(b);
  if (isObjectA && isObjectB) {
    try {
      var isArrayA = Array.isArray(a);
      var isArrayB = Array.isArray(b);
      if (isArrayA && isArrayB) {
        return a.length === b.length && a.every(function (e, i) {
          return looseEqual(e, b[i])
        })
      } else if (a instanceof Date && b instanceof Date) {
        return a.getTime() === b.getTime()
      } else if (!isArrayA && !isArrayB) {
        var keysA = Object.keys(a);
        var keysB = Object.keys(b);
        return keysA.length === keysB.length && keysA.every(function (key) {
          return looseEqual(a[key], b[key])
        })
      } else {
        /* istanbul ignore next */
        return false
      }
    } catch (e) {
      /* istanbul ignore next */
      return false
    }
  } else if (!isObjectA && !isObjectB) {
    return String(a) === String(b)
  } else {
    return false
  }
}

/**
 * Return the first index at which a loosely equal value can be
 * found in the array (if value is a plain object, the array must
 * contain an object of the same shape), or -1 if it is not present.
 */
function looseIndexOf (arr, val) {
  for (var i = 0; i < arr.length; i++) {
    if (looseEqual(arr[i], val)) { return i }
  }
  return -1
}

/**
 * Ensure a function is called only once.
 */
function once (fn) {
  var called = false;
  return function () {
    if (!called) {
      called = true;
      fn.apply(this, arguments);
    }
  }
}

var ASSET_TYPES = [
  'component',
  'directive',
  'filter'
];

var LIFECYCLE_HOOKS = [
  'beforeCreate',
  'created',
  'beforeMount',
  'mounted',
  'beforeUpdate',
  'updated',
  'beforeDestroy',
  'destroyed',
  'activated',
  'deactivated',
  'errorCaptured',
  'serverPrefetch'
];

/*  */



var config = ({
  /**
   * Option merge strategies (used in core/util/options)
   */
  // $flow-disable-line
  optionMergeStrategies: Object.create(null),

  /**
   * Whether to suppress warnings.
   */
  silent: false,

  /**
   * Show production mode tip message on boot?
   */
  productionTip: "development" !== 'production',

  /**
   * Whether to enable devtools
   */
  devtools: "development" !== 'production',

  /**
   * Whether to record perf
   */
  performance: false,

  /**
   * Error handler for watcher errors
   */
  errorHandler: null,

  /**
   * Warn handler for watcher warns
   */
  warnHandler: null,

  /**
   * Ignore certain custom elements
   */
  ignoredElements: [],

  /**
   * Custom user key aliases for v-on
   */
  // $flow-disable-line
  keyCodes: Object.create(null),

  /**
   * Check if a tag is reserved so that it cannot be registered as a
   * component. This is platform-dependent and may be overwritten.
   */
  isReservedTag: no,

  /**
   * Check if an attribute is reserved so that it cannot be used as a component
   * prop. This is platform-dependent and may be overwritten.
   */
  isReservedAttr: no,

  /**
   * Check if a tag is an unknown element.
   * Platform-dependent.
   */
  isUnknownElement: no,

  /**
   * Get the namespace of an element
   */
  getTagNamespace: noop,

  /**
   * Parse the real tag name for the specific platform.
   */
  parsePlatformTagName: identity,

  /**
   * Check if an attribute must be bound using property, e.g. value
   * Platform-dependent.
   */
  mustUseProp: no,

  /**
   * Perform updates asynchronously. Intended to be used by Vue Test Utils
   * This will significantly reduce performance if set to false.
   */
  async: true,

  /**
   * Exposed for legacy reasons
   */
  _lifecycleHooks: LIFECYCLE_HOOKS
});

/*  */

/**
 * unicode letters used for parsing html tags, component names and property paths.
 * using https://www.w3.org/TR/html53/semantics-scripting.html#potentialcustomelementname
 * skipping \u10000-\uEFFFF due to it freezing up PhantomJS
 */
var unicodeRegExp = /a-zA-Z\u00B7\u00C0-\u00D6\u00D8-\u00F6\u00F8-\u037D\u037F-\u1FFF\u200C-\u200D\u203F-\u2040\u2070-\u218F\u2C00-\u2FEF\u3001-\uD7FF\uF900-\uFDCF\uFDF0-\uFFFD/;

/**
 * Check if a string starts with $ or _
 */
function isReserved (str) {
  var c = (str + '').charCodeAt(0);
  return c === 0x24 || c === 0x5F
}

/**
 * Define a property.
 */
function def (obj, key, val, enumerable) {
  Object.defineProperty(obj, key, {
    value: val,
    enumerable: !!enumerable,
    writable: true,
    configurable: true
  });
}

/**
 * Parse simple path.
 */
var bailRE = new RegExp(("[^" + (unicodeRegExp.source) + ".$_\\d]"));
function parsePath (path) {
  if (bailRE.test(path)) {
    return
  }
  var segments = path.split('.');
  return function (obj) {
    for (var i = 0; i < segments.length; i++) {
      if (!obj) { return }
      obj = obj[segments[i]];
    }
    return obj
  }
}

/*  */

// can we use __proto__?
var hasProto = '__proto__' in {};

// Browser environment sniffing
var inBrowser = typeof window !== 'undefined';
var inWeex = typeof WXEnvironment !== 'undefined' && !!WXEnvironment.platform;
var weexPlatform = inWeex && WXEnvironment.platform.toLowerCase();
var UA = inBrowser && window.navigator.userAgent.toLowerCase();
var isIE = UA && /msie|trident/.test(UA);
var isIE9 = UA && UA.indexOf('msie 9.0') > 0;
var isEdge = UA && UA.indexOf('edge/') > 0;
var isAndroid = (UA && UA.indexOf('android') > 0) || (weexPlatform === 'android');
var isIOS = (UA && /iphone|ipad|ipod|ios/.test(UA)) || (weexPlatform === 'ios');
var isChrome = UA && /chrome\/\d+/.test(UA) && !isEdge;
var isPhantomJS = UA && /phantomjs/.test(UA);
var isFF = UA && UA.match(/firefox\/(\d+)/);

// Firefox has a "watch" function on Object.prototype...
var nativeWatch = ({}).watch;
if (inBrowser) {
  try {
    var opts = {};
    Object.defineProperty(opts, 'passive', ({
      get: function get () {
      }
    })); // https://github.com/facebook/flow/issues/285
    window.addEventListener('test-passive', null, opts);
  } catch (e) {}
}

// this needs to be lazy-evaled because vue may be required before
// vue-server-renderer can set VUE_ENV
var _isServer;
var isServerRendering = function () {
  if (_isServer === undefined) {
    /* istanbul ignore if */
    if (!inBrowser && !inWeex && typeof global !== 'undefined') {
      // detect presence of vue-server-renderer and avoid
      // Webpack shimming the process
      _isServer = global['process'] && global['process'].env.VUE_ENV === 'server';
    } else {
      _isServer = false;
    }
  }
  return _isServer
};

// detect devtools
var devtools = inBrowser && window.__VUE_DEVTOOLS_GLOBAL_HOOK__;

/* istanbul ignore next */
function isNative (Ctor) {
  return typeof Ctor === 'function' && /native code/.test(Ctor.toString())
}

var hasSymbol =
  typeof Symbol !== 'undefined' && isNative(Symbol) &&
  typeof Reflect !== 'undefined' && isNative(Reflect.ownKeys);

var _Set;
/* istanbul ignore if */ // $flow-disable-line
if (typeof Set !== 'undefined' && isNative(Set)) {
  // use native Set when available.
  _Set = Set;
} else {
  // a non-standard Set polyfill that only works with primitive keys.
  _Set = /*@__PURE__*/(function () {
    function Set () {
      this.set = Object.create(null);
    }
    Set.prototype.has = function has (key) {
      return this.set[key] === true
    };
    Set.prototype.add = function add (key) {
      this.set[key] = true;
    };
    Set.prototype.clear = function clear () {
      this.set = Object.create(null);
    };

    return Set;
  }());
}

/*  */

var warn = noop;
var tip = noop;
var generateComponentTrace = (noop); // work around flow check
var formatComponentName = (noop);

if (true) {
  var hasConsole = typeof console !== 'undefined';
  var classifyRE = /(?:^|[-_])(\w)/g;
  var classify = function (str) { return str
    .replace(classifyRE, function (c) { return c.toUpperCase(); })
    .replace(/[-_]/g, ''); };

  warn = function (msg, vm) {
    var trace = vm ? generateComponentTrace(vm) : '';

    if (config.warnHandler) {
      config.warnHandler.call(null, msg, vm, trace);
    } else if (hasConsole && (!config.silent)) {
      console.error(("[Vue warn]: " + msg + trace));
    }
  };

  tip = function (msg, vm) {
    if (hasConsole && (!config.silent)) {
      console.warn("[Vue tip]: " + msg + (
        vm ? generateComponentTrace(vm) : ''
      ));
    }
  };

  formatComponentName = function (vm, includeFile) {
    {
      if(vm.$mp && vm.$mp[vm.mpType]){
        return vm.$mp[vm.mpType].is
      }
    }
    if (vm.$root === vm) {
      return '<Root>'
    }
    var options = typeof vm === 'function' && vm.cid != null
      ? vm.options
      : vm._isVue
        ? vm.$options || vm.constructor.options
        : vm;
    var name = options.name || options._componentTag;
    var file = options.__file;
    if (!name && file) {
      var match = file.match(/([^/\\]+)\.vue$/);
      name = match && match[1];
    }

    return (
      (name ? ("<" + (classify(name)) + ">") : "<Anonymous>") +
      (file && includeFile !== false ? (" at " + file) : '')
    )
  };

  var repeat = function (str, n) {
    var res = '';
    while (n) {
      if (n % 2 === 1) { res += str; }
      if (n > 1) { str += str; }
      n >>= 1;
    }
    return res
  };

  generateComponentTrace = function (vm) {
    if (vm._isVue && vm.$parent) {
      var tree = [];
      var currentRecursiveSequence = 0;
      while (vm) {
        if (tree.length > 0) {
          var last = tree[tree.length - 1];
          if (last.constructor === vm.constructor) {
            currentRecursiveSequence++;
            vm = vm.$parent;
            continue
          } else if (currentRecursiveSequence > 0) {
            tree[tree.length - 1] = [last, currentRecursiveSequence];
            currentRecursiveSequence = 0;
          }
        }
        tree.push(vm);
        vm = vm.$parent;
      }
      return '\n\nfound in\n\n' + tree
        .map(function (vm, i) { return ("" + (i === 0 ? '---> ' : repeat(' ', 5 + i * 2)) + (Array.isArray(vm)
            ? ((formatComponentName(vm[0])) + "... (" + (vm[1]) + " recursive calls)")
            : formatComponentName(vm))); })
        .join('\n')
    } else {
      return ("\n\n(found in " + (formatComponentName(vm)) + ")")
    }
  };
}

/*  */

var uid = 0;

/**
 * A dep is an observable that can have multiple
 * directives subscribing to it.
 */
var Dep = function Dep () {
  this.id = uid++;
  this.subs = [];
};

Dep.prototype.addSub = function addSub (sub) {
  this.subs.push(sub);
};

Dep.prototype.removeSub = function removeSub (sub) {
  remove(this.subs, sub);
};

Dep.prototype.depend = function depend () {
  if (Dep.target) {
    Dep.target.addDep(this);
  }
};

Dep.prototype.notify = function notify () {
  // stabilize the subscriber list first
  var subs = this.subs.slice();
  if ( true && !config.async) {
    // subs aren't sorted in scheduler if not running async
    // we need to sort them now to make sure they fire in correct
    // order
    subs.sort(function (a, b) { return a.id - b.id; });
  }
  for (var i = 0, l = subs.length; i < l; i++) {
    subs[i].update();
  }
};

// The current target watcher being evaluated.
// This is globally unique because only one watcher
// can be evaluated at a time.
Dep.target = null;
var targetStack = [];

function pushTarget (target) {
  targetStack.push(target);
  Dep.target = target;
}

function popTarget () {
  targetStack.pop();
  Dep.target = targetStack[targetStack.length - 1];
}

/*  */

var VNode = function VNode (
  tag,
  data,
  children,
  text,
  elm,
  context,
  componentOptions,
  asyncFactory
) {
  this.tag = tag;
  this.data = data;
  this.children = children;
  this.text = text;
  this.elm = elm;
  this.ns = undefined;
  this.context = context;
  this.fnContext = undefined;
  this.fnOptions = undefined;
  this.fnScopeId = undefined;
  this.key = data && data.key;
  this.componentOptions = componentOptions;
  this.componentInstance = undefined;
  this.parent = undefined;
  this.raw = false;
  this.isStatic = false;
  this.isRootInsert = true;
  this.isComment = false;
  this.isCloned = false;
  this.isOnce = false;
  this.asyncFactory = asyncFactory;
  this.asyncMeta = undefined;
  this.isAsyncPlaceholder = false;
};

var prototypeAccessors = { child: { configurable: true } };

// DEPRECATED: alias for componentInstance for backwards compat.
/* istanbul ignore next */
prototypeAccessors.child.get = function () {
  return this.componentInstance
};

Object.defineProperties( VNode.prototype, prototypeAccessors );

var createEmptyVNode = function (text) {
  if ( text === void 0 ) text = '';

  var node = new VNode();
  node.text = text;
  node.isComment = true;
  return node
};

function createTextVNode (val) {
  return new VNode(undefined, undefined, undefined, String(val))
}

// optimized shallow clone
// used for static nodes and slot nodes because they may be reused across
// multiple renders, cloning them avoids errors when DOM manipulations rely
// on their elm reference.
function cloneVNode (vnode) {
  var cloned = new VNode(
    vnode.tag,
    vnode.data,
    // #7975
    // clone children array to avoid mutating original in case of cloning
    // a child.
    vnode.children && vnode.children.slice(),
    vnode.text,
    vnode.elm,
    vnode.context,
    vnode.componentOptions,
    vnode.asyncFactory
  );
  cloned.ns = vnode.ns;
  cloned.isStatic = vnode.isStatic;
  cloned.key = vnode.key;
  cloned.isComment = vnode.isComment;
  cloned.fnContext = vnode.fnContext;
  cloned.fnOptions = vnode.fnOptions;
  cloned.fnScopeId = vnode.fnScopeId;
  cloned.asyncMeta = vnode.asyncMeta;
  cloned.isCloned = true;
  return cloned
}

/*
 * not type checking this file because flow doesn't play well with
 * dynamically accessing methods on Array prototype
 */

var arrayProto = Array.prototype;
var arrayMethods = Object.create(arrayProto);

var methodsToPatch = [
  'push',
  'pop',
  'shift',
  'unshift',
  'splice',
  'sort',
  'reverse'
];

/**
 * Intercept mutating methods and emit events
 */
methodsToPatch.forEach(function (method) {
  // cache original method
  var original = arrayProto[method];
  def(arrayMethods, method, function mutator () {
    var args = [], len = arguments.length;
    while ( len-- ) args[ len ] = arguments[ len ];

    var result = original.apply(this, args);
    var ob = this.__ob__;
    var inserted;
    switch (method) {
      case 'push':
      case 'unshift':
        inserted = args;
        break
      case 'splice':
        inserted = args.slice(2);
        break
    }
    if (inserted) { ob.observeArray(inserted); }
    // notify change
    ob.dep.notify();
    return result
  });
});

/*  */

var arrayKeys = Object.getOwnPropertyNames(arrayMethods);

/**
 * In some cases we may want to disable observation inside a component's
 * update computation.
 */
var shouldObserve = true;

function toggleObserving (value) {
  shouldObserve = value;
}

/**
 * Observer class that is attached to each observed
 * object. Once attached, the observer converts the target
 * object's property keys into getter/setters that
 * collect dependencies and dispatch updates.
 */
var Observer = function Observer (value) {
  this.value = value;
  this.dep = new Dep();
  this.vmCount = 0;
  def(value, '__ob__', this);
  if (Array.isArray(value)) {
    if (hasProto) {
      protoAugment(value, arrayMethods);
    } else {
      copyAugment(value, arrayMethods, arrayKeys);
    }
    this.observeArray(value);
  } else {
    this.walk(value);
  }
};

/**
 * Walk through all properties and convert them into
 * getter/setters. This method should only be called when
 * value type is Object.
 */
Observer.prototype.walk = function walk (obj) {
  var keys = Object.keys(obj);
  for (var i = 0; i < keys.length; i++) {
    defineReactive$$1(obj, keys[i]);
  }
};

/**
 * Observe a list of Array items.
 */
Observer.prototype.observeArray = function observeArray (items) {
  for (var i = 0, l = items.length; i < l; i++) {
    observe(items[i]);
  }
};

// helpers

/**
 * Augment a target Object or Array by intercepting
 * the prototype chain using __proto__
 */
function protoAugment (target, src) {
  /* eslint-disable no-proto */
  target.__proto__ = src;
  /* eslint-enable no-proto */
}

/**
 * Augment a target Object or Array by defining
 * hidden properties.
 */
/* istanbul ignore next */
function copyAugment (target, src, keys) {
  for (var i = 0, l = keys.length; i < l; i++) {
    var key = keys[i];
    def(target, key, src[key]);
  }
}

/**
 * Attempt to create an observer instance for a value,
 * returns the new observer if successfully observed,
 * or the existing observer if the value already has one.
 */
function observe (value, asRootData) {
  if (!isObject(value) || value instanceof VNode) {
    return
  }
  var ob;
  if (hasOwn(value, '__ob__') && value.__ob__ instanceof Observer) {
    ob = value.__ob__;
  } else if (
    shouldObserve &&
    !isServerRendering() &&
    (Array.isArray(value) || isPlainObject(value)) &&
    Object.isExtensible(value) &&
    !value._isVue
  ) {
    ob = new Observer(value);
  }
  if (asRootData && ob) {
    ob.vmCount++;
  }
  return ob
}

/**
 * Define a reactive property on an Object.
 */
function defineReactive$$1 (
  obj,
  key,
  val,
  customSetter,
  shallow
) {
  var dep = new Dep();

  var property = Object.getOwnPropertyDescriptor(obj, key);
  if (property && property.configurable === false) {
    return
  }

  // cater for pre-defined getter/setters
  var getter = property && property.get;
  var setter = property && property.set;
  if ((!getter || setter) && arguments.length === 2) {
    val = obj[key];
  }

  var childOb = !shallow && observe(val);
  Object.defineProperty(obj, key, {
    enumerable: true,
    configurable: true,
    get: function reactiveGetter () {
      var value = getter ? getter.call(obj) : val;
      if (Dep.target) {
        dep.depend();
        if (childOb) {
          childOb.dep.depend();
          if (Array.isArray(value)) {
            dependArray(value);
          }
        }
      }
      return value
    },
    set: function reactiveSetter (newVal) {
      var value = getter ? getter.call(obj) : val;
      /* eslint-disable no-self-compare */
      if (newVal === value || (newVal !== newVal && value !== value)) {
        return
      }
      /* eslint-enable no-self-compare */
      if ( true && customSetter) {
        customSetter();
      }
      // #7981: for accessor properties without setter
      if (getter && !setter) { return }
      if (setter) {
        setter.call(obj, newVal);
      } else {
        val = newVal;
      }
      childOb = !shallow && observe(newVal);
      dep.notify();
    }
  });
}

/**
 * Set a property on an object. Adds the new property and
 * triggers change notification if the property doesn't
 * already exist.
 */
function set (target, key, val) {
  if ( true &&
    (isUndef(target) || isPrimitive(target))
  ) {
    warn(("Cannot set reactive property on undefined, null, or primitive value: " + ((target))));
  }
  if (Array.isArray(target) && isValidArrayIndex(key)) {
    target.length = Math.max(target.length, key);
    target.splice(key, 1, val);
    return val
  }
  if (key in target && !(key in Object.prototype)) {
    target[key] = val;
    return val
  }
  var ob = (target).__ob__;
  if (target._isVue || (ob && ob.vmCount)) {
     true && warn(
      'Avoid adding reactive properties to a Vue instance or its root $data ' +
      'at runtime - declare it upfront in the data option.'
    );
    return val
  }
  if (!ob) {
    target[key] = val;
    return val
  }
  defineReactive$$1(ob.value, key, val);
  ob.dep.notify();
  return val
}

/**
 * Delete a property and trigger change if necessary.
 */
function del (target, key) {
  if ( true &&
    (isUndef(target) || isPrimitive(target))
  ) {
    warn(("Cannot delete reactive property on undefined, null, or primitive value: " + ((target))));
  }
  if (Array.isArray(target) && isValidArrayIndex(key)) {
    target.splice(key, 1);
    return
  }
  var ob = (target).__ob__;
  if (target._isVue || (ob && ob.vmCount)) {
     true && warn(
      'Avoid deleting properties on a Vue instance or its root $data ' +
      '- just set it to null.'
    );
    return
  }
  if (!hasOwn(target, key)) {
    return
  }
  delete target[key];
  if (!ob) {
    return
  }
  ob.dep.notify();
}

/**
 * Collect dependencies on array elements when the array is touched, since
 * we cannot intercept array element access like property getters.
 */
function dependArray (value) {
  for (var e = (void 0), i = 0, l = value.length; i < l; i++) {
    e = value[i];
    e && e.__ob__ && e.__ob__.dep.depend();
    if (Array.isArray(e)) {
      dependArray(e);
    }
  }
}

/*  */

/**
 * Option overwriting strategies are functions that handle
 * how to merge a parent option value and a child option
 * value into the final value.
 */
var strats = config.optionMergeStrategies;

/**
 * Options with restrictions
 */
if (true) {
  strats.el = strats.propsData = function (parent, child, vm, key) {
    if (!vm) {
      warn(
        "option \"" + key + "\" can only be used during instance " +
        'creation with the `new` keyword.'
      );
    }
    return defaultStrat(parent, child)
  };
}

/**
 * Helper that recursively merges two data objects together.
 */
function mergeData (to, from) {
  if (!from) { return to }
  var key, toVal, fromVal;

  var keys = hasSymbol
    ? Reflect.ownKeys(from)
    : Object.keys(from);

  for (var i = 0; i < keys.length; i++) {
    key = keys[i];
    // in case the object is already observed...
    if (key === '__ob__') { continue }
    toVal = to[key];
    fromVal = from[key];
    if (!hasOwn(to, key)) {
      set(to, key, fromVal);
    } else if (
      toVal !== fromVal &&
      isPlainObject(toVal) &&
      isPlainObject(fromVal)
    ) {
      mergeData(toVal, fromVal);
    }
  }
  return to
}

/**
 * Data
 */
function mergeDataOrFn (
  parentVal,
  childVal,
  vm
) {
  if (!vm) {
    // in a Vue.extend merge, both should be functions
    if (!childVal) {
      return parentVal
    }
    if (!parentVal) {
      return childVal
    }
    // when parentVal & childVal are both present,
    // we need to return a function that returns the
    // merged result of both functions... no need to
    // check if parentVal is a function here because
    // it has to be a function to pass previous merges.
    return function mergedDataFn () {
      return mergeData(
        typeof childVal === 'function' ? childVal.call(this, this) : childVal,
        typeof parentVal === 'function' ? parentVal.call(this, this) : parentVal
      )
    }
  } else {
    return function mergedInstanceDataFn () {
      // instance merge
      var instanceData = typeof childVal === 'function'
        ? childVal.call(vm, vm)
        : childVal;
      var defaultData = typeof parentVal === 'function'
        ? parentVal.call(vm, vm)
        : parentVal;
      if (instanceData) {
        return mergeData(instanceData, defaultData)
      } else {
        return defaultData
      }
    }
  }
}

strats.data = function (
  parentVal,
  childVal,
  vm
) {
  if (!vm) {
    if (childVal && typeof childVal !== 'function') {
       true && warn(
        'The "data" option should be a function ' +
        'that returns a per-instance value in component ' +
        'definitions.',
        vm
      );

      return parentVal
    }
    return mergeDataOrFn(parentVal, childVal)
  }

  return mergeDataOrFn(parentVal, childVal, vm)
};

/**
 * Hooks and props are merged as arrays.
 */
function mergeHook (
  parentVal,
  childVal
) {
  var res = childVal
    ? parentVal
      ? parentVal.concat(childVal)
      : Array.isArray(childVal)
        ? childVal
        : [childVal]
    : parentVal;
  return res
    ? dedupeHooks(res)
    : res
}

function dedupeHooks (hooks) {
  var res = [];
  for (var i = 0; i < hooks.length; i++) {
    if (res.indexOf(hooks[i]) === -1) {
      res.push(hooks[i]);
    }
  }
  return res
}

LIFECYCLE_HOOKS.forEach(function (hook) {
  strats[hook] = mergeHook;
});

/**
 * Assets
 *
 * When a vm is present (instance creation), we need to do
 * a three-way merge between constructor options, instance
 * options and parent options.
 */
function mergeAssets (
  parentVal,
  childVal,
  vm,
  key
) {
  var res = Object.create(parentVal || null);
  if (childVal) {
     true && assertObjectType(key, childVal, vm);
    return extend(res, childVal)
  } else {
    return res
  }
}

ASSET_TYPES.forEach(function (type) {
  strats[type + 's'] = mergeAssets;
});

/**
 * Watchers.
 *
 * Watchers hashes should not overwrite one
 * another, so we merge them as arrays.
 */
strats.watch = function (
  parentVal,
  childVal,
  vm,
  key
) {
  // work around Firefox's Object.prototype.watch...
  if (parentVal === nativeWatch) { parentVal = undefined; }
  if (childVal === nativeWatch) { childVal = undefined; }
  /* istanbul ignore if */
  if (!childVal) { return Object.create(parentVal || null) }
  if (true) {
    assertObjectType(key, childVal, vm);
  }
  if (!parentVal) { return childVal }
  var ret = {};
  extend(ret, parentVal);
  for (var key$1 in childVal) {
    var parent = ret[key$1];
    var child = childVal[key$1];
    if (parent && !Array.isArray(parent)) {
      parent = [parent];
    }
    ret[key$1] = parent
      ? parent.concat(child)
      : Array.isArray(child) ? child : [child];
  }
  return ret
};

/**
 * Other object hashes.
 */
strats.props =
strats.methods =
strats.inject =
strats.computed = function (
  parentVal,
  childVal,
  vm,
  key
) {
  if (childVal && "development" !== 'production') {
    assertObjectType(key, childVal, vm);
  }
  if (!parentVal) { return childVal }
  var ret = Object.create(null);
  extend(ret, parentVal);
  if (childVal) { extend(ret, childVal); }
  return ret
};
strats.provide = mergeDataOrFn;

/**
 * Default strategy.
 */
var defaultStrat = function (parentVal, childVal) {
  return childVal === undefined
    ? parentVal
    : childVal
};

/**
 * Validate component names
 */
function checkComponents (options) {
  for (var key in options.components) {
    validateComponentName(key);
  }
}

function validateComponentName (name) {
  if (!new RegExp(("^[a-zA-Z][\\-\\.0-9_" + (unicodeRegExp.source) + "]*$")).test(name)) {
    warn(
      'Invalid component name: "' + name + '". Component names ' +
      'should conform to valid custom element name in html5 specification.'
    );
  }
  if (isBuiltInTag(name) || config.isReservedTag(name)) {
    warn(
      'Do not use built-in or reserved HTML elements as component ' +
      'id: ' + name
    );
  }
}

/**
 * Ensure all props option syntax are normalized into the
 * Object-based format.
 */
function normalizeProps (options, vm) {
  var props = options.props;
  if (!props) { return }
  var res = {};
  var i, val, name;
  if (Array.isArray(props)) {
    i = props.length;
    while (i--) {
      val = props[i];
      if (typeof val === 'string') {
        name = camelize(val);
        res[name] = { type: null };
      } else if (true) {
        warn('props must be strings when using array syntax.');
      }
    }
  } else if (isPlainObject(props)) {
    for (var key in props) {
      val = props[key];
      name = camelize(key);
      res[name] = isPlainObject(val)
        ? val
        : { type: val };
    }
  } else if (true) {
    warn(
      "Invalid value for option \"props\": expected an Array or an Object, " +
      "but got " + (toRawType(props)) + ".",
      vm
    );
  }
  options.props = res;
}

/**
 * Normalize all injections into Object-based format
 */
function normalizeInject (options, vm) {
  var inject = options.inject;
  if (!inject) { return }
  var normalized = options.inject = {};
  if (Array.isArray(inject)) {
    for (var i = 0; i < inject.length; i++) {
      normalized[inject[i]] = { from: inject[i] };
    }
  } else if (isPlainObject(inject)) {
    for (var key in inject) {
      var val = inject[key];
      normalized[key] = isPlainObject(val)
        ? extend({ from: key }, val)
        : { from: val };
    }
  } else if (true) {
    warn(
      "Invalid value for option \"inject\": expected an Array or an Object, " +
      "but got " + (toRawType(inject)) + ".",
      vm
    );
  }
}

/**
 * Normalize raw function directives into object format.
 */
function normalizeDirectives (options) {
  var dirs = options.directives;
  if (dirs) {
    for (var key in dirs) {
      var def$$1 = dirs[key];
      if (typeof def$$1 === 'function') {
        dirs[key] = { bind: def$$1, update: def$$1 };
      }
    }
  }
}

function assertObjectType (name, value, vm) {
  if (!isPlainObject(value)) {
    warn(
      "Invalid value for option \"" + name + "\": expected an Object, " +
      "but got " + (toRawType(value)) + ".",
      vm
    );
  }
}

/**
 * Merge two option objects into a new one.
 * Core utility used in both instantiation and inheritance.
 */
function mergeOptions (
  parent,
  child,
  vm
) {
  if (true) {
    checkComponents(child);
  }

  if (typeof child === 'function') {
    child = child.options;
  }

  normalizeProps(child, vm);
  normalizeInject(child, vm);
  normalizeDirectives(child);

  // Apply extends and mixins on the child options,
  // but only if it is a raw options object that isn't
  // the result of another mergeOptions call.
  // Only merged options has the _base property.
  if (!child._base) {
    if (child.extends) {
      parent = mergeOptions(parent, child.extends, vm);
    }
    if (child.mixins) {
      for (var i = 0, l = child.mixins.length; i < l; i++) {
        parent = mergeOptions(parent, child.mixins[i], vm);
      }
    }
  }

  var options = {};
  var key;
  for (key in parent) {
    mergeField(key);
  }
  for (key in child) {
    if (!hasOwn(parent, key)) {
      mergeField(key);
    }
  }
  function mergeField (key) {
    var strat = strats[key] || defaultStrat;
    options[key] = strat(parent[key], child[key], vm, key);
  }
  return options
}

/**
 * Resolve an asset.
 * This function is used because child instances need access
 * to assets defined in its ancestor chain.
 */
function resolveAsset (
  options,
  type,
  id,
  warnMissing
) {
  /* istanbul ignore if */
  if (typeof id !== 'string') {
    return
  }
  var assets = options[type];
  // check local registration variations first
  if (hasOwn(assets, id)) { return assets[id] }
  var camelizedId = camelize(id);
  if (hasOwn(assets, camelizedId)) { return assets[camelizedId] }
  var PascalCaseId = capitalize(camelizedId);
  if (hasOwn(assets, PascalCaseId)) { return assets[PascalCaseId] }
  // fallback to prototype chain
  var res = assets[id] || assets[camelizedId] || assets[PascalCaseId];
  if ( true && warnMissing && !res) {
    warn(
      'Failed to resolve ' + type.slice(0, -1) + ': ' + id,
      options
    );
  }
  return res
}

/*  */



function validateProp (
  key,
  propOptions,
  propsData,
  vm
) {
  var prop = propOptions[key];
  var absent = !hasOwn(propsData, key);
  var value = propsData[key];
  // boolean casting
  var booleanIndex = getTypeIndex(Boolean, prop.type);
  if (booleanIndex > -1) {
    if (absent && !hasOwn(prop, 'default')) {
      value = false;
    } else if (value === '' || value === hyphenate(key)) {
      // only cast empty string / same name to boolean if
      // boolean has higher priority
      var stringIndex = getTypeIndex(String, prop.type);
      if (stringIndex < 0 || booleanIndex < stringIndex) {
        value = true;
      }
    }
  }
  // check default value
  if (value === undefined) {
    value = getPropDefaultValue(vm, prop, key);
    // since the default value is a fresh copy,
    // make sure to observe it.
    var prevShouldObserve = shouldObserve;
    toggleObserving(true);
    observe(value);
    toggleObserving(prevShouldObserve);
  }
  if (
    true
  ) {
    assertProp(prop, key, value, vm, absent);
  }
  return value
}

/**
 * Get the default value of a prop.
 */
function getPropDefaultValue (vm, prop, key) {
  // no default, return undefined
  if (!hasOwn(prop, 'default')) {
    return undefined
  }
  var def = prop.default;
  // warn against non-factory defaults for Object & Array
  if ( true && isObject(def)) {
    warn(
      'Invalid default value for prop "' + key + '": ' +
      'Props with type Object/Array must use a factory function ' +
      'to return the default value.',
      vm
    );
  }
  // the raw prop value was also undefined from previous render,
  // return previous default value to avoid unnecessary watcher trigger
  if (vm && vm.$options.propsData &&
    vm.$options.propsData[key] === undefined &&
    vm._props[key] !== undefined
  ) {
    return vm._props[key]
  }
  // call factory function for non-Function types
  // a value is Function if its prototype is function even across different execution context
  return typeof def === 'function' && getType(prop.type) !== 'Function'
    ? def.call(vm)
    : def
}

/**
 * Assert whether a prop is valid.
 */
function assertProp (
  prop,
  name,
  value,
  vm,
  absent
) {
  if (prop.required && absent) {
    warn(
      'Missing required prop: "' + name + '"',
      vm
    );
    return
  }
  if (value == null && !prop.required) {
    return
  }
  var type = prop.type;
  var valid = !type || type === true;
  var expectedTypes = [];
  if (type) {
    if (!Array.isArray(type)) {
      type = [type];
    }
    for (var i = 0; i < type.length && !valid; i++) {
      var assertedType = assertType(value, type[i]);
      expectedTypes.push(assertedType.expectedType || '');
      valid = assertedType.valid;
    }
  }

  if (!valid) {
    warn(
      getInvalidTypeMessage(name, value, expectedTypes),
      vm
    );
    return
  }
  var validator = prop.validator;
  if (validator) {
    if (!validator(value)) {
      warn(
        'Invalid prop: custom validator check failed for prop "' + name + '".',
        vm
      );
    }
  }
}

var simpleCheckRE = /^(String|Number|Boolean|Function|Symbol)$/;

function assertType (value, type) {
  var valid;
  var expectedType = getType(type);
  if (simpleCheckRE.test(expectedType)) {
    var t = typeof value;
    valid = t === expectedType.toLowerCase();
    // for primitive wrapper objects
    if (!valid && t === 'object') {
      valid = value instanceof type;
    }
  } else if (expectedType === 'Object') {
    valid = isPlainObject(value);
  } else if (expectedType === 'Array') {
    valid = Array.isArray(value);
  } else {
    valid = value instanceof type;
  }
  return {
    valid: valid,
    expectedType: expectedType
  }
}

/**
 * Use function string name to check built-in types,
 * because a simple equality check will fail when running
 * across different vms / iframes.
 */
function getType (fn) {
  var match = fn && fn.toString().match(/^\s*function (\w+)/);
  return match ? match[1] : ''
}

function isSameType (a, b) {
  return getType(a) === getType(b)
}

function getTypeIndex (type, expectedTypes) {
  if (!Array.isArray(expectedTypes)) {
    return isSameType(expectedTypes, type) ? 0 : -1
  }
  for (var i = 0, len = expectedTypes.length; i < len; i++) {
    if (isSameType(expectedTypes[i], type)) {
      return i
    }
  }
  return -1
}

function getInvalidTypeMessage (name, value, expectedTypes) {
  var message = "Invalid prop: type check failed for prop \"" + name + "\"." +
    " Expected " + (expectedTypes.map(capitalize).join(', '));
  var expectedType = expectedTypes[0];
  var receivedType = toRawType(value);
  var expectedValue = styleValue(value, expectedType);
  var receivedValue = styleValue(value, receivedType);
  // check if we need to specify expected value
  if (expectedTypes.length === 1 &&
      isExplicable(expectedType) &&
      !isBoolean(expectedType, receivedType)) {
    message += " with value " + expectedValue;
  }
  message += ", got " + receivedType + " ";
  // check if we need to specify received value
  if (isExplicable(receivedType)) {
    message += "with value " + receivedValue + ".";
  }
  return message
}

function styleValue (value, type) {
  if (type === 'String') {
    return ("\"" + value + "\"")
  } else if (type === 'Number') {
    return ("" + (Number(value)))
  } else {
    return ("" + value)
  }
}

function isExplicable (value) {
  var explicitTypes = ['string', 'number', 'boolean'];
  return explicitTypes.some(function (elem) { return value.toLowerCase() === elem; })
}

function isBoolean () {
  var args = [], len = arguments.length;
  while ( len-- ) args[ len ] = arguments[ len ];

  return args.some(function (elem) { return elem.toLowerCase() === 'boolean'; })
}

/*  */

function handleError (err, vm, info) {
  // Deactivate deps tracking while processing error handler to avoid possible infinite rendering.
  // See: https://github.com/vuejs/vuex/issues/1505
  pushTarget();
  try {
    if (vm) {
      var cur = vm;
      while ((cur = cur.$parent)) {
        var hooks = cur.$options.errorCaptured;
        if (hooks) {
          for (var i = 0; i < hooks.length; i++) {
            try {
              var capture = hooks[i].call(cur, err, vm, info) === false;
              if (capture) { return }
            } catch (e) {
              globalHandleError(e, cur, 'errorCaptured hook');
            }
          }
        }
      }
    }
    globalHandleError(err, vm, info);
  } finally {
    popTarget();
  }
}

function invokeWithErrorHandling (
  handler,
  context,
  args,
  vm,
  info
) {
  var res;
  try {
    res = args ? handler.apply(context, args) : handler.call(context);
    if (res && !res._isVue && isPromise(res) && !res._handled) {
      res.catch(function (e) { return handleError(e, vm, info + " (Promise/async)"); });
      // issue #9511
      // avoid catch triggering multiple times when nested calls
      res._handled = true;
    }
  } catch (e) {
    handleError(e, vm, info);
  }
  return res
}

function globalHandleError (err, vm, info) {
  if (config.errorHandler) {
    try {
      return config.errorHandler.call(null, err, vm, info)
    } catch (e) {
      // if the user intentionally throws the original error in the handler,
      // do not log it twice
      if (e !== err) {
        logError(e, null, 'config.errorHandler');
      }
    }
  }
  logError(err, vm, info);
}

function logError (err, vm, info) {
  if (true) {
    warn(("Error in " + info + ": \"" + (err.toString()) + "\""), vm);
  }
  /* istanbul ignore else */
  if ((inBrowser || inWeex) && typeof console !== 'undefined') {
    console.error(err);
  } else {
    throw err
  }
}

/*  */

var callbacks = [];
var pending = false;

function flushCallbacks () {
  pending = false;
  var copies = callbacks.slice(0);
  callbacks.length = 0;
  for (var i = 0; i < copies.length; i++) {
    copies[i]();
  }
}

// Here we have async deferring wrappers using microtasks.
// In 2.5 we used (macro) tasks (in combination with microtasks).
// However, it has subtle problems when state is changed right before repaint
// (e.g. #6813, out-in transitions).
// Also, using (macro) tasks in event handler would cause some weird behaviors
// that cannot be circumvented (e.g. #7109, #7153, #7546, #7834, #8109).
// So we now use microtasks everywhere, again.
// A major drawback of this tradeoff is that there are some scenarios
// where microtasks have too high a priority and fire in between supposedly
// sequential events (e.g. #4521, #6690, which have workarounds)
// or even between bubbling of the same event (#6566).
var timerFunc;

// The nextTick behavior leverages the microtask queue, which can be accessed
// via either native Promise.then or MutationObserver.
// MutationObserver has wider support, however it is seriously bugged in
// UIWebView in iOS >= 9.3.3 when triggered in touch event handlers. It
// completely stops working after triggering a few times... so, if native
// Promise is available, we will use it:
/* istanbul ignore next, $flow-disable-line */
if (typeof Promise !== 'undefined' && isNative(Promise)) {
  var p = Promise.resolve();
  timerFunc = function () {
    p.then(flushCallbacks);
    // In problematic UIWebViews, Promise.then doesn't completely break, but
    // it can get stuck in a weird state where callbacks are pushed into the
    // microtask queue but the queue isn't being flushed, until the browser
    // needs to do some other work, e.g. handle a timer. Therefore we can
    // "force" the microtask queue to be flushed by adding an empty timer.
    if (isIOS) { setTimeout(noop); }
  };
} else if (!isIE && typeof MutationObserver !== 'undefined' && (
  isNative(MutationObserver) ||
  // PhantomJS and iOS 7.x
  MutationObserver.toString() === '[object MutationObserverConstructor]'
)) {
  // Use MutationObserver where native Promise is not available,
  // e.g. PhantomJS, iOS7, Android 4.4
  // (#6466 MutationObserver is unreliable in IE11)
  var counter = 1;
  var observer = new MutationObserver(flushCallbacks);
  var textNode = document.createTextNode(String(counter));
  observer.observe(textNode, {
    characterData: true
  });
  timerFunc = function () {
    counter = (counter + 1) % 2;
    textNode.data = String(counter);
  };
} else if (typeof setImmediate !== 'undefined' && isNative(setImmediate)) {
  // Fallback to setImmediate.
  // Techinically it leverages the (macro) task queue,
  // but it is still a better choice than setTimeout.
  timerFunc = function () {
    setImmediate(flushCallbacks);
  };
} else {
  // Fallback to setTimeout.
  timerFunc = function () {
    setTimeout(flushCallbacks, 0);
  };
}

function nextTick (cb, ctx) {
  var _resolve;
  callbacks.push(function () {
    if (cb) {
      try {
        cb.call(ctx);
      } catch (e) {
        handleError(e, ctx, 'nextTick');
      }
    } else if (_resolve) {
      _resolve(ctx);
    }
  });
  if (!pending) {
    pending = true;
    timerFunc();
  }
  // $flow-disable-line
  if (!cb && typeof Promise !== 'undefined') {
    return new Promise(function (resolve) {
      _resolve = resolve;
    })
  }
}

/*  */

/* not type checking this file because flow doesn't play well with Proxy */

var initProxy;

if (true) {
  var allowedGlobals = makeMap(
    'Infinity,undefined,NaN,isFinite,isNaN,' +
    'parseFloat,parseInt,decodeURI,decodeURIComponent,encodeURI,encodeURIComponent,' +
    'Math,Number,Date,Array,Object,Boolean,String,RegExp,Map,Set,JSON,Intl,' +
    'require' // for Webpack/Browserify
  );

  var warnNonPresent = function (target, key) {
    warn(
      "Property or method \"" + key + "\" is not defined on the instance but " +
      'referenced during render. Make sure that this property is reactive, ' +
      'either in the data option, or for class-based components, by ' +
      'initializing the property. ' +
      'See: https://vuejs.org/v2/guide/reactivity.html#Declaring-Reactive-Properties.',
      target
    );
  };

  var warnReservedPrefix = function (target, key) {
    warn(
      "Property \"" + key + "\" must be accessed with \"$data." + key + "\" because " +
      'properties starting with "$" or "_" are not proxied in the Vue instance to ' +
      'prevent conflicts with Vue internals' +
      'See: https://vuejs.org/v2/api/#data',
      target
    );
  };

  var hasProxy =
    typeof Proxy !== 'undefined' && isNative(Proxy);

  if (hasProxy) {
    var isBuiltInModifier = makeMap('stop,prevent,self,ctrl,shift,alt,meta,exact');
    config.keyCodes = new Proxy(config.keyCodes, {
      set: function set (target, key, value) {
        if (isBuiltInModifier(key)) {
          warn(("Avoid overwriting built-in modifier in config.keyCodes: ." + key));
          return false
        } else {
          target[key] = value;
          return true
        }
      }
    });
  }

  var hasHandler = {
    has: function has (target, key) {
      var has = key in target;
      var isAllowed = allowedGlobals(key) ||
        (typeof key === 'string' && key.charAt(0) === '_' && !(key in target.$data));
      if (!has && !isAllowed) {
        if (key in target.$data) { warnReservedPrefix(target, key); }
        else { warnNonPresent(target, key); }
      }
      return has || !isAllowed
    }
  };

  var getHandler = {
    get: function get (target, key) {
      if (typeof key === 'string' && !(key in target)) {
        if (key in target.$data) { warnReservedPrefix(target, key); }
        else { warnNonPresent(target, key); }
      }
      return target[key]
    }
  };

  initProxy = function initProxy (vm) {
    if (hasProxy) {
      // determine which proxy handler to use
      var options = vm.$options;
      var handlers = options.render && options.render._withStripped
        ? getHandler
        : hasHandler;
      vm._renderProxy = new Proxy(vm, handlers);
    } else {
      vm._renderProxy = vm;
    }
  };
}

/*  */

var seenObjects = new _Set();

/**
 * Recursively traverse an object to evoke all converted
 * getters, so that every nested property inside the object
 * is collected as a "deep" dependency.
 */
function traverse (val) {
  _traverse(val, seenObjects);
  seenObjects.clear();
}

function _traverse (val, seen) {
  var i, keys;
  var isA = Array.isArray(val);
  if ((!isA && !isObject(val)) || Object.isFrozen(val) || val instanceof VNode) {
    return
  }
  if (val.__ob__) {
    var depId = val.__ob__.dep.id;
    if (seen.has(depId)) {
      return
    }
    seen.add(depId);
  }
  if (isA) {
    i = val.length;
    while (i--) { _traverse(val[i], seen); }
  } else {
    keys = Object.keys(val);
    i = keys.length;
    while (i--) { _traverse(val[keys[i]], seen); }
  }
}

var mark;
var measure;

if (true) {
  var perf = inBrowser && window.performance;
  /* istanbul ignore if */
  if (
    perf &&
    perf.mark &&
    perf.measure &&
    perf.clearMarks &&
    perf.clearMeasures
  ) {
    mark = function (tag) { return perf.mark(tag); };
    measure = function (name, startTag, endTag) {
      perf.measure(name, startTag, endTag);
      perf.clearMarks(startTag);
      perf.clearMarks(endTag);
      // perf.clearMeasures(name)
    };
  }
}

/*  */

var normalizeEvent = cached(function (name) {
  var passive = name.charAt(0) === '&';
  name = passive ? name.slice(1) : name;
  var once$$1 = name.charAt(0) === '~'; // Prefixed last, checked first
  name = once$$1 ? name.slice(1) : name;
  var capture = name.charAt(0) === '!';
  name = capture ? name.slice(1) : name;
  return {
    name: name,
    once: once$$1,
    capture: capture,
    passive: passive
  }
});

function createFnInvoker (fns, vm) {
  function invoker () {
    var arguments$1 = arguments;

    var fns = invoker.fns;
    if (Array.isArray(fns)) {
      var cloned = fns.slice();
      for (var i = 0; i < cloned.length; i++) {
        invokeWithErrorHandling(cloned[i], null, arguments$1, vm, "v-on handler");
      }
    } else {
      // return handler return value for single handlers
      return invokeWithErrorHandling(fns, null, arguments, vm, "v-on handler")
    }
  }
  invoker.fns = fns;
  return invoker
}

function updateListeners (
  on,
  oldOn,
  add,
  remove$$1,
  createOnceHandler,
  vm
) {
  var name, def$$1, cur, old, event;
  for (name in on) {
    def$$1 = cur = on[name];
    old = oldOn[name];
    event = normalizeEvent(name);
    if (isUndef(cur)) {
       true && warn(
        "Invalid handler for event \"" + (event.name) + "\": got " + String(cur),
        vm
      );
    } else if (isUndef(old)) {
      if (isUndef(cur.fns)) {
        cur = on[name] = createFnInvoker(cur, vm);
      }
      if (isTrue(event.once)) {
        cur = on[name] = createOnceHandler(event.name, cur, event.capture);
      }
      add(event.name, cur, event.capture, event.passive, event.params);
    } else if (cur !== old) {
      old.fns = cur;
      on[name] = old;
    }
  }
  for (name in oldOn) {
    if (isUndef(on[name])) {
      event = normalizeEvent(name);
      remove$$1(event.name, oldOn[name], event.capture);
    }
  }
}

/*  */

/*  */

function extractPropsFromVNodeData (
  data,
  Ctor,
  tag
) {
  // we are only extracting raw values here.
  // validation and default values are handled in the child
  // component itself.
  var propOptions = Ctor.options.props;
  if (isUndef(propOptions)) {
    return
  }
  var res = {};
  var attrs = data.attrs;
  var props = data.props;
  if (isDef(attrs) || isDef(props)) {
    for (var key in propOptions) {
      var altKey = hyphenate(key);
      if (true) {
        var keyInLowerCase = key.toLowerCase();
        if (
          key !== keyInLowerCase &&
          attrs && hasOwn(attrs, keyInLowerCase)
        ) {
          tip(
            "Prop \"" + keyInLowerCase + "\" is passed to component " +
            (formatComponentName(tag || Ctor)) + ", but the declared prop name is" +
            " \"" + key + "\". " +
            "Note that HTML attributes are case-insensitive and camelCased " +
            "props need to use their kebab-case equivalents when using in-DOM " +
            "templates. You should probably use \"" + altKey + "\" instead of \"" + key + "\"."
          );
        }
      }
      checkProp(res, props, key, altKey, true) ||
      checkProp(res, attrs, key, altKey, false);
    }
  }
  return res
}

function checkProp (
  res,
  hash,
  key,
  altKey,
  preserve
) {
  if (isDef(hash)) {
    if (hasOwn(hash, key)) {
      res[key] = hash[key];
      if (!preserve) {
        delete hash[key];
      }
      return true
    } else if (hasOwn(hash, altKey)) {
      res[key] = hash[altKey];
      if (!preserve) {
        delete hash[altKey];
      }
      return true
    }
  }
  return false
}

/*  */

// The template compiler attempts to minimize the need for normalization by
// statically analyzing the template at compile time.
//
// For plain HTML markup, normalization can be completely skipped because the
// generated render function is guaranteed to return Array<VNode>. There are
// two cases where extra normalization is needed:

// 1. When the children contains components - because a functional component
// may return an Array instead of a single root. In this case, just a simple
// normalization is needed - if any child is an Array, we flatten the whole
// thing with Array.prototype.concat. It is guaranteed to be only 1-level deep
// because functional components already normalize their own children.
function simpleNormalizeChildren (children) {
  for (var i = 0; i < children.length; i++) {
    if (Array.isArray(children[i])) {
      return Array.prototype.concat.apply([], children)
    }
  }
  return children
}

// 2. When the children contains constructs that always generated nested Arrays,
// e.g. <template>, <slot>, v-for, or when the children is provided by user
// with hand-written render functions / JSX. In such cases a full normalization
// is needed to cater to all possible types of children values.
function normalizeChildren (children) {
  return isPrimitive(children)
    ? [createTextVNode(children)]
    : Array.isArray(children)
      ? normalizeArrayChildren(children)
      : undefined
}

function isTextNode (node) {
  return isDef(node) && isDef(node.text) && isFalse(node.isComment)
}

function normalizeArrayChildren (children, nestedIndex) {
  var res = [];
  var i, c, lastIndex, last;
  for (i = 0; i < children.length; i++) {
    c = children[i];
    if (isUndef(c) || typeof c === 'boolean') { continue }
    lastIndex = res.length - 1;
    last = res[lastIndex];
    //  nested
    if (Array.isArray(c)) {
      if (c.length > 0) {
        c = normalizeArrayChildren(c, ((nestedIndex || '') + "_" + i));
        // merge adjacent text nodes
        if (isTextNode(c[0]) && isTextNode(last)) {
          res[lastIndex] = createTextVNode(last.text + (c[0]).text);
          c.shift();
        }
        res.push.apply(res, c);
      }
    } else if (isPrimitive(c)) {
      if (isTextNode(last)) {
        // merge adjacent text nodes
        // this is necessary for SSR hydration because text nodes are
        // essentially merged when rendered to HTML strings
        res[lastIndex] = createTextVNode(last.text + c);
      } else if (c !== '') {
        // convert primitive to vnode
        res.push(createTextVNode(c));
      }
    } else {
      if (isTextNode(c) && isTextNode(last)) {
        // merge adjacent text nodes
        res[lastIndex] = createTextVNode(last.text + c.text);
      } else {
        // default key for nested array children (likely generated by v-for)
        if (isTrue(children._isVList) &&
          isDef(c.tag) &&
          isUndef(c.key) &&
          isDef(nestedIndex)) {
          c.key = "__vlist" + nestedIndex + "_" + i + "__";
        }
        res.push(c);
      }
    }
  }
  return res
}

/*  */

function initProvide (vm) {
  var provide = vm.$options.provide;
  if (provide) {
    vm._provided = typeof provide === 'function'
      ? provide.call(vm)
      : provide;
  }
}

function initInjections (vm) {
  var result = resolveInject(vm.$options.inject, vm);
  if (result) {
    toggleObserving(false);
    Object.keys(result).forEach(function (key) {
      /* istanbul ignore else */
      if (true) {
        defineReactive$$1(vm, key, result[key], function () {
          warn(
            "Avoid mutating an injected value directly since the changes will be " +
            "overwritten whenever the provided component re-renders. " +
            "injection being mutated: \"" + key + "\"",
            vm
          );
        });
      } else {}
    });
    toggleObserving(true);
  }
}

function resolveInject (inject, vm) {
  if (inject) {
    // inject is :any because flow is not smart enough to figure out cached
    var result = Object.create(null);
    var keys = hasSymbol
      ? Reflect.ownKeys(inject)
      : Object.keys(inject);

    for (var i = 0; i < keys.length; i++) {
      var key = keys[i];
      // #6574 in case the inject object is observed...
      if (key === '__ob__') { continue }
      var provideKey = inject[key].from;
      var source = vm;
      while (source) {
        if (source._provided && hasOwn(source._provided, provideKey)) {
          result[key] = source._provided[provideKey];
          break
        }
        source = source.$parent;
      }
      if (!source) {
        if ('default' in inject[key]) {
          var provideDefault = inject[key].default;
          result[key] = typeof provideDefault === 'function'
            ? provideDefault.call(vm)
            : provideDefault;
        } else if (true) {
          warn(("Injection \"" + key + "\" not found"), vm);
        }
      }
    }
    return result
  }
}

/*  */



/**
 * Runtime helper for resolving raw children VNodes into a slot object.
 */
function resolveSlots (
  children,
  context
) {
  if (!children || !children.length) {
    return {}
  }
  var slots = {};
  for (var i = 0, l = children.length; i < l; i++) {
    var child = children[i];
    var data = child.data;
    // remove slot attribute if the node is resolved as a Vue slot node
    if (data && data.attrs && data.attrs.slot) {
      delete data.attrs.slot;
    }
    // named slots should only be respected if the vnode was rendered in the
    // same context.
    if ((child.context === context || child.fnContext === context) &&
      data && data.slot != null
    ) {
      var name = data.slot;
      var slot = (slots[name] || (slots[name] = []));
      if (child.tag === 'template') {
        slot.push.apply(slot, child.children || []);
      } else {
        slot.push(child);
      }
    } else {
      (slots.default || (slots.default = [])).push(child);
    }
  }
  // ignore slots that contains only whitespace
  for (var name$1 in slots) {
    if (slots[name$1].every(isWhitespace)) {
      delete slots[name$1];
    }
  }
  return slots
}

function isWhitespace (node) {
  return (node.isComment && !node.asyncFactory) || node.text === ' '
}

/*  */

function normalizeScopedSlots (
  slots,
  normalSlots,
  prevSlots
) {
  var res;
  var hasNormalSlots = Object.keys(normalSlots).length > 0;
  var isStable = slots ? !!slots.$stable : !hasNormalSlots;
  var key = slots && slots.$key;
  if (!slots) {
    res = {};
  } else if (slots._normalized) {
    // fast path 1: child component re-render only, parent did not change
    return slots._normalized
  } else if (
    isStable &&
    prevSlots &&
    prevSlots !== emptyObject &&
    key === prevSlots.$key &&
    !hasNormalSlots &&
    !prevSlots.$hasNormal
  ) {
    // fast path 2: stable scoped slots w/ no normal slots to proxy,
    // only need to normalize once
    return prevSlots
  } else {
    res = {};
    for (var key$1 in slots) {
      if (slots[key$1] && key$1[0] !== '$') {
        res[key$1] = normalizeScopedSlot(normalSlots, key$1, slots[key$1]);
      }
    }
  }
  // expose normal slots on scopedSlots
  for (var key$2 in normalSlots) {
    if (!(key$2 in res)) {
      res[key$2] = proxyNormalSlot(normalSlots, key$2);
    }
  }
  // avoriaz seems to mock a non-extensible $scopedSlots object
  // and when that is passed down this would cause an error
  if (slots && Object.isExtensible(slots)) {
    (slots)._normalized = res;
  }
  def(res, '$stable', isStable);
  def(res, '$key', key);
  def(res, '$hasNormal', hasNormalSlots);
  return res
}

function normalizeScopedSlot(normalSlots, key, fn) {
  var normalized = function () {
    var res = arguments.length ? fn.apply(null, arguments) : fn({});
    res = res && typeof res === 'object' && !Array.isArray(res)
      ? [res] // single vnode
      : normalizeChildren(res);
    return res && (
      res.length === 0 ||
      (res.length === 1 && res[0].isComment) // #9658
    ) ? undefined
      : res
  };
  // this is a slot using the new v-slot syntax without scope. although it is
  // compiled as a scoped slot, render fn users would expect it to be present
  // on this.$slots because the usage is semantically a normal slot.
  if (fn.proxy) {
    Object.defineProperty(normalSlots, key, {
      get: normalized,
      enumerable: true,
      configurable: true
    });
  }
  return normalized
}

function proxyNormalSlot(slots, key) {
  return function () { return slots[key]; }
}

/*  */

/**
 * Runtime helper for rendering v-for lists.
 */
function renderList (
  val,
  render
) {
  var ret, i, l, keys, key;
  if (Array.isArray(val) || typeof val === 'string') {
    ret = new Array(val.length);
    for (i = 0, l = val.length; i < l; i++) {
      ret[i] = render(val[i], i);
    }
  } else if (typeof val === 'number') {
    ret = new Array(val);
    for (i = 0; i < val; i++) {
      ret[i] = render(i + 1, i);
    }
  } else if (isObject(val)) {
    if (hasSymbol && val[Symbol.iterator]) {
      ret = [];
      var iterator = val[Symbol.iterator]();
      var result = iterator.next();
      while (!result.done) {
        ret.push(render(result.value, ret.length));
        result = iterator.next();
      }
    } else {
      keys = Object.keys(val);
      ret = new Array(keys.length);
      for (i = 0, l = keys.length; i < l; i++) {
        key = keys[i];
        ret[i] = render(val[key], key, i);
      }
    }
  }
  if (!isDef(ret)) {
    ret = [];
  }
  (ret)._isVList = true;
  return ret
}

/*  */

/**
 * Runtime helper for rendering <slot>
 */
function renderSlot (
  name,
  fallback,
  props,
  bindObject
) {
  var scopedSlotFn = this.$scopedSlots[name];
  var nodes;
  if (scopedSlotFn) { // scoped slot
    props = props || {};
    if (bindObject) {
      if ( true && !isObject(bindObject)) {
        warn(
          'slot v-bind without argument expects an Object',
          this
        );
      }
      props = extend(extend({}, bindObject), props);
    }
    nodes = scopedSlotFn(props) || fallback;
  } else {
    nodes = this.$slots[name] || fallback;
  }

  var target = props && props.slot;
  if (target) {
    return this.$createElement('template', { slot: target }, nodes)
  } else {
    return nodes
  }
}

/*  */

/**
 * Runtime helper for resolving filters
 */
function resolveFilter (id) {
  return resolveAsset(this.$options, 'filters', id, true) || identity
}

/*  */

function isKeyNotMatch (expect, actual) {
  if (Array.isArray(expect)) {
    return expect.indexOf(actual) === -1
  } else {
    return expect !== actual
  }
}

/**
 * Runtime helper for checking keyCodes from config.
 * exposed as Vue.prototype._k
 * passing in eventKeyName as last argument separately for backwards compat
 */
function checkKeyCodes (
  eventKeyCode,
  key,
  builtInKeyCode,
  eventKeyName,
  builtInKeyName
) {
  var mappedKeyCode = config.keyCodes[key] || builtInKeyCode;
  if (builtInKeyName && eventKeyName && !config.keyCodes[key]) {
    return isKeyNotMatch(builtInKeyName, eventKeyName)
  } else if (mappedKeyCode) {
    return isKeyNotMatch(mappedKeyCode, eventKeyCode)
  } else if (eventKeyName) {
    return hyphenate(eventKeyName) !== key
  }
}

/*  */

/**
 * Runtime helper for merging v-bind="object" into a VNode's data.
 */
function bindObjectProps (
  data,
  tag,
  value,
  asProp,
  isSync
) {
  if (value) {
    if (!isObject(value)) {
       true && warn(
        'v-bind without argument expects an Object or Array value',
        this
      );
    } else {
      if (Array.isArray(value)) {
        value = toObject(value);
      }
      var hash;
      var loop = function ( key ) {
        if (
          key === 'class' ||
          key === 'style' ||
          isReservedAttribute(key)
        ) {
          hash = data;
        } else {
          var type = data.attrs && data.attrs.type;
          hash = asProp || config.mustUseProp(tag, type, key)
            ? data.domProps || (data.domProps = {})
            : data.attrs || (data.attrs = {});
        }
        var camelizedKey = camelize(key);
        var hyphenatedKey = hyphenate(key);
        if (!(camelizedKey in hash) && !(hyphenatedKey in hash)) {
          hash[key] = value[key];

          if (isSync) {
            var on = data.on || (data.on = {});
            on[("update:" + key)] = function ($event) {
              value[key] = $event;
            };
          }
        }
      };

      for (var key in value) loop( key );
    }
  }
  return data
}

/*  */

/**
 * Runtime helper for rendering static trees.
 */
function renderStatic (
  index,
  isInFor
) {
  var cached = this._staticTrees || (this._staticTrees = []);
  var tree = cached[index];
  // if has already-rendered static tree and not inside v-for,
  // we can reuse the same tree.
  if (tree && !isInFor) {
    return tree
  }
  // otherwise, render a fresh tree.
  tree = cached[index] = this.$options.staticRenderFns[index].call(
    this._renderProxy,
    null,
    this // for render fns generated for functional component templates
  );
  markStatic(tree, ("__static__" + index), false);
  return tree
}

/**
 * Runtime helper for v-once.
 * Effectively it means marking the node as static with a unique key.
 */
function markOnce (
  tree,
  index,
  key
) {
  markStatic(tree, ("__once__" + index + (key ? ("_" + key) : "")), true);
  return tree
}

function markStatic (
  tree,
  key,
  isOnce
) {
  if (Array.isArray(tree)) {
    for (var i = 0; i < tree.length; i++) {
      if (tree[i] && typeof tree[i] !== 'string') {
        markStaticNode(tree[i], (key + "_" + i), isOnce);
      }
    }
  } else {
    markStaticNode(tree, key, isOnce);
  }
}

function markStaticNode (node, key, isOnce) {
  node.isStatic = true;
  node.key = key;
  node.isOnce = isOnce;
}

/*  */

function bindObjectListeners (data, value) {
  if (value) {
    if (!isPlainObject(value)) {
       true && warn(
        'v-on without argument expects an Object value',
        this
      );
    } else {
      var on = data.on = data.on ? extend({}, data.on) : {};
      for (var key in value) {
        var existing = on[key];
        var ours = value[key];
        on[key] = existing ? [].concat(existing, ours) : ours;
      }
    }
  }
  return data
}

/*  */

function resolveScopedSlots (
  fns, // see flow/vnode
  res,
  // the following are added in 2.6
  hasDynamicKeys,
  contentHashKey
) {
  res = res || { $stable: !hasDynamicKeys };
  for (var i = 0; i < fns.length; i++) {
    var slot = fns[i];
    if (Array.isArray(slot)) {
      resolveScopedSlots(slot, res, hasDynamicKeys);
    } else if (slot) {
      // marker for reverse proxying v-slot without scope on this.$slots
      if (slot.proxy) {
        slot.fn.proxy = true;
      }
      res[slot.key] = slot.fn;
    }
  }
  if (contentHashKey) {
    (res).$key = contentHashKey;
  }
  return res
}

/*  */

function bindDynamicKeys (baseObj, values) {
  for (var i = 0; i < values.length; i += 2) {
    var key = values[i];
    if (typeof key === 'string' && key) {
      baseObj[values[i]] = values[i + 1];
    } else if ( true && key !== '' && key !== null) {
      // null is a speical value for explicitly removing a binding
      warn(
        ("Invalid value for dynamic directive argument (expected string or null): " + key),
        this
      );
    }
  }
  return baseObj
}

// helper to dynamically append modifier runtime markers to event names.
// ensure only append when value is already string, otherwise it will be cast
// to string and cause the type check to miss.
function prependModifier (value, symbol) {
  return typeof value === 'string' ? symbol + value : value
}

/*  */

function installRenderHelpers (target) {
  target._o = markOnce;
  target._n = toNumber;
  target._s = toString;
  target._l = renderList;
  target._t = renderSlot;
  target._q = looseEqual;
  target._i = looseIndexOf;
  target._m = renderStatic;
  target._f = resolveFilter;
  target._k = checkKeyCodes;
  target._b = bindObjectProps;
  target._v = createTextVNode;
  target._e = createEmptyVNode;
  target._u = resolveScopedSlots;
  target._g = bindObjectListeners;
  target._d = bindDynamicKeys;
  target._p = prependModifier;
}

/*  */

function FunctionalRenderContext (
  data,
  props,
  children,
  parent,
  Ctor
) {
  var this$1 = this;

  var options = Ctor.options;
  // ensure the createElement function in functional components
  // gets a unique context - this is necessary for correct named slot check
  var contextVm;
  if (hasOwn(parent, '_uid')) {
    contextVm = Object.create(parent);
    // $flow-disable-line
    contextVm._original = parent;
  } else {
    // the context vm passed in is a functional context as well.
    // in this case we want to make sure we are able to get a hold to the
    // real context instance.
    contextVm = parent;
    // $flow-disable-line
    parent = parent._original;
  }
  var isCompiled = isTrue(options._compiled);
  var needNormalization = !isCompiled;

  this.data = data;
  this.props = props;
  this.children = children;
  this.parent = parent;
  this.listeners = data.on || emptyObject;
  this.injections = resolveInject(options.inject, parent);
  this.slots = function () {
    if (!this$1.$slots) {
      normalizeScopedSlots(
        data.scopedSlots,
        this$1.$slots = resolveSlots(children, parent)
      );
    }
    return this$1.$slots
  };

  Object.defineProperty(this, 'scopedSlots', ({
    enumerable: true,
    get: function get () {
      return normalizeScopedSlots(data.scopedSlots, this.slots())
    }
  }));

  // support for compiled functional template
  if (isCompiled) {
    // exposing $options for renderStatic()
    this.$options = options;
    // pre-resolve slots for renderSlot()
    this.$slots = this.slots();
    this.$scopedSlots = normalizeScopedSlots(data.scopedSlots, this.$slots);
  }

  if (options._scopeId) {
    this._c = function (a, b, c, d) {
      var vnode = createElement(contextVm, a, b, c, d, needNormalization);
      if (vnode && !Array.isArray(vnode)) {
        vnode.fnScopeId = options._scopeId;
        vnode.fnContext = parent;
      }
      return vnode
    };
  } else {
    this._c = function (a, b, c, d) { return createElement(contextVm, a, b, c, d, needNormalization); };
  }
}

installRenderHelpers(FunctionalRenderContext.prototype);

function createFunctionalComponent (
  Ctor,
  propsData,
  data,
  contextVm,
  children
) {
  var options = Ctor.options;
  var props = {};
  var propOptions = options.props;
  if (isDef(propOptions)) {
    for (var key in propOptions) {
      props[key] = validateProp(key, propOptions, propsData || emptyObject);
    }
  } else {
    if (isDef(data.attrs)) { mergeProps(props, data.attrs); }
    if (isDef(data.props)) { mergeProps(props, data.props); }
  }

  var renderContext = new FunctionalRenderContext(
    data,
    props,
    children,
    contextVm,
    Ctor
  );

  var vnode = options.render.call(null, renderContext._c, renderContext);

  if (vnode instanceof VNode) {
    return cloneAndMarkFunctionalResult(vnode, data, renderContext.parent, options, renderContext)
  } else if (Array.isArray(vnode)) {
    var vnodes = normalizeChildren(vnode) || [];
    var res = new Array(vnodes.length);
    for (var i = 0; i < vnodes.length; i++) {
      res[i] = cloneAndMarkFunctionalResult(vnodes[i], data, renderContext.parent, options, renderContext);
    }
    return res
  }
}

function cloneAndMarkFunctionalResult (vnode, data, contextVm, options, renderContext) {
  // #7817 clone node before setting fnContext, otherwise if the node is reused
  // (e.g. it was from a cached normal slot) the fnContext causes named slots
  // that should not be matched to match.
  var clone = cloneVNode(vnode);
  clone.fnContext = contextVm;
  clone.fnOptions = options;
  if (true) {
    (clone.devtoolsMeta = clone.devtoolsMeta || {}).renderContext = renderContext;
  }
  if (data.slot) {
    (clone.data || (clone.data = {})).slot = data.slot;
  }
  return clone
}

function mergeProps (to, from) {
  for (var key in from) {
    to[camelize(key)] = from[key];
  }
}

/*  */

/*  */

/*  */

/*  */

// inline hooks to be invoked on component VNodes during patch
var componentVNodeHooks = {
  init: function init (vnode, hydrating) {
    if (
      vnode.componentInstance &&
      !vnode.componentInstance._isDestroyed &&
      vnode.data.keepAlive
    ) {
      // kept-alive components, treat as a patch
      var mountedNode = vnode; // work around flow
      componentVNodeHooks.prepatch(mountedNode, mountedNode);
    } else {
      var child = vnode.componentInstance = createComponentInstanceForVnode(
        vnode,
        activeInstance
      );
      child.$mount(hydrating ? vnode.elm : undefined, hydrating);
    }
  },

  prepatch: function prepatch (oldVnode, vnode) {
    var options = vnode.componentOptions;
    var child = vnode.componentInstance = oldVnode.componentInstance;
    updateChildComponent(
      child,
      options.propsData, // updated props
      options.listeners, // updated listeners
      vnode, // new parent vnode
      options.children // new children
    );
  },

  insert: function insert (vnode) {
    var context = vnode.context;
    var componentInstance = vnode.componentInstance;
    if (!componentInstance._isMounted) {
      componentInstance._isMounted = true;
      callHook(componentInstance, 'mounted');
    }
    if (vnode.data.keepAlive) {
      if (context._isMounted) {
        // vue-router#1212
        // During updates, a kept-alive component's child components may
        // change, so directly walking the tree here may call activated hooks
        // on incorrect children. Instead we push them into a queue which will
        // be processed after the whole patch process ended.
        queueActivatedComponent(componentInstance);
      } else {
        activateChildComponent(componentInstance, true /* direct */);
      }
    }
  },

  destroy: function destroy (vnode) {
    var componentInstance = vnode.componentInstance;
    if (!componentInstance._isDestroyed) {
      if (!vnode.data.keepAlive) {
        componentInstance.$destroy();
      } else {
        deactivateChildComponent(componentInstance, true /* direct */);
      }
    }
  }
};

var hooksToMerge = Object.keys(componentVNodeHooks);

function createComponent (
  Ctor,
  data,
  context,
  children,
  tag
) {
  if (isUndef(Ctor)) {
    return
  }

  var baseCtor = context.$options._base;

  // plain options object: turn it into a constructor
  if (isObject(Ctor)) {
    Ctor = baseCtor.extend(Ctor);
  }

  // if at this stage it's not a constructor or an async component factory,
  // reject.
  if (typeof Ctor !== 'function') {
    if (true) {
      warn(("Invalid Component definition: " + (String(Ctor))), context);
    }
    return
  }

  // async component
  var asyncFactory;
  if (isUndef(Ctor.cid)) {
    asyncFactory = Ctor;
    Ctor = resolveAsyncComponent(asyncFactory, baseCtor);
    if (Ctor === undefined) {
      // return a placeholder node for async component, which is rendered
      // as a comment node but preserves all the raw information for the node.
      // the information will be used for async server-rendering and hydration.
      return createAsyncPlaceholder(
        asyncFactory,
        data,
        context,
        children,
        tag
      )
    }
  }

  data = data || {};

  // resolve constructor options in case global mixins are applied after
  // component constructor creation
  resolveConstructorOptions(Ctor);

  // transform component v-model data into props & events
  if (isDef(data.model)) {
    transformModel(Ctor.options, data);
  }

  // extract props
  var propsData = extractPropsFromVNodeData(data, Ctor, tag);

  // functional component
  if (isTrue(Ctor.options.functional)) {
    return createFunctionalComponent(Ctor, propsData, data, context, children)
  }

  // extract listeners, since these needs to be treated as
  // child component listeners instead of DOM listeners
  var listeners = data.on;
  // replace with listeners with .native modifier
  // so it gets processed during parent component patch.
  data.on = data.nativeOn;

  if (isTrue(Ctor.options.abstract)) {
    // abstract components do not keep anything
    // other than props & listeners & slot

    // work around flow
    var slot = data.slot;
    data = {};
    if (slot) {
      data.slot = slot;
    }
  }

  // install component management hooks onto the placeholder node
  installComponentHooks(data);

  // return a placeholder vnode
  var name = Ctor.options.name || tag;
  var vnode = new VNode(
    ("vue-component-" + (Ctor.cid) + (name ? ("-" + name) : '')),
    data, undefined, undefined, undefined, context,
    { Ctor: Ctor, propsData: propsData, listeners: listeners, tag: tag, children: children },
    asyncFactory
  );

  return vnode
}

function createComponentInstanceForVnode (
  vnode, // we know it's MountedComponentVNode but flow doesn't
  parent // activeInstance in lifecycle state
) {
  var options = {
    _isComponent: true,
    _parentVnode: vnode,
    parent: parent
  };
  // check inline-template render functions
  var inlineTemplate = vnode.data.inlineTemplate;
  if (isDef(inlineTemplate)) {
    options.render = inlineTemplate.render;
    options.staticRenderFns = inlineTemplate.staticRenderFns;
  }
  return new vnode.componentOptions.Ctor(options)
}

function installComponentHooks (data) {
  var hooks = data.hook || (data.hook = {});
  for (var i = 0; i < hooksToMerge.length; i++) {
    var key = hooksToMerge[i];
    var existing = hooks[key];
    var toMerge = componentVNodeHooks[key];
    if (existing !== toMerge && !(existing && existing._merged)) {
      hooks[key] = existing ? mergeHook$1(toMerge, existing) : toMerge;
    }
  }
}

function mergeHook$1 (f1, f2) {
  var merged = function (a, b) {
    // flow complains about extra args which is why we use any
    f1(a, b);
    f2(a, b);
  };
  merged._merged = true;
  return merged
}

// transform component v-model info (value and callback) into
// prop and event handler respectively.
function transformModel (options, data) {
  var prop = (options.model && options.model.prop) || 'value';
  var event = (options.model && options.model.event) || 'input'
  ;(data.attrs || (data.attrs = {}))[prop] = data.model.value;
  var on = data.on || (data.on = {});
  var existing = on[event];
  var callback = data.model.callback;
  if (isDef(existing)) {
    if (
      Array.isArray(existing)
        ? existing.indexOf(callback) === -1
        : existing !== callback
    ) {
      on[event] = [callback].concat(existing);
    }
  } else {
    on[event] = callback;
  }
}

/*  */

var SIMPLE_NORMALIZE = 1;
var ALWAYS_NORMALIZE = 2;

// wrapper function for providing a more flexible interface
// without getting yelled at by flow
function createElement (
  context,
  tag,
  data,
  children,
  normalizationType,
  alwaysNormalize
) {
  if (Array.isArray(data) || isPrimitive(data)) {
    normalizationType = children;
    children = data;
    data = undefined;
  }
  if (isTrue(alwaysNormalize)) {
    normalizationType = ALWAYS_NORMALIZE;
  }
  return _createElement(context, tag, data, children, normalizationType)
}

function _createElement (
  context,
  tag,
  data,
  children,
  normalizationType
) {
  if (isDef(data) && isDef((data).__ob__)) {
     true && warn(
      "Avoid using observed data object as vnode data: " + (JSON.stringify(data)) + "\n" +
      'Always create fresh vnode data objects in each render!',
      context
    );
    return createEmptyVNode()
  }
  // object syntax in v-bind
  if (isDef(data) && isDef(data.is)) {
    tag = data.is;
  }
  if (!tag) {
    // in case of component :is set to falsy value
    return createEmptyVNode()
  }
  // warn against non-primitive key
  if ( true &&
    isDef(data) && isDef(data.key) && !isPrimitive(data.key)
  ) {
    {
      warn(
        'Avoid using non-primitive value as key, ' +
        'use string/number value instead.',
        context
      );
    }
  }
  // support single function children as default scoped slot
  if (Array.isArray(children) &&
    typeof children[0] === 'function'
  ) {
    data = data || {};
    data.scopedSlots = { default: children[0] };
    children.length = 0;
  }
  if (normalizationType === ALWAYS_NORMALIZE) {
    children = normalizeChildren(children);
  } else if (normalizationType === SIMPLE_NORMALIZE) {
    children = simpleNormalizeChildren(children);
  }
  var vnode, ns;
  if (typeof tag === 'string') {
    var Ctor;
    ns = (context.$vnode && context.$vnode.ns) || config.getTagNamespace(tag);
    if (config.isReservedTag(tag)) {
      // platform built-in elements
      vnode = new VNode(
        config.parsePlatformTagName(tag), data, children,
        undefined, undefined, context
      );
    } else if ((!data || !data.pre) && isDef(Ctor = resolveAsset(context.$options, 'components', tag))) {
      // component
      vnode = createComponent(Ctor, data, context, children, tag);
    } else {
      // unknown or unlisted namespaced elements
      // check at runtime because it may get assigned a namespace when its
      // parent normalizes children
      vnode = new VNode(
        tag, data, children,
        undefined, undefined, context
      );
    }
  } else {
    // direct component options / constructor
    vnode = createComponent(tag, data, context, children);
  }
  if (Array.isArray(vnode)) {
    return vnode
  } else if (isDef(vnode)) {
    if (isDef(ns)) { applyNS(vnode, ns); }
    if (isDef(data)) { registerDeepBindings(data); }
    return vnode
  } else {
    return createEmptyVNode()
  }
}

function applyNS (vnode, ns, force) {
  vnode.ns = ns;
  if (vnode.tag === 'foreignObject') {
    // use default namespace inside foreignObject
    ns = undefined;
    force = true;
  }
  if (isDef(vnode.children)) {
    for (var i = 0, l = vnode.children.length; i < l; i++) {
      var child = vnode.children[i];
      if (isDef(child.tag) && (
        isUndef(child.ns) || (isTrue(force) && child.tag !== 'svg'))) {
        applyNS(child, ns, force);
      }
    }
  }
}

// ref #5318
// necessary to ensure parent re-render when deep bindings like :style and
// :class are used on slot nodes
function registerDeepBindings (data) {
  if (isObject(data.style)) {
    traverse(data.style);
  }
  if (isObject(data.class)) {
    traverse(data.class);
  }
}

/*  */

function initRender (vm) {
  vm._vnode = null; // the root of the child tree
  vm._staticTrees = null; // v-once cached trees
  var options = vm.$options;
  var parentVnode = vm.$vnode = options._parentVnode; // the placeholder node in parent tree
  var renderContext = parentVnode && parentVnode.context;
  vm.$slots = resolveSlots(options._renderChildren, renderContext);
  vm.$scopedSlots = emptyObject;
  // bind the createElement fn to this instance
  // so that we get proper render context inside it.
  // args order: tag, data, children, normalizationType, alwaysNormalize
  // internal version is used by render functions compiled from templates
  vm._c = function (a, b, c, d) { return createElement(vm, a, b, c, d, false); };
  // normalization is always applied for the public version, used in
  // user-written render functions.
  vm.$createElement = function (a, b, c, d) { return createElement(vm, a, b, c, d, true); };

  // $attrs & $listeners are exposed for easier HOC creation.
  // they need to be reactive so that HOCs using them are always updated
  var parentData = parentVnode && parentVnode.data;

  /* istanbul ignore else */
  if (true) {
    defineReactive$$1(vm, '$attrs', parentData && parentData.attrs || emptyObject, function () {
      !isUpdatingChildComponent && warn("$attrs is readonly.", vm);
    }, true);
    defineReactive$$1(vm, '$listeners', options._parentListeners || emptyObject, function () {
      !isUpdatingChildComponent && warn("$listeners is readonly.", vm);
    }, true);
  } else {}
}

var currentRenderingInstance = null;

function renderMixin (Vue) {
  // install runtime convenience helpers
  installRenderHelpers(Vue.prototype);

  Vue.prototype.$nextTick = function (fn) {
    return nextTick(fn, this)
  };

  Vue.prototype._render = function () {
    var vm = this;
    var ref = vm.$options;
    var render = ref.render;
    var _parentVnode = ref._parentVnode;

    if (_parentVnode) {
      vm.$scopedSlots = normalizeScopedSlots(
        _parentVnode.data.scopedSlots,
        vm.$slots,
        vm.$scopedSlots
      );
    }

    // set parent vnode. this allows render functions to have access
    // to the data on the placeholder node.
    vm.$vnode = _parentVnode;
    // render self
    var vnode;
    try {
      // There's no need to maintain a stack becaues all render fns are called
      // separately from one another. Nested component's render fns are called
      // when parent component is patched.
      currentRenderingInstance = vm;
      vnode = render.call(vm._renderProxy, vm.$createElement);
    } catch (e) {
      handleError(e, vm, "render");
      // return error render result,
      // or previous vnode to prevent render error causing blank component
      /* istanbul ignore else */
      if ( true && vm.$options.renderError) {
        try {
          vnode = vm.$options.renderError.call(vm._renderProxy, vm.$createElement, e);
        } catch (e) {
          handleError(e, vm, "renderError");
          vnode = vm._vnode;
        }
      } else {
        vnode = vm._vnode;
      }
    } finally {
      currentRenderingInstance = null;
    }
    // if the returned array contains only a single node, allow it
    if (Array.isArray(vnode) && vnode.length === 1) {
      vnode = vnode[0];
    }
    // return empty vnode in case the render function errored out
    if (!(vnode instanceof VNode)) {
      if ( true && Array.isArray(vnode)) {
        warn(
          'Multiple root nodes returned from render function. Render function ' +
          'should return a single root node.',
          vm
        );
      }
      vnode = createEmptyVNode();
    }
    // set parent
    vnode.parent = _parentVnode;
    return vnode
  };
}

/*  */

function ensureCtor (comp, base) {
  if (
    comp.__esModule ||
    (hasSymbol && comp[Symbol.toStringTag] === 'Module')
  ) {
    comp = comp.default;
  }
  return isObject(comp)
    ? base.extend(comp)
    : comp
}

function createAsyncPlaceholder (
  factory,
  data,
  context,
  children,
  tag
) {
  var node = createEmptyVNode();
  node.asyncFactory = factory;
  node.asyncMeta = { data: data, context: context, children: children, tag: tag };
  return node
}

function resolveAsyncComponent (
  factory,
  baseCtor
) {
  if (isTrue(factory.error) && isDef(factory.errorComp)) {
    return factory.errorComp
  }

  if (isDef(factory.resolved)) {
    return factory.resolved
  }

  var owner = currentRenderingInstance;
  if (owner && isDef(factory.owners) && factory.owners.indexOf(owner) === -1) {
    // already pending
    factory.owners.push(owner);
  }

  if (isTrue(factory.loading) && isDef(factory.loadingComp)) {
    return factory.loadingComp
  }

  if (owner && !isDef(factory.owners)) {
    var owners = factory.owners = [owner];
    var sync = true;
    var timerLoading = null;
    var timerTimeout = null

    ;(owner).$on('hook:destroyed', function () { return remove(owners, owner); });

    var forceRender = function (renderCompleted) {
      for (var i = 0, l = owners.length; i < l; i++) {
        (owners[i]).$forceUpdate();
      }

      if (renderCompleted) {
        owners.length = 0;
        if (timerLoading !== null) {
          clearTimeout(timerLoading);
          timerLoading = null;
        }
        if (timerTimeout !== null) {
          clearTimeout(timerTimeout);
          timerTimeout = null;
        }
      }
    };

    var resolve = once(function (res) {
      // cache resolved
      factory.resolved = ensureCtor(res, baseCtor);
      // invoke callbacks only if this is not a synchronous resolve
      // (async resolves are shimmed as synchronous during SSR)
      if (!sync) {
        forceRender(true);
      } else {
        owners.length = 0;
      }
    });

    var reject = once(function (reason) {
       true && warn(
        "Failed to resolve async component: " + (String(factory)) +
        (reason ? ("\nReason: " + reason) : '')
      );
      if (isDef(factory.errorComp)) {
        factory.error = true;
        forceRender(true);
      }
    });

    var res = factory(resolve, reject);

    if (isObject(res)) {
      if (isPromise(res)) {
        // () => Promise
        if (isUndef(factory.resolved)) {
          res.then(resolve, reject);
        }
      } else if (isPromise(res.component)) {
        res.component.then(resolve, reject);

        if (isDef(res.error)) {
          factory.errorComp = ensureCtor(res.error, baseCtor);
        }

        if (isDef(res.loading)) {
          factory.loadingComp = ensureCtor(res.loading, baseCtor);
          if (res.delay === 0) {
            factory.loading = true;
          } else {
            timerLoading = setTimeout(function () {
              timerLoading = null;
              if (isUndef(factory.resolved) && isUndef(factory.error)) {
                factory.loading = true;
                forceRender(false);
              }
            }, res.delay || 200);
          }
        }

        if (isDef(res.timeout)) {
          timerTimeout = setTimeout(function () {
            timerTimeout = null;
            if (isUndef(factory.resolved)) {
              reject(
                 true
                  ? ("timeout (" + (res.timeout) + "ms)")
                  : undefined
              );
            }
          }, res.timeout);
        }
      }
    }

    sync = false;
    // return in case resolved synchronously
    return factory.loading
      ? factory.loadingComp
      : factory.resolved
  }
}

/*  */

function isAsyncPlaceholder (node) {
  return node.isComment && node.asyncFactory
}

/*  */

function getFirstComponentChild (children) {
  if (Array.isArray(children)) {
    for (var i = 0; i < children.length; i++) {
      var c = children[i];
      if (isDef(c) && (isDef(c.componentOptions) || isAsyncPlaceholder(c))) {
        return c
      }
    }
  }
}

/*  */

/*  */

function initEvents (vm) {
  vm._events = Object.create(null);
  vm._hasHookEvent = false;
  // init parent attached events
  var listeners = vm.$options._parentListeners;
  if (listeners) {
    updateComponentListeners(vm, listeners);
  }
}

var target;

function add (event, fn) {
  target.$on(event, fn);
}

function remove$1 (event, fn) {
  target.$off(event, fn);
}

function createOnceHandler (event, fn) {
  var _target = target;
  return function onceHandler () {
    var res = fn.apply(null, arguments);
    if (res !== null) {
      _target.$off(event, onceHandler);
    }
  }
}

function updateComponentListeners (
  vm,
  listeners,
  oldListeners
) {
  target = vm;
  updateListeners(listeners, oldListeners || {}, add, remove$1, createOnceHandler, vm);
  target = undefined;
}

function eventsMixin (Vue) {
  var hookRE = /^hook:/;
  Vue.prototype.$on = function (event, fn) {
    var vm = this;
    if (Array.isArray(event)) {
      for (var i = 0, l = event.length; i < l; i++) {
        vm.$on(event[i], fn);
      }
    } else {
      (vm._events[event] || (vm._events[event] = [])).push(fn);
      // optimize hook:event cost by using a boolean flag marked at registration
      // instead of a hash lookup
      if (hookRE.test(event)) {
        vm._hasHookEvent = true;
      }
    }
    return vm
  };

  Vue.prototype.$once = function (event, fn) {
    var vm = this;
    function on () {
      vm.$off(event, on);
      fn.apply(vm, arguments);
    }
    on.fn = fn;
    vm.$on(event, on);
    return vm
  };

  Vue.prototype.$off = function (event, fn) {
    var vm = this;
    // all
    if (!arguments.length) {
      vm._events = Object.create(null);
      return vm
    }
    // array of events
    if (Array.isArray(event)) {
      for (var i$1 = 0, l = event.length; i$1 < l; i$1++) {
        vm.$off(event[i$1], fn);
      }
      return vm
    }
    // specific event
    var cbs = vm._events[event];
    if (!cbs) {
      return vm
    }
    if (!fn) {
      vm._events[event] = null;
      return vm
    }
    // specific handler
    var cb;
    var i = cbs.length;
    while (i--) {
      cb = cbs[i];
      if (cb === fn || cb.fn === fn) {
        cbs.splice(i, 1);
        break
      }
    }
    return vm
  };

  Vue.prototype.$emit = function (event) {
    var vm = this;
    if (true) {
      var lowerCaseEvent = event.toLowerCase();
      if (lowerCaseEvent !== event && vm._events[lowerCaseEvent]) {
        tip(
          "Event \"" + lowerCaseEvent + "\" is emitted in component " +
          (formatComponentName(vm)) + " but the handler is registered for \"" + event + "\". " +
          "Note that HTML attributes are case-insensitive and you cannot use " +
          "v-on to listen to camelCase events when using in-DOM templates. " +
          "You should probably use \"" + (hyphenate(event)) + "\" instead of \"" + event + "\"."
        );
      }
    }
    var cbs = vm._events[event];
    if (cbs) {
      cbs = cbs.length > 1 ? toArray(cbs) : cbs;
      var args = toArray(arguments, 1);
      var info = "event handler for \"" + event + "\"";
      for (var i = 0, l = cbs.length; i < l; i++) {
        invokeWithErrorHandling(cbs[i], vm, args, vm, info);
      }
    }
    return vm
  };
}

/*  */

var activeInstance = null;
var isUpdatingChildComponent = false;

function setActiveInstance(vm) {
  var prevActiveInstance = activeInstance;
  activeInstance = vm;
  return function () {
    activeInstance = prevActiveInstance;
  }
}

function initLifecycle (vm) {
  var options = vm.$options;

  // locate first non-abstract parent
  var parent = options.parent;
  if (parent && !options.abstract) {
    while (parent.$options.abstract && parent.$parent) {
      parent = parent.$parent;
    }
    parent.$children.push(vm);
  }

  vm.$parent = parent;
  vm.$root = parent ? parent.$root : vm;

  vm.$children = [];
  vm.$refs = {};

  vm._watcher = null;
  vm._inactive = null;
  vm._directInactive = false;
  vm._isMounted = false;
  vm._isDestroyed = false;
  vm._isBeingDestroyed = false;
}

function lifecycleMixin (Vue) {
  Vue.prototype._update = function (vnode, hydrating) {
    var vm = this;
    var prevEl = vm.$el;
    var prevVnode = vm._vnode;
    var restoreActiveInstance = setActiveInstance(vm);
    vm._vnode = vnode;
    // Vue.prototype.__patch__ is injected in entry points
    // based on the rendering backend used.
    if (!prevVnode) {
      // initial render
      vm.$el = vm.__patch__(vm.$el, vnode, hydrating, false /* removeOnly */);
    } else {
      // updates
      vm.$el = vm.__patch__(prevVnode, vnode);
    }
    restoreActiveInstance();
    // update __vue__ reference
    if (prevEl) {
      prevEl.__vue__ = null;
    }
    if (vm.$el) {
      vm.$el.__vue__ = vm;
    }
    // if parent is an HOC, update its $el as well
    if (vm.$vnode && vm.$parent && vm.$vnode === vm.$parent._vnode) {
      vm.$parent.$el = vm.$el;
    }
    // updated hook is called by the scheduler to ensure that children are
    // updated in a parent's updated hook.
  };

  Vue.prototype.$forceUpdate = function () {
    var vm = this;
    if (vm._watcher) {
      vm._watcher.update();
    }
  };

  Vue.prototype.$destroy = function () {
    var vm = this;
    if (vm._isBeingDestroyed) {
      return
    }
    callHook(vm, 'beforeDestroy');
    vm._isBeingDestroyed = true;
    // remove self from parent
    var parent = vm.$parent;
    if (parent && !parent._isBeingDestroyed && !vm.$options.abstract) {
      remove(parent.$children, vm);
    }
    // teardown watchers
    if (vm._watcher) {
      vm._watcher.teardown();
    }
    var i = vm._watchers.length;
    while (i--) {
      vm._watchers[i].teardown();
    }
    // remove reference from data ob
    // frozen object may not have observer.
    if (vm._data.__ob__) {
      vm._data.__ob__.vmCount--;
    }
    // call the last hook...
    vm._isDestroyed = true;
    // invoke destroy hooks on current rendered tree
    vm.__patch__(vm._vnode, null);
    // fire destroyed hook
    callHook(vm, 'destroyed');
    // turn off all instance listeners.
    vm.$off();
    // remove __vue__ reference
    if (vm.$el) {
      vm.$el.__vue__ = null;
    }
    // release circular reference (#6759)
    if (vm.$vnode) {
      vm.$vnode.parent = null;
    }
  };
}

function updateChildComponent (
  vm,
  propsData,
  listeners,
  parentVnode,
  renderChildren
) {
  if (true) {
    isUpdatingChildComponent = true;
  }

  // determine whether component has slot children
  // we need to do this before overwriting $options._renderChildren.

  // check if there are dynamic scopedSlots (hand-written or compiled but with
  // dynamic slot names). Static scoped slots compiled from template has the
  // "$stable" marker.
  var newScopedSlots = parentVnode.data.scopedSlots;
  var oldScopedSlots = vm.$scopedSlots;
  var hasDynamicScopedSlot = !!(
    (newScopedSlots && !newScopedSlots.$stable) ||
    (oldScopedSlots !== emptyObject && !oldScopedSlots.$stable) ||
    (newScopedSlots && vm.$scopedSlots.$key !== newScopedSlots.$key)
  );

  // Any static slot children from the parent may have changed during parent's
  // update. Dynamic scoped slots may also have changed. In such cases, a forced
  // update is necessary to ensure correctness.
  var needsForceUpdate = !!(
    renderChildren ||               // has new static slots
    vm.$options._renderChildren ||  // has old static slots
    hasDynamicScopedSlot
  );

  vm.$options._parentVnode = parentVnode;
  vm.$vnode = parentVnode; // update vm's placeholder node without re-render

  if (vm._vnode) { // update child tree's parent
    vm._vnode.parent = parentVnode;
  }
  vm.$options._renderChildren = renderChildren;

  // update $attrs and $listeners hash
  // these are also reactive so they may trigger child update if the child
  // used them during render
  vm.$attrs = parentVnode.data.attrs || emptyObject;
  vm.$listeners = listeners || emptyObject;

  // update props
  if (propsData && vm.$options.props) {
    toggleObserving(false);
    var props = vm._props;
    var propKeys = vm.$options._propKeys || [];
    for (var i = 0; i < propKeys.length; i++) {
      var key = propKeys[i];
      var propOptions = vm.$options.props; // wtf flow?
      props[key] = validateProp(key, propOptions, propsData, vm);
    }
    toggleObserving(true);
    // keep a copy of raw propsData
    vm.$options.propsData = propsData;
  }

  // update listeners
  listeners = listeners || emptyObject;
  var oldListeners = vm.$options._parentListeners;
  vm.$options._parentListeners = listeners;
  updateComponentListeners(vm, listeners, oldListeners);

  // resolve slots + force update if has children
  if (needsForceUpdate) {
    vm.$slots = resolveSlots(renderChildren, parentVnode.context);
    vm.$forceUpdate();
  }

  if (true) {
    isUpdatingChildComponent = false;
  }
}

function isInInactiveTree (vm) {
  while (vm && (vm = vm.$parent)) {
    if (vm._inactive) { return true }
  }
  return false
}

function activateChildComponent (vm, direct) {
  if (direct) {
    vm._directInactive = false;
    if (isInInactiveTree(vm)) {
      return
    }
  } else if (vm._directInactive) {
    return
  }
  if (vm._inactive || vm._inactive === null) {
    vm._inactive = false;
    for (var i = 0; i < vm.$children.length; i++) {
      activateChildComponent(vm.$children[i]);
    }
    callHook(vm, 'activated');
  }
}

function deactivateChildComponent (vm, direct) {
  if (direct) {
    vm._directInactive = true;
    if (isInInactiveTree(vm)) {
      return
    }
  }
  if (!vm._inactive) {
    vm._inactive = true;
    for (var i = 0; i < vm.$children.length; i++) {
      deactivateChildComponent(vm.$children[i]);
    }
    callHook(vm, 'deactivated');
  }
}

function callHook (vm, hook) {
  // #7573 disable dep collection when invoking lifecycle hooks
  pushTarget();
  var handlers = vm.$options[hook];
  var info = hook + " hook";
  if (handlers) {
    for (var i = 0, j = handlers.length; i < j; i++) {
      invokeWithErrorHandling(handlers[i], vm, null, vm, info);
    }
  }
  if (vm._hasHookEvent) {
    vm.$emit('hook:' + hook);
  }
  popTarget();
}

/*  */

var MAX_UPDATE_COUNT = 100;

var queue = [];
var activatedChildren = [];
var has = {};
var circular = {};
var waiting = false;
var flushing = false;
var index = 0;

/**
 * Reset the scheduler's state.
 */
function resetSchedulerState () {
  index = queue.length = activatedChildren.length = 0;
  has = {};
  if (true) {
    circular = {};
  }
  waiting = flushing = false;
}

// Async edge case #6566 requires saving the timestamp when event listeners are
// attached. However, calling performance.now() has a perf overhead especially
// if the page has thousands of event listeners. Instead, we take a timestamp
// every time the scheduler flushes and use that for all event listeners
// attached during that flush.
var currentFlushTimestamp = 0;

// Async edge case fix requires storing an event listener's attach timestamp.
var getNow = Date.now;

// Determine what event timestamp the browser is using. Annoyingly, the
// timestamp can either be hi-res (relative to page load) or low-res
// (relative to UNIX epoch), so in order to compare time we have to use the
// same timestamp type when saving the flush timestamp.
// All IE versions use low-res event timestamps, and have problematic clock
// implementations (#9632)
if (inBrowser && !isIE) {
  var performance = window.performance;
  if (
    performance &&
    typeof performance.now === 'function' &&
    getNow() > document.createEvent('Event').timeStamp
  ) {
    // if the event timestamp, although evaluated AFTER the Date.now(), is
    // smaller than it, it means the event is using a hi-res timestamp,
    // and we need to use the hi-res version for event listener timestamps as
    // well.
    getNow = function () { return performance.now(); };
  }
}

/**
 * Flush both queues and run the watchers.
 */
function flushSchedulerQueue () {
  currentFlushTimestamp = getNow();
  flushing = true;
  var watcher, id;

  // Sort queue before flush.
  // This ensures that:
  // 1. Components are updated from parent to child. (because parent is always
  //    created before the child)
  // 2. A component's user watchers are run before its render watcher (because
  //    user watchers are created before the render watcher)
  // 3. If a component is destroyed during a parent component's watcher run,
  //    its watchers can be skipped.
  queue.sort(function (a, b) { return a.id - b.id; });

  // do not cache length because more watchers might be pushed
  // as we run existing watchers
  for (index = 0; index < queue.length; index++) {
    watcher = queue[index];
    if (watcher.before) {
      watcher.before();
    }
    id = watcher.id;
    has[id] = null;
    watcher.run();
    // in dev build, check and stop circular updates.
    if ( true && has[id] != null) {
      circular[id] = (circular[id] || 0) + 1;
      if (circular[id] > MAX_UPDATE_COUNT) {
        warn(
          'You may have an infinite update loop ' + (
            watcher.user
              ? ("in watcher with expression \"" + (watcher.expression) + "\"")
              : "in a component render function."
          ),
          watcher.vm
        );
        break
      }
    }
  }

  // keep copies of post queues before resetting state
  var activatedQueue = activatedChildren.slice();
  var updatedQueue = queue.slice();

  resetSchedulerState();

  // call component updated and activated hooks
  callActivatedHooks(activatedQueue);
  callUpdatedHooks(updatedQueue);

  // devtool hook
  /* istanbul ignore if */
  if (devtools && config.devtools) {
    devtools.emit('flush');
  }
}

function callUpdatedHooks (queue) {
  var i = queue.length;
  while (i--) {
    var watcher = queue[i];
    var vm = watcher.vm;
    if (vm._watcher === watcher && vm._isMounted && !vm._isDestroyed) {
      callHook(vm, 'updated');
    }
  }
}

/**
 * Queue a kept-alive component that was activated during patch.
 * The queue will be processed after the entire tree has been patched.
 */
function queueActivatedComponent (vm) {
  // setting _inactive to false here so that a render function can
  // rely on checking whether it's in an inactive tree (e.g. router-view)
  vm._inactive = false;
  activatedChildren.push(vm);
}

function callActivatedHooks (queue) {
  for (var i = 0; i < queue.length; i++) {
    queue[i]._inactive = true;
    activateChildComponent(queue[i], true /* true */);
  }
}

/**
 * Push a watcher into the watcher queue.
 * Jobs with duplicate IDs will be skipped unless it's
 * pushed when the queue is being flushed.
 */
function queueWatcher (watcher) {
  var id = watcher.id;
  if (has[id] == null) {
    has[id] = true;
    if (!flushing) {
      queue.push(watcher);
    } else {
      // if already flushing, splice the watcher based on its id
      // if already past its id, it will be run next immediately.
      var i = queue.length - 1;
      while (i > index && queue[i].id > watcher.id) {
        i--;
      }
      queue.splice(i + 1, 0, watcher);
    }
    // queue the flush
    if (!waiting) {
      waiting = true;

      if ( true && !config.async) {
        flushSchedulerQueue();
        return
      }
      nextTick(flushSchedulerQueue);
    }
  }
}

/*  */



var uid$2 = 0;

/**
 * A watcher parses an expression, collects dependencies,
 * and fires callback when the expression value changes.
 * This is used for both the $watch() api and directives.
 */
var Watcher = function Watcher (
  vm,
  expOrFn,
  cb,
  options,
  isRenderWatcher
) {
  this.vm = vm;
  if (isRenderWatcher) {
    vm._watcher = this;
  }
  vm._watchers.push(this);
  // options
  if (options) {
    this.deep = !!options.deep;
    this.user = !!options.user;
    this.lazy = !!options.lazy;
    this.sync = !!options.sync;
    this.before = options.before;
  } else {
    this.deep = this.user = this.lazy = this.sync = false;
  }
  this.cb = cb;
  this.id = ++uid$2; // uid for batching
  this.active = true;
  this.dirty = this.lazy; // for lazy watchers
  this.deps = [];
  this.newDeps = [];
  this.depIds = new _Set();
  this.newDepIds = new _Set();
  this.expression =  true
    ? expOrFn.toString()
    : undefined;
  // parse expression for getter
  if (typeof expOrFn === 'function') {
    this.getter = expOrFn;
  } else {
    this.getter = parsePath(expOrFn);
    if (!this.getter) {
      this.getter = noop;
       true && warn(
        "Failed watching path: \"" + expOrFn + "\" " +
        'Watcher only accepts simple dot-delimited paths. ' +
        'For full control, use a function instead.',
        vm
      );
    }
  }
  this.value = this.lazy
    ? undefined
    : this.get();
};

/**
 * Evaluate the getter, and re-collect dependencies.
 */
Watcher.prototype.get = function get () {
  pushTarget(this);
  var value;
  var vm = this.vm;
  try {
    value = this.getter.call(vm, vm);
  } catch (e) {
    if (this.user) {
      handleError(e, vm, ("getter for watcher \"" + (this.expression) + "\""));
    } else {
      throw e
    }
  } finally {
    // "touch" every property so they are all tracked as
    // dependencies for deep watching
    if (this.deep) {
      traverse(value);
    }
    popTarget();
    this.cleanupDeps();
  }
  return value
};

/**
 * Add a dependency to this directive.
 */
Watcher.prototype.addDep = function addDep (dep) {
  var id = dep.id;
  if (!this.newDepIds.has(id)) {
    this.newDepIds.add(id);
    this.newDeps.push(dep);
    if (!this.depIds.has(id)) {
      dep.addSub(this);
    }
  }
};

/**
 * Clean up for dependency collection.
 */
Watcher.prototype.cleanupDeps = function cleanupDeps () {
  var i = this.deps.length;
  while (i--) {
    var dep = this.deps[i];
    if (!this.newDepIds.has(dep.id)) {
      dep.removeSub(this);
    }
  }
  var tmp = this.depIds;
  this.depIds = this.newDepIds;
  this.newDepIds = tmp;
  this.newDepIds.clear();
  tmp = this.deps;
  this.deps = this.newDeps;
  this.newDeps = tmp;
  this.newDeps.length = 0;
};

/**
 * Subscriber interface.
 * Will be called when a dependency changes.
 */
Watcher.prototype.update = function update () {
  /* istanbul ignore else */
  if (this.lazy) {
    this.dirty = true;
  } else if (this.sync) {
    this.run();
  } else {
    queueWatcher(this);
  }
};

/**
 * Scheduler job interface.
 * Will be called by the scheduler.
 */
Watcher.prototype.run = function run () {
  if (this.active) {
    var value = this.get();
    if (
      value !== this.value ||
      // Deep watchers and watchers on Object/Arrays should fire even
      // when the value is the same, because the value may
      // have mutated.
      isObject(value) ||
      this.deep
    ) {
      // set new value
      var oldValue = this.value;
      this.value = value;
      if (this.user) {
        try {
          this.cb.call(this.vm, value, oldValue);
        } catch (e) {
          handleError(e, this.vm, ("callback for watcher \"" + (this.expression) + "\""));
        }
      } else {
        this.cb.call(this.vm, value, oldValue);
      }
    }
  }
};

/**
 * Evaluate the value of the watcher.
 * This only gets called for lazy watchers.
 */
Watcher.prototype.evaluate = function evaluate () {
  this.value = this.get();
  this.dirty = false;
};

/**
 * Depend on all deps collected by this watcher.
 */
Watcher.prototype.depend = function depend () {
  var i = this.deps.length;
  while (i--) {
    this.deps[i].depend();
  }
};

/**
 * Remove self from all dependencies' subscriber list.
 */
Watcher.prototype.teardown = function teardown () {
  if (this.active) {
    // remove self from vm's watcher list
    // this is a somewhat expensive operation so we skip it
    // if the vm is being destroyed.
    if (!this.vm._isBeingDestroyed) {
      remove(this.vm._watchers, this);
    }
    var i = this.deps.length;
    while (i--) {
      this.deps[i].removeSub(this);
    }
    this.active = false;
  }
};

/*  */

var sharedPropertyDefinition = {
  enumerable: true,
  configurable: true,
  get: noop,
  set: noop
};

function proxy (target, sourceKey, key) {
  sharedPropertyDefinition.get = function proxyGetter () {
    return this[sourceKey][key]
  };
  sharedPropertyDefinition.set = function proxySetter (val) {
    this[sourceKey][key] = val;
  };
  Object.defineProperty(target, key, sharedPropertyDefinition);
}

function initState (vm) {
  vm._watchers = [];
  var opts = vm.$options;
  if (opts.props) { initProps(vm, opts.props); }
  if (opts.methods) { initMethods(vm, opts.methods); }
  if (opts.data) {
    initData(vm);
  } else {
    observe(vm._data = {}, true /* asRootData */);
  }
  if (opts.computed) { initComputed(vm, opts.computed); }
  if (opts.watch && opts.watch !== nativeWatch) {
    initWatch(vm, opts.watch);
  }
}

function initProps (vm, propsOptions) {
  var propsData = vm.$options.propsData || {};
  var props = vm._props = {};
  // cache prop keys so that future props updates can iterate using Array
  // instead of dynamic object key enumeration.
  var keys = vm.$options._propKeys = [];
  var isRoot = !vm.$parent;
  // root instance props should be converted
  if (!isRoot) {
    toggleObserving(false);
  }
  var loop = function ( key ) {
    keys.push(key);
    var value = validateProp(key, propsOptions, propsData, vm);
    /* istanbul ignore else */
    if (true) {
      var hyphenatedKey = hyphenate(key);
      if (isReservedAttribute(hyphenatedKey) ||
          config.isReservedAttr(hyphenatedKey)) {
        warn(
          ("\"" + hyphenatedKey + "\" is a reserved attribute and cannot be used as component prop."),
          vm
        );
      }
      defineReactive$$1(props, key, value, function () {
        if (!isRoot && !isUpdatingChildComponent) {
          warn(
            "Avoid mutating a prop directly since the value will be " +
            "overwritten whenever the parent component re-renders. " +
            "Instead, use a data or computed property based on the prop's " +
            "value. Prop being mutated: \"" + key + "\"",
            vm
          );
        }
      });
    } else {}
    // static props are already proxied on the component's prototype
    // during Vue.extend(). We only need to proxy props defined at
    // instantiation here.
    if (!(key in vm)) {
      proxy(vm, "_props", key);
    }
  };

  for (var key in propsOptions) loop( key );
  toggleObserving(true);
}

function initData (vm) {
  var data = vm.$options.data;
  data = vm._data = typeof data === 'function'
    ? getData(data, vm)
    : data || {};
  if (!isPlainObject(data)) {
    data = {};
     true && warn(
      'data functions should return an object:\n' +
      'https://vuejs.org/v2/guide/components.html#data-Must-Be-a-Function',
      vm
    );
  }
  // proxy data on instance
  var keys = Object.keys(data);
  var props = vm.$options.props;
  var methods = vm.$options.methods;
  var i = keys.length;
  while (i--) {
    var key = keys[i];
    if (true) {
      if (methods && hasOwn(methods, key)) {
        warn(
          ("Method \"" + key + "\" has already been defined as a data property."),
          vm
        );
      }
    }
    if (props && hasOwn(props, key)) {
       true && warn(
        "The data property \"" + key + "\" is already declared as a prop. " +
        "Use prop default value instead.",
        vm
      );
    } else if (!isReserved(key)) {
      proxy(vm, "_data", key);
    }
  }
  // observe data
  observe(data, true /* asRootData */);
}

function getData (data, vm) {
  // #7573 disable dep collection when invoking data getters
  pushTarget();
  try {
    return data.call(vm, vm)
  } catch (e) {
    handleError(e, vm, "data()");
    return {}
  } finally {
    popTarget();
  }
}

var computedWatcherOptions = { lazy: true };

function initComputed (vm, computed) {
  // $flow-disable-line
  var watchers = vm._computedWatchers = Object.create(null);
  // computed properties are just getters during SSR
  var isSSR = isServerRendering();

  for (var key in computed) {
    var userDef = computed[key];
    var getter = typeof userDef === 'function' ? userDef : userDef.get;
    if ( true && getter == null) {
      warn(
        ("Getter is missing for computed property \"" + key + "\"."),
        vm
      );
    }

    if (!isSSR) {
      // create internal watcher for the computed property.
      watchers[key] = new Watcher(
        vm,
        getter || noop,
        noop,
        computedWatcherOptions
      );
    }

    // component-defined computed properties are already defined on the
    // component prototype. We only need to define computed properties defined
    // at instantiation here.
    if (!(key in vm)) {
      defineComputed(vm, key, userDef);
    } else if (true) {
      if (key in vm.$data) {
        warn(("The computed property \"" + key + "\" is already defined in data."), vm);
      } else if (vm.$options.props && key in vm.$options.props) {
        warn(("The computed property \"" + key + "\" is already defined as a prop."), vm);
      }
    }
  }
}

function defineComputed (
  target,
  key,
  userDef
) {
  var shouldCache = !isServerRendering();
  if (typeof userDef === 'function') {
    sharedPropertyDefinition.get = shouldCache
      ? createComputedGetter(key)
      : createGetterInvoker(userDef);
    sharedPropertyDefinition.set = noop;
  } else {
    sharedPropertyDefinition.get = userDef.get
      ? shouldCache && userDef.cache !== false
        ? createComputedGetter(key)
        : createGetterInvoker(userDef.get)
      : noop;
    sharedPropertyDefinition.set = userDef.set || noop;
  }
  if ( true &&
      sharedPropertyDefinition.set === noop) {
    sharedPropertyDefinition.set = function () {
      warn(
        ("Computed property \"" + key + "\" was assigned to but it has no setter."),
        this
      );
    };
  }
  Object.defineProperty(target, key, sharedPropertyDefinition);
}

function createComputedGetter (key) {
  return function computedGetter () {
    var watcher = this._computedWatchers && this._computedWatchers[key];
    if (watcher) {
      if (watcher.dirty) {
        watcher.evaluate();
      }
      if (Dep.target) {
        watcher.depend();
      }
      return watcher.value
    }
  }
}

function createGetterInvoker(fn) {
  return function computedGetter () {
    return fn.call(this, this)
  }
}

function initMethods (vm, methods) {
  var props = vm.$options.props;
  for (var key in methods) {
    if (true) {
      if (typeof methods[key] !== 'function') {
        warn(
          "Method \"" + key + "\" has type \"" + (typeof methods[key]) + "\" in the component definition. " +
          "Did you reference the function correctly?",
          vm
        );
      }
      if (props && hasOwn(props, key)) {
        warn(
          ("Method \"" + key + "\" has already been defined as a prop."),
          vm
        );
      }
      if ((key in vm) && isReserved(key)) {
        warn(
          "Method \"" + key + "\" conflicts with an existing Vue instance method. " +
          "Avoid defining component methods that start with _ or $."
        );
      }
    }
    vm[key] = typeof methods[key] !== 'function' ? noop : bind(methods[key], vm);
  }
}

function initWatch (vm, watch) {
  for (var key in watch) {
    var handler = watch[key];
    if (Array.isArray(handler)) {
      for (var i = 0; i < handler.length; i++) {
        createWatcher(vm, key, handler[i]);
      }
    } else {
      createWatcher(vm, key, handler);
    }
  }
}

function createWatcher (
  vm,
  expOrFn,
  handler,
  options
) {
  if (isPlainObject(handler)) {
    options = handler;
    handler = handler.handler;
  }
  if (typeof handler === 'string') {
    handler = vm[handler];
  }
  return vm.$watch(expOrFn, handler, options)
}

function stateMixin (Vue) {
  // flow somehow has problems with directly declared definition object
  // when using Object.defineProperty, so we have to procedurally build up
  // the object here.
  var dataDef = {};
  dataDef.get = function () { return this._data };
  var propsDef = {};
  propsDef.get = function () { return this._props };
  if (true) {
    dataDef.set = function () {
      warn(
        'Avoid replacing instance root $data. ' +
        'Use nested data properties instead.',
        this
      );
    };
    propsDef.set = function () {
      warn("$props is readonly.", this);
    };
  }
  Object.defineProperty(Vue.prototype, '$data', dataDef);
  Object.defineProperty(Vue.prototype, '$props', propsDef);

  Vue.prototype.$set = set;
  Vue.prototype.$delete = del;

  Vue.prototype.$watch = function (
    expOrFn,
    cb,
    options
  ) {
    var vm = this;
    if (isPlainObject(cb)) {
      return createWatcher(vm, expOrFn, cb, options)
    }
    options = options || {};
    options.user = true;
    var watcher = new Watcher(vm, expOrFn, cb, options);
    if (options.immediate) {
      try {
        cb.call(vm, watcher.value);
      } catch (error) {
        handleError(error, vm, ("callback for immediate watcher \"" + (watcher.expression) + "\""));
      }
    }
    return function unwatchFn () {
      watcher.teardown();
    }
  };
}

/*  */

var uid$3 = 0;

function initMixin (Vue) {
  Vue.prototype._init = function (options) {
    var vm = this;
    // a uid
    vm._uid = uid$3++;

    var startTag, endTag;
    /* istanbul ignore if */
    if ( true && config.performance && mark) {
      startTag = "vue-perf-start:" + (vm._uid);
      endTag = "vue-perf-end:" + (vm._uid);
      mark(startTag);
    }

    // a flag to avoid this being observed
    vm._isVue = true;
    // merge options
    if (options && options._isComponent) {
      // optimize internal component instantiation
      // since dynamic options merging is pretty slow, and none of the
      // internal component options needs special treatment.
      initInternalComponent(vm, options);
    } else {
      vm.$options = mergeOptions(
        resolveConstructorOptions(vm.constructor),
        options || {},
        vm
      );
    }
    /* istanbul ignore else */
    if (true) {
      initProxy(vm);
    } else {}
    // expose real self
    vm._self = vm;
    initLifecycle(vm);
    initEvents(vm);
    initRender(vm);
    callHook(vm, 'beforeCreate');
    // initInjections(vm) // resolve injections before data/props
    initState(vm);
    // initProvide(vm) // resolve provide after data/props
    // callHook(vm, 'created')

    /* istanbul ignore if */
    if ( true && config.performance && mark) {
      vm._name = formatComponentName(vm, false);
      mark(endTag);
      measure(("vue " + (vm._name) + " init"), startTag, endTag);
    }

    if (vm.$options.el) {
      vm.$mount(vm.$options.el);
    }
  };
}

function initInternalComponent (vm, options) {
  var opts = vm.$options = Object.create(vm.constructor.options);
  // doing this because it's faster than dynamic enumeration.
  var parentVnode = options._parentVnode;
  opts.parent = options.parent;
  opts._parentVnode = parentVnode;

  var vnodeComponentOptions = parentVnode.componentOptions;
  opts.propsData = vnodeComponentOptions.propsData;
  opts._parentListeners = vnodeComponentOptions.listeners;
  opts._renderChildren = vnodeComponentOptions.children;
  opts._componentTag = vnodeComponentOptions.tag;

  if (options.render) {
    opts.render = options.render;
    opts.staticRenderFns = options.staticRenderFns;
  }
}

function resolveConstructorOptions (Ctor) {
  var options = Ctor.options;
  if (Ctor.super) {
    var superOptions = resolveConstructorOptions(Ctor.super);
    var cachedSuperOptions = Ctor.superOptions;
    if (superOptions !== cachedSuperOptions) {
      // super option changed,
      // need to resolve new options.
      Ctor.superOptions = superOptions;
      // check if there are any late-modified/attached options (#4976)
      var modifiedOptions = resolveModifiedOptions(Ctor);
      // update base extend options
      if (modifiedOptions) {
        extend(Ctor.extendOptions, modifiedOptions);
      }
      options = Ctor.options = mergeOptions(superOptions, Ctor.extendOptions);
      if (options.name) {
        options.components[options.name] = Ctor;
      }
    }
  }
  return options
}

function resolveModifiedOptions (Ctor) {
  var modified;
  var latest = Ctor.options;
  var sealed = Ctor.sealedOptions;
  for (var key in latest) {
    if (latest[key] !== sealed[key]) {
      if (!modified) { modified = {}; }
      modified[key] = latest[key];
    }
  }
  return modified
}

function Vue (options) {
  if ( true &&
    !(this instanceof Vue)
  ) {
    warn('Vue is a constructor and should be called with the `new` keyword');
  }
  this._init(options);
}

initMixin(Vue);
stateMixin(Vue);
eventsMixin(Vue);
lifecycleMixin(Vue);
renderMixin(Vue);

/*  */

function initUse (Vue) {
  Vue.use = function (plugin) {
    var installedPlugins = (this._installedPlugins || (this._installedPlugins = []));
    if (installedPlugins.indexOf(plugin) > -1) {
      return this
    }

    // additional parameters
    var args = toArray(arguments, 1);
    args.unshift(this);
    if (typeof plugin.install === 'function') {
      plugin.install.apply(plugin, args);
    } else if (typeof plugin === 'function') {
      plugin.apply(null, args);
    }
    installedPlugins.push(plugin);
    return this
  };
}

/*  */

function initMixin$1 (Vue) {
  Vue.mixin = function (mixin) {
    this.options = mergeOptions(this.options, mixin);
    return this
  };
}

/*  */

function initExtend (Vue) {
  /**
   * Each instance constructor, including Vue, has a unique
   * cid. This enables us to create wrapped "child
   * constructors" for prototypal inheritance and cache them.
   */
  Vue.cid = 0;
  var cid = 1;

  /**
   * Class inheritance
   */
  Vue.extend = function (extendOptions) {
    extendOptions = extendOptions || {};
    var Super = this;
    var SuperId = Super.cid;
    var cachedCtors = extendOptions._Ctor || (extendOptions._Ctor = {});
    if (cachedCtors[SuperId]) {
      return cachedCtors[SuperId]
    }

    var name = extendOptions.name || Super.options.name;
    if ( true && name) {
      validateComponentName(name);
    }

    var Sub = function VueComponent (options) {
      this._init(options);
    };
    Sub.prototype = Object.create(Super.prototype);
    Sub.prototype.constructor = Sub;
    Sub.cid = cid++;
    Sub.options = mergeOptions(
      Super.options,
      extendOptions
    );
    Sub['super'] = Super;

    // For props and computed properties, we define the proxy getters on
    // the Vue instances at extension time, on the extended prototype. This
    // avoids Object.defineProperty calls for each instance created.
    if (Sub.options.props) {
      initProps$1(Sub);
    }
    if (Sub.options.computed) {
      initComputed$1(Sub);
    }

    // allow further extension/mixin/plugin usage
    Sub.extend = Super.extend;
    Sub.mixin = Super.mixin;
    Sub.use = Super.use;

    // create asset registers, so extended classes
    // can have their private assets too.
    ASSET_TYPES.forEach(function (type) {
      Sub[type] = Super[type];
    });
    // enable recursive self-lookup
    if (name) {
      Sub.options.components[name] = Sub;
    }

    // keep a reference to the super options at extension time.
    // later at instantiation we can check if Super's options have
    // been updated.
    Sub.superOptions = Super.options;
    Sub.extendOptions = extendOptions;
    Sub.sealedOptions = extend({}, Sub.options);

    // cache constructor
    cachedCtors[SuperId] = Sub;
    return Sub
  };
}

function initProps$1 (Comp) {
  var props = Comp.options.props;
  for (var key in props) {
    proxy(Comp.prototype, "_props", key);
  }
}

function initComputed$1 (Comp) {
  var computed = Comp.options.computed;
  for (var key in computed) {
    defineComputed(Comp.prototype, key, computed[key]);
  }
}

/*  */

function initAssetRegisters (Vue) {
  /**
   * Create asset registration methods.
   */
  ASSET_TYPES.forEach(function (type) {
    Vue[type] = function (
      id,
      definition
    ) {
      if (!definition) {
        return this.options[type + 's'][id]
      } else {
        /* istanbul ignore if */
        if ( true && type === 'component') {
          validateComponentName(id);
        }
        if (type === 'component' && isPlainObject(definition)) {
          definition.name = definition.name || id;
          definition = this.options._base.extend(definition);
        }
        if (type === 'directive' && typeof definition === 'function') {
          definition = { bind: definition, update: definition };
        }
        this.options[type + 's'][id] = definition;
        return definition
      }
    };
  });
}

/*  */



function getComponentName (opts) {
  return opts && (opts.Ctor.options.name || opts.tag)
}

function matches (pattern, name) {
  if (Array.isArray(pattern)) {
    return pattern.indexOf(name) > -1
  } else if (typeof pattern === 'string') {
    return pattern.split(',').indexOf(name) > -1
  } else if (isRegExp(pattern)) {
    return pattern.test(name)
  }
  /* istanbul ignore next */
  return false
}

function pruneCache (keepAliveInstance, filter) {
  var cache = keepAliveInstance.cache;
  var keys = keepAliveInstance.keys;
  var _vnode = keepAliveInstance._vnode;
  for (var key in cache) {
    var cachedNode = cache[key];
    if (cachedNode) {
      var name = getComponentName(cachedNode.componentOptions);
      if (name && !filter(name)) {
        pruneCacheEntry(cache, key, keys, _vnode);
      }
    }
  }
}

function pruneCacheEntry (
  cache,
  key,
  keys,
  current
) {
  var cached$$1 = cache[key];
  if (cached$$1 && (!current || cached$$1.tag !== current.tag)) {
    cached$$1.componentInstance.$destroy();
  }
  cache[key] = null;
  remove(keys, key);
}

var patternTypes = [String, RegExp, Array];

var KeepAlive = {
  name: 'keep-alive',
  abstract: true,

  props: {
    include: patternTypes,
    exclude: patternTypes,
    max: [String, Number]
  },

  created: function created () {
    this.cache = Object.create(null);
    this.keys = [];
  },

  destroyed: function destroyed () {
    for (var key in this.cache) {
      pruneCacheEntry(this.cache, key, this.keys);
    }
  },

  mounted: function mounted () {
    var this$1 = this;

    this.$watch('include', function (val) {
      pruneCache(this$1, function (name) { return matches(val, name); });
    });
    this.$watch('exclude', function (val) {
      pruneCache(this$1, function (name) { return !matches(val, name); });
    });
  },

  render: function render () {
    var slot = this.$slots.default;
    var vnode = getFirstComponentChild(slot);
    var componentOptions = vnode && vnode.componentOptions;
    if (componentOptions) {
      // check pattern
      var name = getComponentName(componentOptions);
      var ref = this;
      var include = ref.include;
      var exclude = ref.exclude;
      if (
        // not included
        (include && (!name || !matches(include, name))) ||
        // excluded
        (exclude && name && matches(exclude, name))
      ) {
        return vnode
      }

      var ref$1 = this;
      var cache = ref$1.cache;
      var keys = ref$1.keys;
      var key = vnode.key == null
        // same constructor may get registered as different local components
        // so cid alone is not enough (#3269)
        ? componentOptions.Ctor.cid + (componentOptions.tag ? ("::" + (componentOptions.tag)) : '')
        : vnode.key;
      if (cache[key]) {
        vnode.componentInstance = cache[key].componentInstance;
        // make current key freshest
        remove(keys, key);
        keys.push(key);
      } else {
        cache[key] = vnode;
        keys.push(key);
        // prune oldest entry
        if (this.max && keys.length > parseInt(this.max)) {
          pruneCacheEntry(cache, keys[0], keys, this._vnode);
        }
      }

      vnode.data.keepAlive = true;
    }
    return vnode || (slot && slot[0])
  }
};

var builtInComponents = {
  KeepAlive: KeepAlive
};

/*  */

function initGlobalAPI (Vue) {
  // config
  var configDef = {};
  configDef.get = function () { return config; };
  if (true) {
    configDef.set = function () {
      warn(
        'Do not replace the Vue.config object, set individual fields instead.'
      );
    };
  }
  Object.defineProperty(Vue, 'config', configDef);

  // exposed util methods.
  // NOTE: these are not considered part of the public API - avoid relying on
  // them unless you are aware of the risk.
  Vue.util = {
    warn: warn,
    extend: extend,
    mergeOptions: mergeOptions,
    defineReactive: defineReactive$$1
  };

  Vue.set = set;
  Vue.delete = del;
  Vue.nextTick = nextTick;

  // 2.6 explicit observable API
  Vue.observable = function (obj) {
    observe(obj);
    return obj
  };

  Vue.options = Object.create(null);
  ASSET_TYPES.forEach(function (type) {
    Vue.options[type + 's'] = Object.create(null);
  });

  // this is used to identify the "base" constructor to extend all plain-object
  // components with in Weex's multi-instance scenarios.
  Vue.options._base = Vue;

  extend(Vue.options.components, builtInComponents);

  initUse(Vue);
  initMixin$1(Vue);
  initExtend(Vue);
  initAssetRegisters(Vue);
}

initGlobalAPI(Vue);

Object.defineProperty(Vue.prototype, '$isServer', {
  get: isServerRendering
});

Object.defineProperty(Vue.prototype, '$ssrContext', {
  get: function get () {
    /* istanbul ignore next */
    return this.$vnode && this.$vnode.ssrContext
  }
});

// expose FunctionalRenderContext for ssr runtime helper installation
Object.defineProperty(Vue, 'FunctionalRenderContext', {
  value: FunctionalRenderContext
});

Vue.version = '2.6.10';

/**
 * https://raw.githubusercontent.com/Tencent/westore/master/packages/westore/utils/diff.js
 */
var ARRAYTYPE = '[object Array]';
var OBJECTTYPE = '[object Object]';
// const FUNCTIONTYPE = '[object Function]'

function diff(current, pre) {
    var result = {};
    syncKeys(current, pre);
    _diff(current, pre, '', result);
    return result
}

function syncKeys(current, pre) {
    if (current === pre) { return }
    var rootCurrentType = type(current);
    var rootPreType = type(pre);
    if (rootCurrentType == OBJECTTYPE && rootPreType == OBJECTTYPE) {
        if(Object.keys(current).length >= Object.keys(pre).length){
            for (var key in pre) {
                var currentValue = current[key];
                if (currentValue === undefined) {
                    current[key] = null;
                } else {
                    syncKeys(currentValue, pre[key]);
                }
            }
        }
    } else if (rootCurrentType == ARRAYTYPE && rootPreType == ARRAYTYPE) {
        if (current.length >= pre.length) {
            pre.forEach(function (item, index) {
                syncKeys(current[index], item);
            });
        }
    }
}

function _diff(current, pre, path, result) {
    if (current === pre) { return }
    var rootCurrentType = type(current);
    var rootPreType = type(pre);
    if (rootCurrentType == OBJECTTYPE) {
        if (rootPreType != OBJECTTYPE || Object.keys(current).length < Object.keys(pre).length) {
            setResult(result, path, current);
        } else {
            var loop = function ( key ) {
                var currentValue = current[key];
                var preValue = pre[key];
                var currentType = type(currentValue);
                var preType = type(preValue);
                if (currentType != ARRAYTYPE && currentType != OBJECTTYPE) {
                    if (currentValue != pre[key]) {
                        setResult(result, (path == '' ? '' : path + ".") + key, currentValue);
                    }
                } else if (currentType == ARRAYTYPE) {
                    if (preType != ARRAYTYPE) {
                        setResult(result, (path == '' ? '' : path + ".") + key, currentValue);
                    } else {
                        if (currentValue.length < preValue.length) {
                            setResult(result, (path == '' ? '' : path + ".") + key, currentValue);
                        } else {
                            currentValue.forEach(function (item, index) {
                                _diff(item, preValue[index], (path == '' ? '' : path + ".") + key + '[' + index + ']', result);
                            });
                        }
                    }
                } else if (currentType == OBJECTTYPE) {
                    if (preType != OBJECTTYPE || Object.keys(currentValue).length < Object.keys(preValue).length) {
                        setResult(result, (path == '' ? '' : path + ".") + key, currentValue);
                    } else {
                        for (var subKey in currentValue) {
                            _diff(currentValue[subKey], preValue[subKey], (path == '' ? '' : path + ".") + key + '.' + subKey, result);
                        }
                    }
                }
            };

            for (var key in current) loop( key );
        }
    } else if (rootCurrentType == ARRAYTYPE) {
        if (rootPreType != ARRAYTYPE) {
            setResult(result, path, current);
        } else {
            if (current.length < pre.length) {
                setResult(result, path, current);
            } else {
                current.forEach(function (item, index) {
                    _diff(item, pre[index], path + '[' + index + ']', result);
                });
            }
        }
    } else {
        setResult(result, path, current);
    }
}

function setResult(result, k, v) {
    // if (type(v) != FUNCTIONTYPE) {
        result[k] = v;
    // }
}

function type(obj) {
    return Object.prototype.toString.call(obj)
}

/*  */

function cloneWithData(vm) {
    // 确保当前 vm 所有数据被同步
    var dataKeys = [].concat(
        Object.keys(vm._data || {}),
        Object.keys(vm._computedWatchers || {}));

    var ret = dataKeys.reduce(function(ret, key) {
        ret[key] = vm[key];
        return ret
    }, Object.create(null));
    //TODO 需要把无用数据处理掉，比如 list=>l0 则 list 需要移除，否则多传输一份数据
    Object.assign(ret, vm.$mp.data || {});
    //remove observer
    return JSON.parse(JSON.stringify(ret))
}

var patch = function(oldVnode, vnode) {
    if (vnode === null) { //destroy
        return
    }
    if (this.mpType === 'page' || this.mpType === 'component') {
        var mpInstance = this.$mp[this.mpType];
        var data = cloneWithData(this);
        data.__webviewId__ = mpInstance.data.__webviewId__;
        var mpData = Object.create(null);
        Object.keys(data).forEach(function (key) { //仅同步 data 中有的数据
            mpData[key] = mpInstance.data[key];
        });
        var diffData = diff(data, mpData);
        if (Object.keys(diffData).length) {
            if (Object({"VUE_APP_PLATFORM":"mp-weixin","NODE_ENV":"development","BASE_URL":"/"}).VUE_APP_DEBUG) {
                console.log('[' + (+new Date) + '][' + (mpInstance.is || mpInstance.route) + '][' + this._uid +
                    ']差量更新',
                    JSON.stringify(diffData));
            }
            mpInstance.setData(diffData,function(){
                //TODO
            });
        }
    }
};

/*  */

function createEmptyRender() {

}

function mountComponent$1(
  vm,
  el,
  hydrating
) {
  if (!vm.mpType) {//main.js 中的 new Vue
    return vm
  }
  if (vm.mpType === 'app') {
    vm.$options.render = createEmptyRender;
  }
  if (!vm.$options.render) {
    vm.$options.render = createEmptyRender;
    if (true) {
      /* istanbul ignore if */
      if ((vm.$options.template && vm.$options.template.charAt(0) !== '#') ||
        vm.$options.el || el) {
        warn(
          'You are using the runtime-only build of Vue where the template ' +
          'compiler is not available. Either pre-compile the templates into ' +
          'render functions, or use the compiler-included build.',
          vm
        );
      } else {
        warn(
          'Failed to mount component: template or render function not defined.',
          vm
        );
      }
    }
  }

  var updateComponent = function () {
    vm._update(vm._render(), hydrating);
  };

  // we set this to vm._watcher inside the watcher's constructor
  // since the watcher's initial patch may call $forceUpdate (e.g. inside child
  // component's mounted hook), which relies on vm._watcher being already defined
  new Watcher(vm, updateComponent, noop, {
    before: function before() {
      if (vm._isMounted && !vm._isDestroyed) {
        callHook(vm, 'beforeUpdate');
      }
    }
  }, true /* isRenderWatcher */);
  hydrating = false;
  return vm
}

/*  */

function renderClass (
  staticClass,
  dynamicClass
) {
  if (isDef(staticClass) || isDef(dynamicClass)) {
    return concat(staticClass, stringifyClass(dynamicClass))
  }
  /* istanbul ignore next */
  return ''
}

function concat (a, b) {
  return a ? b ? (a + ' ' + b) : a : (b || '')
}

function stringifyClass (value) {
  if (Array.isArray(value)) {
    return stringifyArray(value)
  }
  if (isObject(value)) {
    return stringifyObject(value)
  }
  if (typeof value === 'string') {
    return value
  }
  /* istanbul ignore next */
  return ''
}

function stringifyArray (value) {
  var res = '';
  var stringified;
  for (var i = 0, l = value.length; i < l; i++) {
    if (isDef(stringified = stringifyClass(value[i])) && stringified !== '') {
      if (res) { res += ' '; }
      res += stringified;
    }
  }
  return res
}

function stringifyObject (value) {
  var res = '';
  for (var key in value) {
    if (value[key]) {
      if (res) { res += ' '; }
      res += key;
    }
  }
  return res
}

/*  */

var parseStyleText = cached(function (cssText) {
  var res = {};
  var listDelimiter = /;(?![^(]*\))/g;
  var propertyDelimiter = /:(.+)/;
  cssText.split(listDelimiter).forEach(function (item) {
    if (item) {
      var tmp = item.split(propertyDelimiter);
      tmp.length > 1 && (res[tmp[0].trim()] = tmp[1].trim());
    }
  });
  return res
});

// normalize possible array / string values into Object
function normalizeStyleBinding (bindingStyle) {
  if (Array.isArray(bindingStyle)) {
    return toObject(bindingStyle)
  }
  if (typeof bindingStyle === 'string') {
    return parseStyleText(bindingStyle)
  }
  return bindingStyle
}

/*  */


var MP_METHODS = ['createSelectorQuery', 'createIntersectionObserver', 'selectAllComponents', 'selectComponent'];

var customizeRE = /:/g;

var customize = cached(function (str) {
    return camelize(str.replace(customizeRE, '-'))
});

function getTarget(obj, path) {
    var parts = path.split('.');
    if (parts.length === 1) {
        return obj[parts[0]]
    }
    return getTarget(obj[parts[0]], parts.slice(1).join('.'))
}

function internalMixin(Vue) {

    var oldEmit = Vue.prototype.$emit;

    Vue.prototype.$emit = function(event) {
        if (this.$mp && event) {
            //click-left,click:left => clickLeft
            this.$mp[this.mpType]['triggerEvent'](customize(event), toArray(arguments, 1));
        }
        return oldEmit.apply(this, arguments)
    };

    MP_METHODS.forEach(function (method) {
        Vue.prototype[method] = function(args) {
            if (this.$mp) {
                return this.$mp[this.mpType][method](args)
            }
        };
    });

    Vue.prototype.__init_provide = initProvide;

    Vue.prototype.__init_injections = initInjections;

    Vue.prototype.__call_hook = function(hook, args) {
        var vm = this;
        // #7573 disable dep collection when invoking lifecycle hooks
        pushTarget();
        var handlers = vm.$options[hook];
        var info = hook + " hook";
        var ret;
        if (handlers) {
            for (var i = 0, j = handlers.length; i < j; i++) {
                ret = invokeWithErrorHandling(handlers[i], vm, args ? [args] : null, vm, info);
            }
        }
        if (vm._hasHookEvent) {
            vm.$emit('hook:' + hook);
        }
        popTarget();
        return ret
    };

    Vue.prototype.__set_model = function(target, value, modifiers) {
        if (Array.isArray(modifiers)) {
            if (modifiers.includes('trim')) {
                value = value.trim();
            }
            if (modifiers.includes('number')) {
                value = this._n(value);
            }
        }
        if (target.indexOf('.') === -1) {
            this[target] = value;
        } else {
            var paths = target.split('.');
            var key = paths.pop();
            this.$set(getTarget(this, paths.join('.')), key, value);
        }
    };

    Vue.prototype.__set_sync = function(target, key, value) {
        if (!target) {
            this[key] = value;
        } else {
            this.$set(getTarget(this, target), key, value);
        }
    };

    Vue.prototype.__get_orig = function(item) {
        if (isPlainObject(item)) {
            return item['$orig'] || item
        }
        return item
    };


    Vue.prototype.__get_class = function(dynamicClass, staticClass) {
        return renderClass(staticClass, dynamicClass)
    };

    Vue.prototype.__get_style = function(dynamicStyle, staticStyle) {
        if (!dynamicStyle && !staticStyle) {
            return ''
        }
        var dynamicStyleObj = normalizeStyleBinding(dynamicStyle);
        var styleObj = staticStyle ? extend(staticStyle, dynamicStyleObj) : dynamicStyleObj;
        return Object.keys(styleObj).map(function (name) { return ((hyphenate(name)) + ":" + (styleObj[name])); }).join(';')
    };

}

/*  */

var LIFECYCLE_HOOKS$1 = [
    //App
    'onLaunch',
    'onShow',
    'onHide',
    'onUniNViewMessage',
    //Page
    'onLoad',
    // 'onShow',
    'onReady',
    // 'onHide',
    'onUnload',
    'onPullDownRefresh',
    'onReachBottom',
    'onTabItemTap',
    'onShareAppMessage',
    'onPageScroll',
    'onNavigationBarButtonTap',
    'onBackPress',
    'onNavigationBarSearchInputChanged',
    'onNavigationBarSearchInputConfirmed',
    'onNavigationBarSearchInputClicked',
    //Component
    'onReady', // 兼容旧版本，应该移除该事件
    'onPageShow',
    'onPageHide',
    'onPageResize'
];
function lifecycleMixin$1(Vue) {

    //fixed vue-class-component
    var oldExtend = Vue.extend;
    Vue.extend = function(extendOptions) {
        extendOptions = extendOptions || {};

        var methods = extendOptions.methods;
        if (methods) {
            Object.keys(methods).forEach(function (methodName) {
                if (LIFECYCLE_HOOKS$1.includes(methodName)) {
                    extendOptions[methodName] = methods[methodName];
                    delete methods[methodName];
                }
            });
        }
        
        return oldExtend.call(this,extendOptions)
    };

    var strategies = Vue.config.optionMergeStrategies;
    var mergeHook = strategies.created;
    LIFECYCLE_HOOKS$1.forEach(function (hook) {
        strategies[hook] = mergeHook;
    });
}

/*  */

// install platform patch function
Vue.prototype.__patch__ = patch;

// public mount method
Vue.prototype.$mount = function(
    el ,
    hydrating 
) {
    return mountComponent$1(this, el, hydrating)
};

lifecycleMixin$1(Vue);
internalMixin(Vue);

/*  */

/* harmony default export */ __webpack_exports__["default"] = (Vue);

/* WEBPACK VAR INJECTION */}.call(this, __webpack_require__(/*! ./../../../../../webpack/buildin/global.js */ "./node_modules/webpack/buildin/global.js")))

/***/ }),

/***/ "./node_modules/vue-loader/lib/runtime/componentNormalizer.js":
/*!********************************************************************!*\
  !*** ./node_modules/vue-loader/lib/runtime/componentNormalizer.js ***!
  \********************************************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "default", function() { return normalizeComponent; });
/* globals __VUE_SSR_CONTEXT__ */

// IMPORTANT: Do NOT use ES2015 features in this file (except for modules).
// This module is a runtime utility for cleaner component module output and will
// be included in the final webpack user bundle.

function normalizeComponent (
  scriptExports,
  render,
  staticRenderFns,
  functionalTemplate,
  injectStyles,
  scopeId,
  moduleIdentifier, /* server only */
  shadowMode /* vue-cli only */
) {
  // Vue.extend constructor export interop
  var options = typeof scriptExports === 'function'
    ? scriptExports.options
    : scriptExports

  // render functions
  if (render) {
    options.render = render
    options.staticRenderFns = staticRenderFns
    options._compiled = true
  }

  // functional template
  if (functionalTemplate) {
    options.functional = true
  }

  // scopedId
  if (scopeId) {
    options._scopeId = 'data-v-' + scopeId
  }

  var hook
  if (moduleIdentifier) { // server build
    hook = function (context) {
      // 2.3 injection
      context =
        context || // cached call
        (this.$vnode && this.$vnode.ssrContext) || // stateful
        (this.parent && this.parent.$vnode && this.parent.$vnode.ssrContext) // functional
      // 2.2 with runInNewContext: true
      if (!context && typeof __VUE_SSR_CONTEXT__ !== 'undefined') {
        context = __VUE_SSR_CONTEXT__
      }
      // inject component styles
      if (injectStyles) {
        injectStyles.call(this, context)
      }
      // register component module identifier for async chunk inferrence
      if (context && context._registeredComponents) {
        context._registeredComponents.add(moduleIdentifier)
      }
    }
    // used by ssr in case component is cached and beforeCreate
    // never gets called
    options._ssrRegister = hook
  } else if (injectStyles) {
    hook = shadowMode
      ? function () { injectStyles.call(this, this.$root.$options.shadowRoot) }
      : injectStyles
  }

  if (hook) {
    if (options.functional) {
      // for template-only hot-reload because in that case the render fn doesn't
      // go through the normalizer
      options._injectStyles = hook
      // register for functioal component in vue file
      var originalRender = options.render
      options.render = function renderWithStyleInjection (h, context) {
        hook.call(context)
        return originalRender(h, context)
      }
    } else {
      // inject component registration as beforeCreate hook
      var existing = options.beforeCreate
      options.beforeCreate = existing
        ? [].concat(existing, hook)
        : [hook]
    }
  }

  return {
    exports: scriptExports,
    options: options
  }
}


/***/ }),

/***/ "./node_modules/webpack/buildin/global.js":
/*!***********************************!*\
  !*** (webpack)/buildin/global.js ***!
  \***********************************/
/*! no static exports found */
/***/ (function(module, exports) {

var g;

// This works in non-strict mode
g = (function() {
	return this;
})();

try {
	// This works if eval is allowed (see CSP)
	g = g || new Function("return this")();
} catch (e) {
	// This works if the window reference is available
	if (typeof window === "object") g = window;
}

// g can still be undefined, but nothing to do about it...
// We return undefined, instead of nothing here, so it's
// easier to handle this case. if(!global) { ...}

module.exports = g;


/***/ })

}]);
//# sourceMappingURL=../../.sourcemap/mp-weixin/common/vendor.js.map